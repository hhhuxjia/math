# 积分性质与概念专题

## 一、定积分的定义

### 1.1 定积分的本质定义（黎曼积分）

设 $f(x)$ 在 $[a,b]$ 上有定义，在 $[a,b]$ 中任意插入 $n-1$ 个分点：

$$
a = x_0 < x_1 < x_2 < \cdots < x_{n-1} < x_n = b
$$

把 $[a,b]$ 分成 $n$ 个子区间 $[x_{i-1}, x_i]$，各小区间长度为 $\Delta x_i = x_i - x_{i-1}$。

在各小区间上任取一点 $\xi_i \in [x_{i-1}, x_i]$，作和式：

$$
S_n = \sum_{i=1}^n f(\xi_i) \Delta x_i
$$

记 $\lambda = \max\limits_{1 \le i \le n} \Delta x_i$，若极限

$$
\lim_{\lambda \to 0} \sum_{i=1}^n f(\xi_i) \Delta x_i
$$

存在且**不依赖于分点选取方式与 $\xi_i$ 的取法**，则称 $f(x)$ 在 $[a,b]$ 上**黎曼可积**，称该极限值为 $f(x)$ 在 $[a,b]$ 上的定积分，记为：

$$
\int_a^b f(x)\, dx
$$

### 1.2 定积分的几何意义

- 当 $f(x) \ge 0$ 时，$\int_a^b f(x)\, dx$ 表示曲线 $y=f(x)$ 与 $x$ 轴、直线 $x=a$、$x=b$ 所围曲边梯形的面积
- 当 $f(x) \le 0$ 时，定积分表示面积的负值
- 一般情形下，定积分是 **x 轴上方面积减去下方面积** 的代数和

### 1.3 定积分的精确定义（$\varepsilon-\delta$ 语言）

对于任意 $\varepsilon > 0$，存在 $\delta > 0$，使得对 $[a,b]$ 的任何分划，只要 $\lambda < \delta$，不论 $\xi_i$ 如何选取，都有：

$$
\left| \sum_{i=1}^n f(\xi_i)\Delta x_i - I \right| < \varepsilon
$$

则称 $I$ 为 $f(x)$ 在 $[a,b]$ 上的定积分。

---

## 二、定积分存在的条件

### 2.1 定积分存在的必要条件

> **定理**：若 $f(x)$ 在 $[a,b]$ 上可积，则 $f(x)$ 在 $[a,b]$ 上**必须有界**。

**逆否命题**：若 $f(x)$ 在 $[a,b]$ 上**无界**，则 $f(x)$ 在 $[a,b]$ 上**一定不可积**。

> ⚠️ **注意**：有界只是可积的**必要条件**，**不是充分条件**（见下文 Dirichlet 函数反例）。

### 2.2 定积分存在的充要条件（Darboux 定理）

$f(x)$ 在 $[a,b]$ 上可积的充要条件是：

> 对于任意的 $\varepsilon > 0$，总存在一个分划，使得**振幅之和** $\sum\limits_{i=1}^n \omega_i \Delta x_i < \varepsilon$

其中 $\omega_i = \sup\limits_{x\in[x_{i-1},x_i]} f(x) - \inf\limits_{x\in[x_{i-1},x_i]} f(x)$ 为第 $i$ 个小区间上的振幅。

### 2.3 定积分存在的充分条件

#### 条件一：连续性（最常用）

> 若 $f(x)$ 在闭区间 $[a,b]$ 上**连续**，则 $f(x)$ 在 $[a,b]$ 上可积。

#### 条件二：只有有限个第一类间断点

> 若 $f(x)$ 在 $[a,b]$ 上**有界，且只有有限个第一类间断点**（可去间断点或跳跃间断点），则 $f(x)$ 在 $[a,b]$ 上可积。

**这类函数称为**：**分段连续函数**，定积分存在。

#### 条件三：只有有限个第二类振荡间断点（有界情形）

> 若 $f(x)$ 在 $[a,b]$ 上**有界，且只有有限个振荡间断点**（如 $x\sin\frac{1}{x}$ 在 $x=0$ 处），则仍可积。

#### 条件四：单调有界

> 若 $f(x)$ 在 $[a,b]$ 上**单调**，则 $f(x)$ 在 $[a,b]$ 上可积（不一定需要连续）。

#### 条件五：只有有限个间断点

> 若 $f(x)$ 在 $[a,b]$ 上**有界，且只有有限个间断点**（无论类型），则 $f(x)$ 在 $[a,b]$ 上可积。

> ✅ **记忆口诀**：**有界 + 间断点有限 → 可积**（这是最常用的判别准则）。

### 2.4 不可积的反例

#### 反例 1：无界函数
$$
f(x) = \frac{1}{x},\quad x\in(0,1]
$$
在 $x=0$ 附近无界，**不可积**（但可考虑**反常积分**）。

#### 反例 2：Dirichlet 函数（有界但处处不连续）
$$
D(x) = \begin{cases}
1, & x \in \mathbb{Q} \\
0, & x \notin \mathbb{Q}
\end{cases}
$$
- 在任意区间上有界（$0 \le D(x) \le 1$）
- 但处处不连续，且每点的振幅 $\omega_i = 1$
- **不可积**（黎曼意义下）

#### 反例 3：Riemann 函数（可积的"反常"例子）
$$
R(x) = \begin{cases}
\frac{1}{q}, & x = \frac{p}{q}\ (\text{既约分数}) \\
0, & x \text{为无理数}
\end{cases}
$$
- 在 $[0,1]$ 上有界
- 在有理点不连续，在无理点连续
- 但是**可积**（间断点集为可数集）

> 💡 **重要结论**：**有界 + 间断点集为零测集 → 可积**（Lebesgue 判别法）

---

## 三、原函数存在定理

### 3.1 原函数的定义

设 $F'(x) = f(x)$ 在区间 $I$ 上成立，则称 $F(x)$ 是 $f(x)$ 在区间 $I$ 上的一个**原函数**。

### 3.2 原函数存在定理（连续 ⇒ 原函数存在）

> **定理**：若 $f(x)$ 在区间 $I$ 上**连续**，则 $f(x)$ 在 $I$ 上**存在原函数**。

更具体地，变上限积分 $\displaystyle F(x) = \int_a^x f(t)\, dt$ 就是 $f(x)$ 的一个原函数（$a \in I$）。

### 3.3 原函数存在性与可积性的关系

⚠️ **这两个概念没有直接的包含关系！** 务必区分：

| 情形 | 定积分存在 | 原函数存在 | 示例 |
|------|-----------|-----------|------|
| $f(x)$ 在 $[a,b]$ 上连续 | ✅ 必存在 | ✅ 必存在 | $\sin x, x^2$ |
| $f(x)$ 有有限个第一类间断点 | ✅ 存在 | ❌ 不存在 | $f(x)=\operatorname{sgn}(x)$ |
| $f(x)$ 有第二类振荡间断点但可积 | ✅ 存在 | ❌ 不存在 | $f(x)=x\sin\frac{1}{x}$（补充定义 $x=0$ 处为 0） |
| $f(x)$ 有第二类无穷间断点 | ❌ 不存在 | ❌ 不存在 | $f(x)=\frac{1}{x}$ 在 $x=0$ |
| 有界但原函数存在 | ✅ 存在 | ✅ 存在 | $f(x)$ 连续时 |
| Dirichlet 函数 | ❌ 不存在 | ❌ 不存在 | 无处连续，不可积，无原函数 |

---

## 四、间断点与原函数的关系（重要专题）

### 4.1 第一类间断点 → 没有原函数

> **定理**：若 $x_0$ 是 $f(x)$ 的第一类间断点（可去或跳跃），则 $f(x)$ 在包含 $x_0$ 的区间上**不存在原函数**。

**证明思路**：假设存在原函数 $F(x)$，则 $F'(x)=f(x)$。但导函数 $F'(x)$ 具有**介值性**（达布定理），第一类间断点破坏了介值性，矛盾。

### 4.2 第二类间断点 → 可能有无穷间断点（无原函数）

- 若 $x_0$ 是 $f(x)$ 的**无穷间断点**（$\lim f(x) = \infty$），则 $f(x)$ 在该点附近无界，**不存在原函数**（因为导函数不可能趋于无穷而不破坏介值性）
- 若 $x_0$ 是 $f(x)$ 的**振荡间断点**（有界），则**可能存在原函数**

### 4.3 典型例子：第二类振荡间断点但有原函数

> **关键反例**：
$$
f(x) = \begin{cases}
2x\sin\frac{1}{x} - \cos\frac{1}{x}, & x \neq 0 \\
0, & x = 0
\end{cases}
$$

- $x=0$ 是 $f(x)$ 的第二类振荡间断点（$\lim\limits_{x\to 0} f(x)$ 不存在）
- 但 $F(x) = \begin{cases} x^2\sin\frac{1}{x}, & x\neq 0 \\ 0, & x=0 \end{cases}$ 是 $f(x)$ 的原函数（$F'(0)=0$）
- $f(x)$ 在任意包含 0 的闭区间上**可积**（有界 + 只有一个间断点）

> 💡 **结论**：**有第二类振荡间断点 ≠ 没有原函数**！有些振荡间断点反而存在原函数。

### 4.4 原函数存在与定积分存在的关系总结

| 性质 | 关系图示 |
|------|---------|
| 连续 $\Rightarrow$ 原函数存在 | ✅ 一定成立 |
| 连续 $\Rightarrow$ 可积 | ✅ 一定成立 |
| 可积 $\nRightarrow$ 原函数存在 | ❌ 不一定（有限个第一类间断点时可积但无原函数）|
| 原函数存在 $\nRightarrow$ 可积 | ❌ 不一定（上例中 $f$ 有振荡间断点但可积，两者都成立）|
| 原函数存在 $\nRightarrow$ 连续 | ❌ 原函数可以有不连续的导函数 |

---

## 五、变上限积分及其性质

### 5.1 定义

设 $f(x)$ 在 $[a,b]$ 上可积，则称：

$$
\Phi(x) = \int_a^x f(t)\, dt,\quad x \in [a,b]
$$

为**变上限积分函数**（积分上限函数）。

### 5.2 变上限积分的连续性

> **定理**：若 $f(x)$ 在 $[a,b]$ 上**可积**，则 $\Phi(x) = \int_a^x f(t)\, dt$ 在 $[a,b]$ 上**连续**。

**证明大意**：$\displaystyle |\Phi(x+\Delta x) - \Phi(x)| = \left|\int_x^{x+\Delta x} f(t)\, dt\right| \le M|\Delta x|$（因为 $f$ 有界），当 $\Delta x\to 0$ 时趋于 0。

> ✅ **重要结论**：**可积 ⇒ 变上限积分连续**，无论 $f$ 在原区间上有多少间断点。

### 5.3 变上限积分的可积性

> **定理**：若 $f(x)$ 在 $[a,b]$ 上**可积**，则 $\Phi(x) = \int_a^x f(t)\, dt$ 在 $[a,b]$ 上**可积**。

实际上，$\Phi(x)$ 不仅可积，而且连续，因此自动可积（更强）。

### 5.4 变上限积分的可导性

#### 情形一：$f(x)$ 连续

> 若 $f(x)$ 在 $[a,b]$ 上**连续**，则 $\Phi(x) = \int_a^x f(t)\, dt$ 在 $[a,b]$ 上**可导**，且：
>
> $$
> \Phi'(x) = f(x)
> $$

这就是**微积分基本定理**（牛顿-莱布尼茨公式的基础）。

#### 情形二：$f(x)$ 只有有限个第一类间断点

> 若 $f(x)$ 在 $[a,b]$ 上**可积**，且 $x_0$ 是 $f(x)$ 的第一类间断点，则 $\Phi(x)$ 在 $x_0$ 处**不可导**。
>
> 左右导数存在但不相等：
> $$
> \Phi'_-(x_0) = f(x_0^-),\quad \Phi'_+(x_0) = f(x_0^+)
> $$

> ✅ 在连续点处，$\Phi'(x)=f(x)$ 仍然成立。

#### 情形三：$f(x)$ 有第二类间断点

- 若为**可去间断点**（重新定义后连续），则 $\Phi'(x_0)=$ 重新定义后的值
- 若为**振荡间断点**（如上例 $2x\sin\frac{1}{x}-\cos\frac{1}{x}$），则 $\Phi'(x)$ 在振荡点处可能存在导数值（可能需要用定义求导）

### 5.5 变上限积分可导性总结图

| $f(x)$ 在 $x_0$ 处 | $\Phi(x)=\int_a^x f$ 在 $x_0$ 处 |
|-------------------|-------------------------------|
| 连续 | 可导，且 $\Phi'(x_0)=f(x_0)$ |
| 可去间断点 | 不可导 |
| 跳跃间断点 | 不可导（左右导数存在但不相等） |
| 第二类振荡间断点（有界） | 导函数可能存在（需具体分析） |
| 第一类间断点 | ✅ **可积**，✅ $\Phi$ **连续**，❌ $\Phi$ **不可导** |

> 💡 **核心结论**：**变上限积分 $\Phi(x)$ 一定比 $f(x)$ "好"（更光滑）**：
> - 可积 $\rightarrow$ 连续
> - 连续 $\rightarrow$ 可导
> - 可导 $\rightarrow$ 与原函数关系
> - 整体上看，变上限积分会"抹平"间断点的影响（使函数连续），但无法"抹平"到可导（第一类间断点处仍不可导）

---

## 六、奇偶函数与变上限积分

### 6.1 奇偶函数的基本性质

| 性质 | 说明 |
|------|------|
| 奇 $+$ 奇 $=$ 奇 | |
| 偶 $+$ 偶 $=$ 偶 | |
| 奇 $\times$ 奇 $=$ 偶 | |
| 奇 $\times$ 偶 $=$ 奇 | |
| 偶 $\times$ 偶 $=$ 偶 | |

### 6.2 奇偶函数的定积分性质

> 设 $f(x)$ 在 $[-a,a]$ 上可积：

- 若 $f(x)$ 是**奇函数**，则 $\displaystyle\int_{-a}^{a} f(x)\, dx = 0$
- 若 $f(x)$ 是**偶函数**，则 $\displaystyle\int_{-a}^{a} f(x)\, dx = 2\int_{0}^{a} f(x)\, dx$

### 6.3 奇偶函数与变上限积分

#### 情形：奇函数的变上限积分

> 若 $f(x)$ 是**奇函数**，则 $\displaystyle F(x) = \int_0^x f(t)\, dt$ 是**偶函数**。

**证明**：
$$
F(-x) = \int_0^{-x} f(t)\, dt \xlongequal{t=-u} \int_0^x f(-u)(-du) = -\int_0^x [-f(u)]\, du = \int_0^x f(u)\, du = F(x)
$$

> ✅ 即：**奇函数积分得偶函数**（积分下限为 0 时）

#### 情形：偶函数的变上限积分

> 若 $f(x)$ 是**偶函数**，则 $\displaystyle F(x) = \int_0^x f(t)\, dt$ 是**奇函数**。

**证明**：
$$
F(-x) = \int_0^{-x} f(t)\, dt \xlongequal{t=-u} \int_0^x f(-u)(-du) = -\int_0^x f(u)\, du = -F(x)
$$

> ✅ 即：**偶函数积分得奇函数**（积分下限为 0 时）

#### 重要提醒：积分下限的影响

若积分下限不为 0，则奇偶性可能被破坏：

$$
\int_a^x f(t)\, dt = \int_0^x f(t)\, dt - \int_0^a f(t)\, dt
$$

- 多了一个常数项 $\int_0^a f(t)\, dt$
- 若 $f$ 为奇函数：$\int_0^a f = $ 偶函数值在 $a$ 处的值，该常数可能使得偶性仅变为平移
- 若 $f$ 为偶函数：$\int_0^a f = $ 某个常数（不一定为 0），奇函数加常数仍为奇函数当且仅当常数为 0

> 💡 **常用结论总结**：
> $$
> f\text{ 为奇函数 } \Rightarrow F(x)=\int_0^x f(t)\,dt \text{ 为偶函数}
> $$
> $$
> f\text{ 为偶函数 } \Rightarrow F(x)=\int_0^x f(t)\,dt \text{ 为奇函数}
> $$
> $$
> f\text{ 为奇函数 } \Rightarrow \int_a^x f(t)\,dt \text{ 既非奇也非偶（除非 }a=0\text{）}
> $$

### 6.4 奇偶函数变上限积分可导性

由于奇函数/偶函数在连续点处光滑性质传递：

- 若 $f$ 连续且为奇函数 → $F$ 可导且为偶函数 → $F'=f$ 为奇函数（导函数奇偶性互换）
- 若 $f$ 连续且为偶函数 → $F$ 可导且为奇函数 → $F'=f$ 为偶函数

> 💡 记忆：**求导一次，奇偶性互换；积分一次，奇偶性也互换**

---

## 七、各概念关系总览

### 7.1 函数可积性 + 原函数存在性 + 连续性关系图

```
                    连续
                    / \
                   /   \
                  /     \
                 /       \
          原函数存在 ---→ 可积
                \         /
                 \       /
                  \     /
                   \   /
            第一类间断点有限
                    |
                    ↓
             可积 + 无原函数
```

### 7.2 变上限积分性质递进

```
f可积  →  Φ连续  →  Φ可积
                        ↓
                   f在x0连续 → Φ在x0可导
                   f在x0第一类间断 → Φ在x0不可导（但左右导数存在）
                   f在x0振荡间断 → Φ在x0处可能可导也可能不可导
```

### 7.3 常见函数类型的性质一览

| 函数类型 | 可积性 | 原函数存在性 | 变上限积分性质 |
|---------|--------|------------|--------------|
| 连续函数 | ✅ 可积 | ✅ 存在（即变上限积分）| 连续、可导 |
| 有界、有限个第一类间断点 | ✅ 可积 | ❌ 不存在 | 连续、在间断点不可导 |
| 有界、有限个第二类振荡间断点 | ✅ 可积 | ❌ 一般不存在 | 连续、可导性看情况 |
| 无界函数 | ❌ 不可积（黎曼）| ❌ 不存在 | 不可讨论 |
| Dirichlet 函数 | ❌ 不可积 | ❌ 不存在 | 不存在 |
| $x\sin\frac1x$ 型（补充后连续） | ✅ 可积 | ✅ 存在（连续）| 连续可导 |
| $2x\sin\frac1x-\cos\frac1x$ 型 | ✅ 可积 | ✅ 存在（但导函数有振荡）| 连续、可导 |

---

## 八、重要题目类型与方法总结

### 8.1 利用定积分定义求极限

$$
\lim_{n\to\infty} \frac{1}{n}\sum_{i=1}^n f\left(\frac{i}{n}\right) = \int_0^1 f(x)\, dx
$$

推广形式：

$$
\lim_{n\to\infty} \frac{b-a}{n}\sum_{i=1}^n f\left(a + \frac{b-a}{n}i\right) = \int_a^b f(x)\, dx
$$

### 8.2 变上限积分求导公式

$$
\frac{d}{dx} \int_{a(x)}^{b(x)} f(t)\, dt = f(b(x))\cdot b'(x) - f(a(x))\cdot a'(x)
$$

### 8.3 判断可积性的一般步骤

1. 检查函数在区间上是否**有界**（无界 ⇒ 不可积）
2. 检查**间断点个数**（有限个 ⇒ 可积）
3. 若间断点无限个，判断是否为零测集

### 8.4 判断原函数存在性的一般步骤

1. 检查函数是否**连续**（连续 ⇒ 原函数存在）
2. 若有间断点，检查是否为**第一类**（第一类 ⇒ 原函数不存在）
3. 若为第二类振荡间断点，需具体分析（可能可能有原函数）

---

## 九、易错点与典型例题

### 9.1 易错点

| 易错点 | 正确理解 |
|-------|---------|
| "可积必有原函数" | ❌ 错！可积不一定有原函数（例：$f(x)=\operatorname{sgn}(x)$ 可积但无原函数）|
| "原函数存在必可积" | ❌ 错！原函数存在不一定黎曼可积（只是通常两者都成立）|
| "有间断点必不可积" | ❌ 错！有限个间断点仍可积 |
| "有间断点必无原函数" | ❌ 错！第二类振荡间断点可能有原函数 |
| "变上限积分可导 ⇔ $f$ 连续" | ✅ 正确，变上限积分可导的充要条件是 $f$ 在该点连续 |
| "奇函数积分一定是偶函数" | ✅ 仅当下限为 0 时成立 |

### 9.2 典型例题方向

1. **定积分定义求和式极限**（利用 $\frac{1}{n}\sum f(\frac{i}{n}) = \int_0^1$）
2. **判断函数是否可积**（有界 + 间断点有限）
3. **判断函数是否有原函数**（连续 ⇒ 有，第一类间断 ⇒ 无，第二类振荡 ⇒ 可能）
4. **变上限积分求导**（带参数、含 $x$ 上下限）
5. **奇偶函数与变上限积分综合题**（利用对称性化简）
6. **含间断点的变上限积分**（判断可导性、连续点导数等）

---

> 📝 **关键记忆**：
> - **可积** ≈ **有界 + 间断点有限**
> - **有原函数** ≈ **连续** 或 **第二类振荡间断点（特殊情形）**
> - **第一类间断点** ⇒ **可积但无原函数**
> - **变上限积分**总能**提高一个光滑度**：可积→连续，连续→可导
> - **奇偶性互换**：积分一次奇偶互换，求导一次也奇偶互换