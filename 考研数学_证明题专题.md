# 考研数学证明题专题

> 本专题把微分中值定理、积分中值定理和常见积分不等式合并整理。重点不是堆例题，而是先认出可用定理，再把目标式改写成定理的结论。

---

# 一、证明题先做什么

证明题通常先检查四件事：

1. **定义域和光滑性**：连续、可导、二阶可导分别在哪个区间成立。
2. **目标的类型**：要证明存在一个点、两个点，还是证明恒等式/不等式。
3. **端点条件**：罗尔定理要端点函数值相等；积分中值定理要连续性或不变号条件。
4. **能否构造辅助函数**：把目标改造成 $F'(\xi)=0$、$F'(\xi)=k$ 或某个差商。

常见的“定理匹配”：

| 目标形式 | 优先考虑 |
|:---|:---|
| $f'(\xi)=0$ | 费马引理、罗尔定理 |
| $f'(\xi)=\dfrac{f(b)-f(a)}{b-a}$ | 拉格朗日中值定理 |
| $\dfrac{f'(\xi)}{g'(\xi)}=\dfrac{f(b)-f(a)}{g(b)-g(a)}$ | 柯西中值定理 |
| $f(\xi)=\dfrac1{b-a}\int_a^b f(x)\,dx$ | 积分第一中值定理 |
| $f(\xi)\int_a^b g=\int_a^b fg$ | 积分第一中值定理的推广 |
| 两个差商比较 | 拉格朗日中值定理 + 区间包含/加权平均 |
| 导数严格单调、三点不等式 | 凸性、拉格朗日中值定理 |
| 导数取遍中间值 | 达布定理 |

---

# 二、必须熟练调用的定理

## 1. 费马引理

若 $f$ 在点 $x_0$ 的邻域内有定义，$x_0$ 是内点，$f$ 在 $x_0$ 可导，且 $x_0$ 是局部极值点，则
$$
\boxed{f'(x_0)=0.}
$$

它是极值点的必要条件，不是充分条件。$f'(x_0)=0$ 还可能是拐点或其他驻点。

## 2. 罗尔定理

若 $f$ 在 $[a,b]$ 上连续、在 $(a,b)$ 内可导，且 $f(a)=f(b)$，则存在 $\xi\in(a,b)$，使
$$
\boxed{f'(\xi)=0.}
$$

**构造法：** 目标若为 $f'(\xi)=\varphi'(\xi)$，令 $F(x)=f(x)-\varphi(x)$；若能让 $F(a)=F(b)$，就对 $F$ 用罗尔定理。

## 3. 拉格朗日中值定理

若 $f$ 在 $[a,b]$ 上连续、在 $(a,b)$ 内可导，则存在 $\xi\in(a,b)$，使
$$
\boxed{f'(\xi)=\frac{f(b)-f(a)}{b-a}.}
$$

差商是割线斜率，$f'(\xi)$ 是某条切线斜率。证明不等式时，经常先把不同区间的差商分别换成导数。

## 4. 柯西中值定理

若 $f,g$ 在 $[a,b]$ 上连续、在 $(a,b)$ 内可导，且 $g(b)\ne g(a)$，则存在 $\xi\in(a,b)$，使
$$
\boxed{
\frac{f(b)-f(a)}{g(b)-g(a)}
=\frac{f'(\xi)}{g'(\xi)}
}
$$
（通常还需在所用区间内 $g'(x)\ne0$）。

把 $g(x)=x$ 即得拉格朗日中值定理。看到分母是 $g(b)-g(a)$ 或目标是导数比值时，不要硬凑罗尔，应优先考虑柯西定理。

## 5. 泰勒公式与拉格朗日余项

若 $f$ 在 $x_0$ 附近具有 $n+1$ 阶导数，则
$$
f(x)=\sum_{k=0}^{n}\frac{f^{(k)}(x_0)}{k!}(x-x_0)^k
+\frac{f^{(n+1)}(\xi)}{(n+1)!}(x-x_0)^{n+1},
$$
其中 $\xi$ 在 $x_0$ 与 $x$ 之间。

证明题中主要用来把函数值差写成导数或高阶导数的量，并判断余项符号。

## 6. 积分第一中值定理

若 $f$ 在 $[a,b]$ 上连续，则存在 $\xi\in[a,b]$，使
$$
\boxed{\int_a^b f(x)\,dx=f(\xi)(b-a).}
$$

若 $f$ 连续，$g$ 可积且在 $[a,b]$ 上不变号，则存在 $\xi\in[a,b]$，使
$$
\boxed{\int_a^b f(x)g(x)\,dx=f(\xi)\int_a^b g(x)\,dx.}
$$

若 $g\ge0$，积分就是带权平均；若 $g$ 变号，不能直接套这个形式。

## 7. 积分第二中值定理（了解常用形式）

若 $f$ 单调，$g$ 可积，则存在 $\xi\in[a,b]$，使
$$
\int_a^b f(x)g(x)\,dx
=f(a)\int_a^\xi g(x)\,dx
+f(b)\int_\xi^b g(x)\,dx.
$$

它常用于单调因子乘振荡因子的积分估计；考研证明题中先检查是否能用第一中值定理或分部积分。

## 8. 介值定理与达布定理

**介值定理：** 若 $f$ 在 $[a,b]$ 上连续，且 $f(a)f(b)<0$，则存在 $\xi\in(a,b)$，使 $f(\xi)=0$。更一般地，$f$ 取到端点函数值之间的每个数。

**达布定理：** 即使 $f'$ 不连续，只要 $f$ 在 $(a,b)$ 内可导，$f'$ 仍具有介值性：若 $x_1<x_2$，且 $f'(x_1)<\mu<f'(x_2)$，则存在 $\xi\in(x_1,x_2)$，使
$$
f'(\xi)=\mu.
$$

达布定理的用途是排除“导数跳跃”。例如证明导数不可能从负值直接跳到正值而不经过 $0$。

---

# 三、核心专题：$f''\ge0$ 与下凸函数

## 1. 定义与等价条件

在区间 $I$ 上，若
$$
f''(x)\ge0,
$$
则 $f'$ 单调不减，称 $f$ 在 $I$ 上**下凸（通常也称凸函数）**。有些教材把图像开口向下的函数称“凹函数”，术语可能相反；考试中以 $f''$ 的符号和不等式方向为准。

在 $f$ 二阶可导的前提下，下面四个条件等价：

1. $f''(x)\ge0$；
2. $f'(x)$ 单调不减；
3. 任意 $x_1<x_2<x_3$，有
   $$
   \frac{f(x_2)-f(x_1)}{x_2-x_1}
   \le
   \frac{f(x_3)-f(x_2)}{x_3-x_2};
   $$
4. 任意 $x_1<x_2<x_3$，有
   $$
   f(x_2)\le
   \frac{x_3-x_2}{x_3-x_1}f(x_1)
   +\frac{x_2-x_1}{x_3-x_1}f(x_3).
   $$

### 证明 $1\Longleftrightarrow2$

若 $f''\ge0$，对任意 $u<v$，由拉格朗日中值定理，存在 $\xi\in(u,v)$，使
$$
f'(v)-f'(u)=f''(\xi)(v-u)\ge0.
$$
故 $f'$ 单调不减。

反过来，若 $f'$ 单调不减，任取 $x\in I$，由导数定义
$$
f''(x)=\lim_{h\to0}\frac{f'(x+h)-f'(x)}h.
$$
当 $h>0$ 时，分子非负、分母为正；当 $h<0$ 时，分子非正、分母也为负。因此差商均非负，极限满足 $f''(x)\ge0$。

### 证明 $2\Longrightarrow3$

分别在 $[x_1,x_2]$、$[x_2,x_3]$ 上用拉格朗日中值定理，存在
$$
\xi_1\in(x_1,x_2),\qquad \xi_2\in(x_2,x_3),
$$
使两段割线斜率分别为 $f'(\xi_1)$、$f'(\xi_2)$。由于 $\xi_1<\xi_2$ 且 $f'$ 单调不减，
$$
\frac{f(x_2)-f(x_1)}{x_2-x_1}
=f'(\xi_1)
\le f'(\xi_2)
=\frac{f(x_3)-f(x_2)}{x_3-x_2}.
$$

### 证明 $3\Longrightarrow2$

固定 $u<v$，对任意 $x\in(u,v)$，条件 3 给出
$$
\frac{f(x)-f(u)}{x-u}
\le
\frac{f(v)-f(x)}{v-x}.
$$
令 $x\to u^+$，得到
$$
f'(u)\le\frac{f(v)-f(u)}{v-u};
$$
令 $x\to v^-$，得到
$$
\frac{f(v)-f(u)}{v-u}\le f'(v).
$$
所以 $f'(u)\le f'(v)$，即 $f'$ 单调不减。

### 证明 $3\Longleftrightarrow4$

记
$$
s_{12}=\frac{f(x_2)-f(x_1)}{x_2-x_1},\qquad
s_{23}=\frac{f(x_3)-f(x_2)}{x_3-x_2}.
$$
由恒等变形，
$$
\frac{f(x_3)-f(x_1)}{x_3-x_1}
=\frac{x_2-x_1}{x_3-x_1}s_{12}
+\frac{x_3-x_2}{x_3-x_1}s_{23}.
$$
条件 3 说明总差商是两个相邻差商的加权平均，因此
$$
s_{12}\le\frac{f(x_3)-f(x_1)}{x_3-x_1}\le s_{23}.
$$
将左侧不等式移项并乘以正数 $(x_2-x_1)(x_3-x_1)$，恰好得到
$$
f(x_2)\le
\frac{x_3-x_2}{x_3-x_1}f(x_1)
+\frac{x_2-x_1}{x_3-x_1}f(x_3).
$$
反过来，将条件 4 两边乘以 $x_3-x_1$ 并整理，得到
$$
(x_2-x_1)\bigl[f(x_3)-f(x_2)\bigr]
\ge
(x_3-x_2)\bigl[f(x_2)-f(x_1)\bigr],
$$
除以正数 $(x_2-x_1)(x_3-x_2)$ 即得 $s_{12}\le s_{23}$，所以条件 3 成立。

若 $f''>0$，则 $f'$ 严格递增，以上关键不等式均严格；若仅有 $f''\ge0$，可能存在一段直线，只能使用非严格不等式。

## 2. 弦线、切线与割线斜率

### 2.1 切线位于图像下方

若 $f'$ 单调不减，取 $x_0,x\in I$，由拉格朗日中值定理或积分表示可得
$$
\boxed{
f(x)\ge f(x_0)+f'(x_0)(x-x_0).
}
$$
这说明下凸函数的图像在任意切线的上方；切线是全局支撑线。

**证明：** 分 $x>x_0$ 和 $x<x_0$ 两种情况。若 $x>x_0$，拉格朗日定理给出
$$
\frac{f(x)-f(x_0)}{x-x_0}=f'(\xi),\qquad x_0<\xi<x,
$$
而 $f'(\xi)\ge f'(x_0)$，乘以正数 $x-x_0$ 即得结论。若 $x<x_0$，存在 $\xi\in(x,x_0)$ 使
$$
\frac{f(x_0)-f(x)}{x_0-x}=f'(\xi)\le f'(x_0).
$$
乘以正数 $x_0-x$ 并移项，同样得到
$$
f(x)\ge f(x_0)+f'(x_0)(x-x_0).
$$

### 2.2 弦线斜率夹在两端切线斜率之间

若 $x_1<x_2$，则
$$
\boxed{
f'(x_1)\le
\frac{f(x_2)-f(x_1)}{x_2-x_1}
\le f'(x_2).
}
$$

**证明：** 由拉格朗日定理，存在 $\xi\in(x_1,x_2)$，使差商等于 $f'(\xi)$。由于 $f'$ 单调不减，
$$
f'(x_1)\le f'(\xi)\le f'(x_2),
$$
即得结论。若 $f''>0$，则 $f'$ 严格递增，两个不等号严格。

### 2.3 三点差商的加权平均

令 $x_1<x_2<x_3$，记
$$
s_{12}=\frac{f(x_2)-f(x_1)}{x_2-x_1},\qquad
s_{23}=\frac{f(x_3)-f(x_2)}{x_3-x_2},
$$
则总差商满足
$$
\boxed{
\frac{f(x_3)-f(x_1)}{x_3-x_1}
=\frac{x_2-x_1}{x_3-x_1}s_{12}
+\frac{x_3-x_2}{x_3-x_1}s_{23}.
}
$$

它是两个相邻割线斜率的加权平均。因此当 $s_{12}\le s_{23}$ 时，
$$
s_{12}\le
\frac{f(x_3)-f(x_1)}{x_3-x_1}
\le s_{23}.
$$
这就是“弦公式夹在两段割线之间”的直接来源。

## 3. 凸组合定义：$tx_1+(1-t)x_2$

下凸函数的定义也可写成：任意 $x_1,x_2\in I$ 和 $t\in[0,1]$，
$$
\boxed{
f\bigl(tx_1+(1-t)x_2\bigr)
\le tf(x_1)+(1-t)f(x_2).
}
$$

其中 $tx_1+(1-t)x_2$ 是两点的加权平均，仍位于 $x_1,x_2$ 之间。几何意义是：曲线上加权点的函数值不高于两端点连成的弦线高度。

当 $f''\ge0$ 时可由割线斜率递增证明；反过来，若 $f$ 二阶可导且满足上式，也可推出 $f''\ge0$。若不等式严格，则得到严格凸性。

**证明 $f''\ge0\Longrightarrow$ 凸组合不等式：** 先设
$$
x=(1-t)x_1+tx_2,\qquad 0<t<1,
$$
且 $x_1<x_2$。由三点割线斜率递增，先在 $x_1<x<x_2$ 上写
$$
\frac{f(x)-f(x_1)}{x-x_1}
\le
\frac{f(x_2)-f(x)}{x_2-x}.
$$
代入 $x-x_1=t(x_2-x_1)$、$x_2-x=(1-t)(x_2-x_1)$，整理得到
$$
f(x)\le(1-t)f(x_1)+tf(x_2).
$$
将参数 $t$ 换成 $1-t$，就得到本文采用的
$$
f\bigl(tx_1+(1-t)x_2\bigr)
\le tf(x_1)+(1-t)f(x_2).
$$
当 $t=0$ 或 $t=1$ 时等号显然成立。

**证明凸组合不等式 $\Longrightarrow f''\ge0$：** 对任意 $x$ 和充分小的 $h$，取
$$
x_1=x-h,\qquad x_2=x+h,\qquad t=\frac12,
$$
得到
$$
f(x)\le\frac{f(x-h)+f(x+h)}2,
$$
即
$$
\frac{f(x+h)-2f(x)+f(x-h)}{h^2}\ge0.
$$
令 $h\to0$，由二阶导数定义
$$
f''(x)=\lim_{h\to0}
\frac{f(x+h)-2f(x)+f(x-h)}{h^2}\ge0.
$$

常用的等距点形式取 $t=\frac12$：
$$
f\left(\frac{x_1+x_2}{2}\right)
\le\frac{f(x_1)+f(x_2)}2.
$$

## 4. 阿达马不等式

若 $f$ 在区间 $I$ 上二阶连续可导，则“对 $I$ 内任意子区间 $[a,b]$ 都成立的阿达马不等式”与 $f''\ge0$ 是等价的。注意：只在某一个固定区间上验证一组不等式，不能反推出整个区间上的凸性。

对任意 $[a,b]\subset I$，下凸时有
$$
\boxed{
f\left(\frac{a+b}{2}\right)
\le \frac1{b-a}\int_a^b f(x)\,dx
\le \frac{f(a)+f(b)}2.
}
$$

等价地，
$$
(b-a)f\left(\frac{a+b}{2}\right)
\le\int_a^b f(x)\,dx
\le\frac{b-a}{2}\bigl[f(a)+f(b)\bigr].
$$

### 阿达马两侧的充要关系

#### 左侧不等式的充要证明

**充分性：** 若 $f''\ge0$，凸组合给出
$$
f\left(\frac{a+b}{2}\right)
\le\frac{f(x)+f(a+b-x)}2,
$$
在 $[a,b]$ 上积分并用换元 $u=a+b-x$，得到
$$
(b-a)f\left(\frac{a+b}{2}\right)\le\int_a^b f(x)\,dx.
$$

**必要性：** 反之，假设左侧不等式对 $I$ 内所有子区间成立。

任意足够小的 $h>0$ 都有
$$
f(x)\le\frac1{2h}\int_{x-h}^{x+h}f(t)\,dt.
$$
由于 $f\in C^2$，在 $x$ 处作对称展开：
$$
f(x+s)=f(x)+f'(x)s+\frac12f''(x)s^2+o(s^2).
$$
两边从 $s=-h$ 到 $s=h$ 积分，奇次项抵消，得到
$$
\frac1{2h}\int_{x-h}^{x+h}f(t)\,dt
=f(x)+\frac{h^2}{6}f''(x)+o(h^2).
$$
代回左侧不等式并除以 $h^2$，令 $h\to0$，得
$$
\frac16f''(x)\ge0,
$$
所以 $f''(x)\ge0$。这证明了左侧不等式族推出凸性。

#### 右侧不等式的充要证明

**充分性：** 若 $f''\ge0$，对每个 $x\in[a,b]$，
$$
x=\frac{b-x}{b-a}a+\frac{x-a}{b-a}b,
$$
凸组合定义给出
$$
f(x)\le\frac{b-x}{b-a}f(a)+\frac{x-a}{b-a}f(b).
$$
积分后利用两权重的积分均为 $(b-a)/2$，得到
$$
\int_a^b f(x)\,dx
\le\frac{b-a}{2}\bigl[f(a)+f(b)\bigr].
$$

**必要性：** 反之，假设右侧不等式对 $I$ 内所有子区间成立。

任意足够小的 $h>0$ 都有
$$
\frac1{2h}\int_{x-h}^{x+h}f(t)\,dt
\le\frac{f(x-h)+f(x+h)}2.
$$
仍用 $f\in C^2$ 的对称展开：
$$
\frac{f(x-h)+f(x+h)}2
=f(x)+\frac{h^2}{2}f''(x)+o(h^2),
$$
而积分平均为
$$
f(x)+\frac{h^2}{6}f''(x)+o(h^2).
$$
相减并除以 $h^2$，令 $h\to0$，得到
$$
\left(\frac12-\frac16\right)f''(x)=\frac13f''(x)\ge0.
$$
故 $f''(x)\ge0$。因此右侧不等式族单独也足以推出凸性。

综上，在 $C^2$ 条件下，阿达马两侧分别都是凸性的充分必要条件：
$$
\boxed{
f''\ge0
\Longleftrightarrow
f\left(\frac{a+b}{2}\right)
\le\frac1{b-a}\int_a^b f(x)\,dx
\quad\text{对所有 }[a,b]\subset I
}
$$
以及
$$
\boxed{
f''\ge0
\Longleftrightarrow
\frac1{b-a}\int_a^b f(x)\,dx
\le\frac{f(a)+f(b)}2
\quad\text{对所有 }[a,b]\subset I.
}
$$
若 $f''\le0$，上述两个充要关系中的不等式方向全部反向。若 $f''\ge0$ 但不是严格大于零，阿达马不等式可以取等；严格凸时，除端点退化等特殊情况外通常为严格不等式。

## 5. 由 $f''\ge0$ 引出的常见考点

### 5.1 单调性与最值

若 $f''\ge0$，则 $f'$ 单调不减：

- $f'(x)\ge0$，则 $f$ 单调不减；
- $f'(x)\le0$，则 $f$ 单调不增；
- $f'$ 从负变正时，驻点是极小点；
- 若 $f'$ 在某点左负右正，结合凸性可直接判断全局最小值。

**证明：** 若任意 $x_1<x_2$ 都有 $f'(x)\ge0$，由拉格朗日中值定理，存在 $\xi\in(x_1,x_2)$，使
$$
f(x_2)-f(x_1)=f'(\xi)(x_2-x_1)\ge0,
$$
故 $f$ 单调不减；$f'\le0$ 时同理。若存在 $x_0$ 使
$$
f'(x)<0\quad(x<x_0),\qquad f'(x)>0\quad(x>x_0),
$$
则 $f$ 在 $x_0$ 左侧递减、右侧递增，所以 $x_0$ 是全局极小点（在所讨论区间内）。

### 5.2 比值和切线截距

若 $f(0)=0$ 且 $f$ 下凸，则
$$
\frac{f(x)}x
$$
在 $x>0$ 上单调不减。证明时对 $0<x_1<x_2$ 使用弦线斜率递增：
$$
\frac{f(x_1)-f(0)}{x_1-0}
\le
\frac{f(x_2)-f(0)}{x_2-0}.
$$
这正是
$$
\frac{f(x_1)}{x_1}\le\frac{f(x_2)}{x_2},
$$
故结论成立。更一般地，若 $f$ 在 $a$ 点有定义且下凸，则对 $x>a$，
$$
\frac{f(x)-f(a)}{x-a}
$$
单调不减。**证明：** 对 $a<x_1<x_2$，三点割线斜率递增给出
$$
\frac{f(x_1)-f(a)}{x_1-a}
\le
\frac{f(x_2)-f(a)}{x_2-a},
$$
这正是所需结论。

### 5.3 积分平均值的比较

若 $f$ 下凸且区间关于 $x_0$ 对称，则
$$
f(x_0)\le \frac1{2h}\int_{x_0-h}^{x_0+h}f(x)\,dx
\le\frac{f(x_0-h)+f(x_0+h)}2.
$$
**证明：** 这是阿达马不等式在区间
$$
[a,b]=[x_0-h,x_0+h]
$$
上的直接代入，因为
$$
\frac{a+b}{2}=x_0,\qquad b-a=2h.
$$
因此不需要重新计算，只需先识别区间中心和半长。

---

# 四、2025 数学一三点差商题

## 题目结论

设 $f$ 在 $(a,b)$ 内可导。证明 $f'(x)$ 在 $(a,b)$ 内严格单调增加的充分必要条件是：对任意
$$
a<x_1<x_2<x_3<b,
$$
都有
$$
\boxed{
\frac{f(x_2)-f(x_1)}{x_2-x_1}
<
\frac{f(x_3)-f(x_1)}{x_3-x_1}
<
\frac{f(x_3)-f(x_2)}{x_3-x_2}.
}
$$

这道题的核心是：中间弦线斜率是两段相邻割线斜率的加权平均；导函数严格递增则割线斜率严格递增。

## 必要性：$f'$ 严格递增推出三点不等式

记
$$
s_{12}=\frac{f(x_2)-f(x_1)}{x_2-x_1},\qquad
s_{23}=\frac{f(x_3)-f(x_2)}{x_3-x_2}.
$$
分别在 $[x_1,x_2]$、$[x_2,x_3]$ 上使用拉格朗日中值定理，存在
$$
\xi_1\in(x_1,x_2),\qquad \xi_2\in(x_2,x_3),
$$
使
$$
s_{12}=f'(\xi_1),\qquad s_{23}=f'(\xi_2).
$$
因为 $\xi_1<\xi_2$ 且 $f'$ 严格递增，所以
$$
s_{12}<s_{23}.
$$
总差商是加权平均：
$$
\frac{f(x_3)-f(x_1)}{x_3-x_1}
=\frac{x_2-x_1}{x_3-x_1}s_{12}
+\frac{x_3-x_2}{x_3-x_1}s_{23},
$$
权重都为正且和为 $1$，故
$$
s_{12}
<
\frac{f(x_3)-f(x_1)}{x_3-x_1}
<
s_{23}.
$$

## 充分性：三点不等式推出 $f'$ 严格递增

已知三点不等式。先由其第一、第三项得到
$$
\frac{f(x_2)-f(x_1)}{x_2-x_1}
<
\frac{f(x_3)-f(x_2)}{x_3-x_2}
\qquad(x_1<x_2<x_3).
$$

固定 $u<v$，对任意 $x\in(u,v)$ 在三点不等式中取
$$
x_1=u,\qquad x_2=x,\qquad x_3=v.
$$
由第一项小于第二项，令 $x\to u^+$，得到
$$
f'(u)\le\frac{f(v)-f(u)}{v-u}.
$$
由第二项小于第三项，令 $x\to v^-$，得到
$$
\frac{f(v)-f(u)}{v-u}\le f'(v).
$$
故
$$
f'(u)\le f'(v),
$$
即 $f'$ 单调不减。
因此 $f'$ 单调不减。

还需排除“平台段”。若存在 $u<v$ 使 $f'(u)=f'(v)$，由于 $f'$ 单调不减，便有
$$
f'(x)=f'(u)\qquad(u\le x\le v).
$$
于是 $f$ 在 $[u,v]$ 上为一次函数，任取 $u<x_1<x_2<x_3<v$，三个差商都相等，与题设严格不等式矛盾。因此不存在这样的 $u<v$，只能有
$$
u<v\Longrightarrow f'(u)<f'(v),
$$
即 $f'$ 严格单调增加。

> **答题模板：** 必要性用“两次拉格朗日 + 加权平均”；充分性先由三点不等式取极限得到 $f'$ 单调不减，再假设两点导数相等，推出一段线性函数，与严格不等式矛盾。

---

# 五、积分证明题的常用套路

## 1. 对称配对

区间为 $[0,a]$ 时常令 $x\mapsto a-x$；区间为 $[-a,a]$ 时常令 $x\mapsto-x$。

若 $f(a-x)=f(x)$，则
$$
\int_0^a f(x)\,dx
=\frac12\int_0^a\bigl[f(x)+f(a-x)\bigr]\,dx.
$$
若 $f(a-x)=-f(x)$，则积分为 $0$。

## 2. 分部积分

$$
\int_a^b u\,dv
=\left.uv\right|_a^b-\int_a^b v\,du.
$$

证明不等式时，优先选择能让边界项消失的 $u,v$。若端点给出 $f(a)=f(b)=0$，常把 $f$ 写成积分，再配合柯西不等式。

## 3. 柯西—施瓦茨不等式

$$
\boxed{
\left(\int_a^b f(x)g(x)\,dx\right)^2
\le
\int_a^b f^2(x)\,dx
\int_a^b g^2(x)\,dx.
}
$$

常用特例：
$$
\left(\int_a^b f(x)\,dx\right)^2
\le(b-a)\int_a^b f^2(x)\,dx.
$$

## 4. 积分不等式的比较顺序

1. 先判断被积函数是否处处非负或可比较；
2. 若出现 $f''\ge0$，先尝试切线、弦线和阿达马不等式；
3. 若出现平方乘积，尝试柯西—施瓦茨；
4. 若端点条件很强，优先分部积分；
5. 最后才考虑逐项展开或复杂换元。

---

# 六、证明题总复盘

## 高频结论

- $f''\ge0\Longleftrightarrow f'$ 单调不减 $\Longrightarrow$ 割线斜率递增。
- 下凸函数图像在任意切线上方、在弦线下方。
- 三点总差商是两段相邻差商的加权平均。
- 阿达马不等式：中点函数值 $\le$ 积分平均 $\le$ 端点函数值平均。
- 费马引理只给极值点的必要条件。
- 罗尔定理必须先验证端点函数值相等。
- 柯西中值定理用于“两个函数的差商之比”。
- 导数不一定连续，但满足达布介值性。

## 最容易丢分的地方

- 把 $f''\ge0$ 和 $f''\le0$ 的弦线方向写反；
- 把普通“单调”误写成“严格单调”，忽略可能存在的线性区间；
- 积分中值定理中忘记检查权函数 $g$ 是否不变号；
- 只写“由中值定理存在 $\xi$”，却没有说明使用的区间和条件；
- 充分必要条件证明只做了一个方向；
- 看到三点差商就直接套拉格朗日，没有先识别加权平均结构。
