# 考研数学一 · 微分方程专题整理

---

# 一、基本概念

- **微分方程**：含有未知函数及其导数的方程
- **阶数**：方程中最高阶导数的阶数
- **通解**：含有与阶数相同个数的独立任意常数的解
- **特解**：由初始条件确定出常数后的解
- **初值问题**：微分方程 + 初始条件

---

# 二、一阶微分方程

## 1. 可分离变量型

$$
\frac{dy}{dx} = f(x)g(y) \quad\Longrightarrow\quad \frac{dy}{g(y)} = f(x)dx \quad\Longrightarrow\quad \int\frac{dy}{g(y)} = \int f(x)dx + C
$$

> ⚠️ 检查 $g(y)=0$ 是否也是解（常数解容易丢失）

## 2. 齐次型（一阶齐次）

$$
\frac{dy}{dx} = \varphi\left(\frac{y}{x}\right)
$$

**解法：** 令 $u = \dfrac{y}{x}$，则 $y = ux$，$y' = u + xu'$，代入得：
$$
u + x\frac{du}{dx} = \varphi(u) \quad\Longrightarrow\quad \frac{du}{\varphi(u)-u} = \frac{dx}{x}
$$
化为可分离变量求解。

**推广形式：** $\displaystyle \frac{dy}{dx} = f\left(\frac{ax+by+c}{a'x+b'y+c'}\right)$
- 若 $\begin{vmatrix}a&b\\a'&b'\end{vmatrix} \neq 0$：令 $x = X+h,\; y = Y+k$ 消去常数项
- 若 $\begin{vmatrix}a&b\\a'&b'\end{vmatrix} = 0$：令 $u = ax+by$

## 3. 一阶线性微分方程

$$
y' + P(x)y = Q(x)
$$

**齐次通解：** $y_h = Ce^{-\int P(x)dx}$

**非齐次通解公式（常数变易法）：**
$$
\boxed{y = e^{-\int P(x)dx}\left(\int Q(x)e^{\int P(x)dx}dx + C\right)}
$$

> ⚠️ $\int P(x)dx$ 只取一个原函数，不加常数

## 4. 伯努利方程

$$
y' + P(x)y = Q(x)y^n \quad (n \neq 0, 1)
$$

**解法：**
1. 两边同除以 $y^n$：$y^{-n}y' + P(x)y^{1-n} = Q(x)$
2. 令 $z = y^{1-n}$，则 $z' = (1-n)y^{-n}y'$，代入得：
   $$
   z' + (1-n)P(x)z = (1-n)Q(x)
   $$
   化为**一阶线性方程**
3. 求解 $z$，再代回 $y = z^{\frac{1}{1-n}}$

> ⚠️ $n>0$ 时，$y=0$ 也是解，别漏掉

## 5. 全微分方程

$$
M(x,y)dx + N(x,y)dy = 0, \quad \frac{\partial M}{\partial y} = \frac{\partial N}{\partial x}
$$

**解法：** 求 $u(x,y)$ 使 $du = Mdx + Ndy$，通解 $u(x,y) = C$

**积分因子（若不满足恰当条件）：**
- 若 $\displaystyle \frac{1}{N}\left(\frac{\partial M}{\partial y} - \frac{\partial N}{\partial x}\right)$ 只与 $x$ 有关 $\Rightarrow$ $\mu(x) = e^{\int \varphi(x)dx}$
- 若 $\displaystyle \frac{1}{M}\left(\frac{\partial N}{\partial x} - \frac{\partial M}{\partial y}\right)$ 只与 $y$ 有关 $\Rightarrow$ $\mu(y) = e^{\int \psi(y)dy}$

---

# 三、可降阶的高阶方程（缺 y 型 / 缺 x 型）

## 1. 缺 $y$ 型：$y'' = f(x, y')$

令 $p = y'$，则 $y'' = p'$，降为一阶：$p' = f(x, p)$

解出 $p(x)$ 后，$y = \int p(x)dx + C$

## 2. 缺 $x$ 型：$y'' = f(y, y')$

令 $p = y'$，则 $y'' = \dfrac{dp}{dx} = \dfrac{dp}{dy}\cdot\dfrac{dy}{dx} = p\dfrac{dp}{dy}$，降为一阶：
$$
p\frac{dp}{dy} = f(y, p)
$$

解出 $p(y)$ 后，由 $\dfrac{dy}{dx} = p(y)$ 得 $\displaystyle \int\frac{dy}{p(y)} = x + C$

## 3. 对照表

| 类型 | 特点 | 换元 | 结果 |
|:---|:---|:---|:---|
| $y''=f(x)$ | 缺 $y'$ 和 $y$ | 直接积分 | 积分两次 |
| $y''=f(x,y')$ | **缺 $y$** | $p=y'$，$p'=y''$ | $p'=f(x,p)$ |
| $y''=f(y,y')$ | **缺 $x$** | $p=y'$，$y''=p\frac{dp}{dy}$ | $p\frac{dp}{dy}=f(y,p)$ |

> ⚠️ **缺 x 型**的 $y''$ 是 $p\frac{dp}{dy}$，**缺 y 型**的 $y''$ 是 $p'$，别搞混！

---

# 四、高阶常系数线性微分方程

## 1. 二阶常系数齐次线性

$$
y'' + py' + qy = 0
$$

**特征方程：** $r^2 + pr + q = 0$

| 特征根 | 通解 |
|:---|:---|
| $r_1 \neq r_2$（不等实根） | $y = C_1e^{r_1x} + C_2e^{r_2x}$ |
| $r_1 = r_2 = r$（重根） | $y = (C_1 + C_2x)e^{rx}$ |
| $r_{1,2} = \alpha \pm i\beta$（复根） | $y = e^{\alpha x}(C_1\cos\beta x + C_2\sin\beta x)$ |

## 2. $n$ 阶常系数齐次线性

特征方程：$r^n + a_1r^{n-1} + \cdots + a_{n-1}r + a_n = 0$

| 特征根 | 通解部分 |
|:---|:---|
| 单实根 $r$ | $Ce^{rx}$ |
| $k$ 重实根 $r$ | $(C_1 + C_2x + \cdots + C_kx^{k-1})e^{rx}$ |
| 单重复根 $\alpha\pm i\beta$ | $e^{\alpha x}(C_1\cos\beta x + C_2\sin\beta x)$ |
| $k$ 重复根 $\alpha\pm i\beta$ | $e^{\alpha x}[(A_1+\cdots+A_kx^{k-1})\cos\beta x + (B_1+\cdots+B_kx^{k-1})\sin\beta x]$ |

## 3. 二阶常系数非齐次线性

$$
y'' + py' + qy = f(x)
$$

**解法：** 齐次通解 $y_h$ + 非齐次特解 $y^*$

### 类型 I：$f(x) = P_m(x)e^{\lambda x}$

$$
y^* = x^k Q_m(x) e^{\lambda x}
$$

$k$ 由 $\lambda$ 与特征根的关系决定：
- $\lambda$ 不是特征根 $\Rightarrow k=0$
- $\lambda$ 是单特征根 $\Rightarrow k=1$
- $\lambda$ 是重特征根 $\Rightarrow k=2$

### 类型 II：$f(x) = e^{\alpha x}[P_m(x)\cos\beta x + Q_n(x)\sin\beta x]$

$$
y^* = x^k e^{\alpha x}[R_l(x)\cos\beta x + S_l(x)\sin\beta x], \quad l = \max\{m,n\}
$$

$k$ 由 $\alpha\pm i\beta$ 与特征根的关系决定：
- 不是特征根 $\Rightarrow k=0$
- 是特征根 $\Rightarrow k=1$

### 类型 III：叠加原理

若 $f(x) = f_1(x) + f_2(x)$，则 $y^* = y_1^* + y_2^*$

### 速查表

| $f(x)$ | 条件 | $y^*$ |
|:---|:---|:---|
| $P_m(x)$ | $0$ 不是特征根 | $Q_m(x)$ |
| $P_m(x)$ | $0$ 是单特征根 | $xQ_m(x)$ |
| $P_m(x)$ | $0$ 是重特征根 | $x^2Q_m(x)$ |
| $P_m(x)e^{\lambda x}$ | $\lambda$ 不是特征根 | $Q_m(x)e^{\lambda x}$ |
| $P_m(x)e^{\lambda x}$ | $\lambda$ 是单特征根 | $xQ_m(x)e^{\lambda x}$ |
| $P_m(x)e^{\lambda x}$ | $\lambda$ 是重特征根 | $x^2Q_m(x)e^{\lambda x}$ |
| $e^{\alpha x}(A\cos\beta x + B\sin\beta x)$ | $\alpha\pm i\beta$ 不是特征根 | $e^{\alpha x}(M\cos\beta x + N\sin\beta x)$ |
| $e^{\alpha x}(A\cos\beta x + B\sin\beta x)$ | $\alpha\pm i\beta$ 是特征根 | $xe^{\alpha x}(M\cos\beta x + N\sin\beta x)$ |

---

# 五、欧拉方程

## 标准形式（二阶最常见）

$$
\boxed{x^2 y'' + pxy' + qy = f(x)}
$$

## 解法一：变量代换法

令 $x = e^t$（$t = \ln x$），则：
$$
xy' = \frac{dy}{dt}, \quad x^2 y'' = \frac{d^2y}{dt^2} - \frac{dy}{dt}
$$

二阶欧拉方程化为：
$$
\frac{d^2y}{dt^2} + (p-1)\frac{dy}{dt} + qy = f(e^t)
$$

转化为常系数方程求解。

## 解法二：直接设解（齐次）

设 $y = x^r$，代入齐次欧拉方程得：
$$
r^2 + (p-1)r + q = 0
$$

| 特征根 | 通解 |
|:---|:---|
| $r_1 \neq r_2$ | $y = C_1\|x\|^{r_1} + C_2\|x\|^{r_2}$ |
| $r_1 = r_2 = r$ | $y = (C_1 + C_2\ln\|x\|)\|x\|^{r}$ |
| $r_{1,2} = \alpha \pm i\beta$ | $y = \|x\|^{\alpha}[C_1\cos(\beta\ln\|x\|) + C_2\sin(\beta\ln\|x\|)]$ |

> ⚠️ 注意 $x<0$ 时用绝对值 $|x|$

---

# 六、快速识别流程图

```
一阶方程？
├── 可分离变量：y' = f(x)g(y)          → 直接分离积分
├── 齐次型：y' = φ(y/x)                → 令 u = y/x
├── 一阶线性：y' + P(x)y = Q(x)        → 公式法
├── 伯努利：y' + P(x)y = Q(x)y^n       → 令 z = y^{1-n}
├── 全微分：Mdx+Ndy=0 (M_y=N_x)        → 求势函数
└── 其他                               → 积分因子或变量代换

二阶及以上？
├── 可降阶
│   ├── 缺 y：y'' = f(x, y')           → 令 p = y'
│   ├── 缺 x：y'' = f(y, y')           → 令 p = y', y''=p dp/dy
│   └── 缺 y'：y'' = f(x)              → 直接积分
├── 常系数齐次                         → 特征方程法
├── 常系数非齐次                       → 待定系数法
└── 欧拉方程                           → x=e^t 或 y=x^r
```

---

# 七、解的结构定理

## 1. $n$ 阶线性微分方程的一般形式

$$
y^{(n)} + a_1(x)y^{(n-1)} + \cdots + a_{n-1}(x)y' + a_n(x)y = f(x)
$$

## 2. 齐次线性方程解的结构

- **叠加原理：** 若 $y_1, y_2, \cdots, y_k$ 是齐次方程的解，则 $C_1y_1 + C_2y_2 + \cdots + C_ky_k$ 也是齐次方程的解
- **通解结构：** 齐次方程的 $n$ 个线性无关的特解 $y_1, y_2, \cdots, y_n$ 构成通解 $y_h = C_1y_1 + C_2y_2 + \cdots + C_ny_n$
- **Wronski 行列式（判断线性无关性）：**
  $$
  W(y_1, y_2, \cdots, y_n) = \begin{vmatrix}
  y_1 & y_2 & \cdots & y_n \\
  y_1' & y_2' & \cdots & y_n' \\
  \vdots & \vdots & \ddots & \vdots \\
  y_1^{(n-1)} & y_2^{(n-1)} & \cdots & y_n^{(n-1)}
  \end{vmatrix}
  $$
  $y_1, y_2, \cdots, y_n$ 线性无关 $\Leftrightarrow W \neq 0$

## 3. 非齐次线性方程解的结构

- 若 $y_h$ 是齐次通解，$y_p$ 是非齐次特解，则非齐次通解为 $y = y_h + y_p$
- **叠加原理：** 若 $y_{p1}$ 是 $y''+py'+qy=f_1(x)$ 的特解，$y_{p2}$ 是 $y''+py'+qy=f_2(x)$ 的特解，则 $y_{p1}+y_{p2}$ 是 $y''+py'+qy=f_1(x)+f_2(x)$ 的特解

---

# 八、微分算子法（求非齐次特解的快速方法）

## 1. 微分算子定义

记 $D = \dfrac{d}{dx}$，则 $Dy = y'$，$D^2y = y''$，$F(D) = D^2 + pD + q$

方程 $y'' + py' + qy = f(x)$ 可写为 $F(D)y = f(x)$

特解：$y^* = \dfrac{1}{F(D)}f(x)$

## 2. 常用公式

### (1) 指数函数：$f(x) = e^{\lambda x}$

$$
\frac{1}{F(D)}e^{\lambda x} = \frac{1}{F(\lambda)}e^{\lambda x} \quad (F(\lambda) \neq 0)
$$

若 $F(\lambda) = 0$ 且 $\lambda$ 是 $k$ 重根，则：
$$
\frac{1}{F(D)}e^{\lambda x} = \frac{x^k}{F^{(k)}(\lambda)}e^{\lambda x}
$$

### (2) 多项式：$f(x) = P_m(x)$

$$
\frac{1}{F(D)}P_m(x) = \text{将 } \frac{1}{F(D)} \text{ 按 } D \text{ 展开成幂级数后作用于 } P_m(x)
$$

例如 $F(D) = D^2 + aD + b$：
$$
\frac{1}{F(D)} = \frac{1}{b}\left(1 - \frac{aD + D^2}{b} + \left(\frac{aD + D^2}{b}\right)^2 - \cdots\right)
$$
只取到 $D^m$ 项即可（因为 $P_m(x)$ 的 $m+1$ 阶导数为 0）。

### (3) 三角函数：$f(x) = \sin\beta x$ 或 $\cos\beta x$

$$
\frac{1}{F(D^2)}\sin\beta x = \frac{1}{F(-\beta^2)}\sin\beta x \quad (F(-\beta^2) \neq 0)
$$

### (4) 指数乘函数：$f(x) = e^{\lambda x}g(x)$

$$
\frac{1}{F(D)}e^{\lambda x}g(x) = e^{\lambda x}\frac{1}{F(D+\lambda)}g(x)
$$

---

# 九、已知齐次一解求另一解（刘维尔公式）

对于二阶变系数齐次方程 $y'' + p(x)y' + q(x)y = 0$，若已知一个非零解 $y_1$，则另一线性无关解为：

$$
y_2 = y_1 \int \frac{e^{-\int p(x)dx}}{y_1^2}dx
$$

**验证：** $y_1$ 与 $y_2$ 线性无关（因为 $\dfrac{y_2}{y_1} \neq \text{常数}$）

**应用场景：** 欧拉方程或可化为常系数的变系数方程中，若已知一个特解，可用此法求通解。

---

# 十、克莱罗方程

**标准形式：**
$$
y = xy' + \varphi(y')
$$

**解法：** 令 $p = y'$，则 $y = xp + \varphi(p)$，两边对 $x$ 求导：
$$
p = p + xp' + \varphi'(p)p' \quad\Longrightarrow\quad p'(x + \varphi'(p)) = 0
$$

- **通解（直线族）：** $p' = 0 \Rightarrow p = C$，代入得 $y = Cx + \varphi(C)$
- **奇解（包络线）：** $\begin{cases}x = -\varphi'(p) \\ y = xp + \varphi(p)\end{cases}$，消去参数 $p$ 得奇解

> ⚠️ 克莱罗方程的通解是一族直线，奇解是这些直线的包络线，数一中低频考点。

---

# 十一、微分方程的应用（数一大题高频考点）

## 1. 几何应用

### (1) 切线性质
曲线 $y = y(x)$ 在点 $(x,y)$ 处：
- 切线斜率：$y'$
- 切线方程：$Y - y = y'(X - x)$
- 切线在 $x$ 轴截距：$x - \dfrac{y}{y'}$
- 切线在 $y$ 轴截距：$y - xy'$
- 法线斜率：$-\dfrac{1}{y'}$（$y' \neq 0$）
- 法线在 $x$ 轴截距：$x + yy'$

### (2) 曲率与曲率半径
- 曲率：$\displaystyle K = \frac{|y''|}{(1 + y'^2)^{3/2}}$
- 曲率半径：$\displaystyle R = \frac{1}{K} = \frac{(1 + y'^2)^{3/2}}{|y''|}$

### (3) 常见几何条件列方程
- 切线长 = 常数（或与某线段成比例）
- 曲率 = 常数
- 切线与坐标轴围成的三角形面积 = 常数
- 法线被坐标轴截得的线段长度 = 常数

## 2. 物理应用

### (1) 牛顿冷却定律
物体冷却速率与温差成正比：
$$
\frac{dT}{dt} = -k(T - T_0)
$$
其中 $T$ 为物体温度，$T_0$ 为环境温度，$k > 0$ 为常数。

### (2) 放射性衰变 / 增长模型
$$
\frac{dN}{dt} = kN \quad\Longrightarrow\quad N = N_0e^{kt}
$$
$k > 0$ 为增长模型，$k < 0$ 为衰减模型。

### (3) 弹簧振动（简谐振动 + 阻尼振动）
- 无阻尼自由振动：$y'' + \omega^2 y = 0$，解为 $y = A\cos(\omega t + \varphi)$
- 有阻尼振动：$y'' + 2\delta y' + \omega^2 y = 0$
  - $\delta < \omega$：欠阻尼（振荡衰减）
  - $\delta = \omega$：临界阻尼
  - $\delta > \omega$：过阻尼（无振荡）

### (4) RL / RC 电路
- RL 电路：$L\dfrac{di}{dt} + Ri = E(t)$（一阶线性）
- RC 电路：$R\dfrac{dq}{dt} + \dfrac{q}{C} = E(t)$（一阶线性）

### (5) 液体流出（托里拆利定律）
$$
\frac{dV}{dt} = -k\sqrt{h}
$$
$V$ 为容器内液体体积，$h$ 为液面高度。

## 3. 应用题解题步骤

```
Step 1: 确定自变量和因变量（通常是 t 或 x）
Step 2: 根据几何/物理条件列出微分方程
Step 3: 识别方程类型
Step 4: 求解方程
Step 5: 代入初始条件确定常数
Step 6: 写出最终表达式，作答
```

---

# 十二、易错点总结

1. **可分离变量**：别忘了 $g(y)=0$ 的常数解
2. **伯努利方程**：$n>0$ 时 $y=0$ 是解
3. **缺 $x$ 型**：$y'' = p\dfrac{dp}{dy}$，不是 $p'$
4. **缺 $y$ 型**：$y'' = p'$，不是 $p\dfrac{dp}{dy}$
5. **欧拉方程**：$x<0$ 时记得加绝对值 $|x|$
6. **待定系数法**：$f(x)$ 与齐次解重合时，必须乘 $x^k$
7. **二阶方程通解**：必须有 2 个独立常数
8. **特征方程**：$r^2 + pr + q = 0$，判别式是 $p^2 - 4q$
9. **微分算子法**：分母 $F(\lambda)=0$ 时不能直接代公式，需用求导或移位公式
10. **物理应用**：注意 $t=0$ 时的初始条件，不要混淆 $t$ 和 $x$ 作为自变量
11. **几何应用**：切线截距公式容易记错，建议推导而非死记

---

# 十三、常用记忆口诀

> **一阶分离和线性，齐次伯努利要分清**
> **缺 y 设 p 缺 x 换，欧拉代换 x=e^t**
> **常系特征求齐次，非齐再看 f(x)**
> **指数多项式乘三角，待定系数解出来**
> **微分算子 D 代替，特解求解快又准**
> **已知一解求另一，刘维尔公式记心里**
> **应用问题不要怕，几何物理列方程**
> **初始条件最后带，常数确定就做完**