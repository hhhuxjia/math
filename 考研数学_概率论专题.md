# 考研数学一 · 概率论与数理统计专题整理

---

# 一、随机事件与概率

## 1. 基本概念

### 事件的关系与运算

| 关系/运算 | 符号 | 含义 |
|:---|:---|:---|
| 包含 | $A \subset B$ | $A$ 发生则 $B$ 必然发生 |
| 相等 | $A = B$ | $A \subset B$ 且 $B \subset A$ |
| 并 | $A \cup B$ | $A$ 与 $B$ 至少一个发生 |
| 交 | $A \cap B$（或 $AB$） | $A$ 与 $B$ 同时发生 |
| 差 | $A - B$（或 $A\overline{B}$） | $A$ 发生而 $B$ 不发生 |
| 互斥（互不相容） | $AB = \varnothing$ | $A$ 与 $B$ 不能同时发生 |
| 对立 | $\overline{A}$ | $A \cup \overline{A} = \Omega$，$A\overline{A} = \varnothing$ |

### 运算律
- **交换律：** $A \cup B = B \cup A$，$AB = BA$
- **结合律：** $A \cup (B \cup C) = (A \cup B) \cup C$，$A(BC) = (AB)C$
- **分配律：** $A(B \cup C) = AB \cup AC$，$A \cup (BC) = (A \cup B)(A \cup C)$
- **对偶律（德摩根定律）：**
$$
\overline{A \cup B}=\overline{A}\cap\overline{B},\qquad
\overline{A\cap B}=\overline{A}\cup\overline{B}.
$$

> **注意**：补集会把“并”与“交”互换，不能写成 $\overline{A\cup B}=\overline A\cup\overline B$。

## 2. 概率的定义与性质

### 公理化定义
概率 $P(A)$ 满足：
1. **非负性：** $P(A) \geq 0$
2. **规范性：** $P(\Omega) = 1$
3. **可列可加性：** 若 $A_1, A_2, \cdots$ 两两互斥，则 $P\left(\bigcup_{i=1}^{\infty} A_i\right) = \sum\limits_{i=1}^{\infty} P(A_i)$

### 重要性质
- $P(\varnothing) = 0$
- $P(\overline{A}) = 1 - P(A)$
- **加法公式：**
  $$
  P(A \cup B) = P(A) + P(B) - P(AB)
  $$
  $$
  P(A \cup B \cup C) = P(A) + P(B) + P(C) - P(AB) - P(AC) - P(BC) + P(ABC)
  $$
- 若 $A \subset B$，则 $P(B - A) = P(B) - P(A)$
- $P(A - B) = P(A) - P(AB)$

## 3. 古典概型与几何概型

### 古典概型

$$
P(A) = \frac{A \text{ 包含的基本事件数}}{\text{基本事件总数}} = \frac{|A|}{|\Omega|}
$$
**常用计数方法：**
| 情况 | 公式 | 说明 |
|:---|:---|:---|
| 排列 | $P_n^m = \dfrac{n!}{(n-m)!}$ | 从 $n$ 个中选 $m$ 个排列 |
| 组合 | $C_n^m = \dfrac{n!}{m!(n-m)!}$ | 从 $n$ 个中选 $m$ 个组合 |
| 全排列 | $P_n = n!$ | $n$ 个元素全排列 |
| 重复排列 | $n^m$ | $m$ 次选取，每次 $n$ 种可能 |
| 重复组合 | $C_{n+m-1}^m$ | $m$ 个相同球放入 $n$ 个盒子 |

### 几何概型

$$
P(A) = \frac{A \text{ 的几何度量（长度/面积/体积）}}{\Omega \text{ 的几何度量（长度/面积/体积）}}
$$

## 4. 条件概率与乘法公式

### 条件概率

$$
P(B \mid A) = \frac{P(AB)}{P(A)}, \quad P(A) > 0
$$
### 乘法公式
- $P(AB) = P(A)P(B \mid A) = P(B)P(A \mid B)$
- $P(A_1A_2\cdots A_n) = P(A_1)P(A_2 \mid A_1)P(A_3 \mid A_1A_2)\cdots P(A_n \mid A_1\cdots A_{n-1})$

## 5. 全概率公式与贝叶斯公式

### 完备事件组

若 $B_1, B_2, \cdots, B_n$ 满足：
1. $B_iB_j = \varnothing\;(i \neq j)$
2. $\bigcup\limits_{i=1}^n B_i = \Omega$
则称 $\{B_i\}$ 为 $\Omega$ 的一个**划分**（完备事件组）。

### 全概率公式

$$
P(A) = \sum_{i=1}^n P(B_i)P(A \mid B_i)
$$
### 贝叶斯公式

$$
P(B_i \mid A) = \frac{P(B_i)P(A \mid B_i)}{\displaystyle\sum_{j=1}^n P(B_j)P(A \mid B_j)}
$$
> **记忆口诀：** 全概率是**由因导果**，贝叶斯是**执果寻因**

## 6. 事件的独立性

### 两个事件独立

$$
P(AB) = P(A)P(B) \;\Longleftrightarrow\; A \text{ 与 } B \text{ 相互独立}
$$

### 等价条件
- $P(B \mid A) = P(B)$（$P(A) > 0$）
- $P(A \mid B) = P(A)$（$P(B) > 0$）
- $\overline{A}$ 与 $B$，$A$ 与 $\overline{B}$，$\overline{A}$ 与 $\overline{B}$ 也相互独立

### 多个事件相互独立
$A_1, A_2, \cdots, A_n$ 相互独立 $\Longleftrightarrow$ 对任意 $k$ 个事件有：

$$
P(A_{i_1}A_{i_2}\cdots A_{i_k}) = P(A_{i_1})P(A_{i_2})\cdots P(A_{i_k})
$$
> ⚠️ **两两独立 ≠ 相互独立！** 相互独立要求所有组合都满足乘积公式

---

# 二、一维随机变量及其分布

## 1. 分布函数

### 定义

$$
F(x) = P\{X \leq x\}, \quad x \in \mathbb{R}
$$
### 性质
1. **单调不减：** $x_1 < x_2 \Rightarrow F(x_1) \leq F(x_2)$
2. **有界性：** $F(-\infty) = 0$，$F(+\infty) = 1$
3. **右连续：** $F(x+0) = F(x)$

### 用分布函数表示概率
- $P\{X \leq a\} = F(a)$
- $P\{X > a\} = 1 - F(a)$
- $P\{a < X \leq b\} = F(b) - F(a)$
- $P\{X = a\} = F(a) - F(a - 0)$

其中 $F(a-0)=\lim_{x\to a^-}F(x)$。分布函数的跳跃大小就是该点的概率：

$$
P\{X=a\}=F(a)-F(a-0).
$$

对任意 $a<b$，还可直接使用
$$
P\{a<X<b\}=F(b-0)-F(a),\qquad
P\{a\le X\le b\}=F(b)-F(a-0).
$$

## 2. 离散型随机变量

离散型随机变量的取值至多可列，概率集中在一个个点上；分布律给出每个取值的概率，分布函数则是分布律的累加：

### 分布律

$$
P\{X = x_k\} = p_k,\quad k = 1, 2, \cdots
$$
满足：$p_k \geq 0$，$\sum\limits_k p_k = 1$

其分布函数为
$$
F(x)=P\{X\le x\}=\sum_{x_k\le x}p_k.
$$

### 常见离散型分布

#### (1) 0-1 分布（伯努利分布）$X \sim B(1, p)$

| $X$ | 0 | 1 |
|:---:|:---:|:---:|
| $P$ | $1-p$ | $p$ |
$$
P\{X = k\} = p^k(1-p)^{1-k},\quad k=0,1
$$
#### (2) 二项分布 $X \sim B(n, p)$
$$
P\{X = k\} = C_n^k p^k (1-p)^{n-k},\quad k = 0, 1, \cdots, n
$$
#### (3) 泊松分布 $X \sim P(\lambda)$
$$
P\{X = k\} = \frac{\lambda^k}{k!} e^{-\lambda},\quad k = 0, 1, 2, \cdots, \;\lambda > 0
$$
**泊松定理（二项分布的近似）：** 当 $n$ 很大，$p$ 很小，$np = \lambda$ 时：

$$
C_n^k p^k (1-p)^{n-k} \approx \frac{\lambda^k}{k!} e^{-\lambda}
$$
#### (4) 几何分布 $X \sim G(p)$
$$
P\{X = k\} = (1-p)^{k-1}p,\quad k = 1, 2, \cdots
$$
**无记忆性：** $P\{X > m+n \mid X > m\} = P\{X > n\}$

#### (5) 超几何分布
$$
P\{X = k\} = \frac{C_M^k C_{N-M}^{n-k}}{C_N^n},\quad k = 0, 1, \cdots, \min\{n, M\}
$$

### 常见离散分布的分布函数

若 $X\sim B(1,p)$，则
$$
F(x)=
\begin{cases}
0,&x<0,\\
1-p,&0\le x<1,\\
1,&x\ge1.
\end{cases}
$$

若 $X\sim B(n,p)$ 或 $X\sim P(\lambda)$，分布函数通常写成概率和：
$$
F(x)=\sum_{k=0}^{\lfloor x\rfloor}P\{X=k\}.
$$

若几何分布表示“第 $k$ 次试验首次成功”，$X\sim G(p)$，则
$$
F(x)=
\begin{cases}
0,&x<1,\\
1-(1-p)^{\lfloor x\rfloor},&x\ge1.
\end{cases}
$$
## 3. 连续型随机变量

### 概率密度函数
若存在非负可积函数 $f(x)$，满足

$$
F(x) = \int_{-\infty}^x f(t)\,dt
$$

则称 $X$ 为连续型随机变量，$f(x)$ 为概率密度函数。

**性质：**
- $f(x) \geq 0$
- $\displaystyle\int_{-\infty}^{+\infty} f(x)\,dx = 1$
- $P\{a < X \leq b\} = \displaystyle\int_a^b f(x)\,dx$
- $F'(x) = f(x)$（在 $f$ 连续点处）
- $P\{X=a\}=0$，所以区间端点取开、闭不影响连续型概率

连续型随机变量的分布函数由密度函数累积得到；反过来，若 $F$ 绝对连续，则 $f=F'$ 几乎处处成立。离散型没有通常意义下的密度函数，连续型也不能用“单点概率”描述整体分布。

### 常见连续型分布

#### (1) 均匀分布 $X \sim U(a, b)$
$$
f(x) = \begin{cases}
\dfrac{1}{b-a}, & a \leq x \leq b \\[8pt]
0, & \text{其他}
\end{cases}
$$

$$
F(x) = \begin{cases}
0, & x < a \\[6pt]
\dfrac{x-a}{b-a}, & a \leq x < b \\[6pt]
1, & x \geq b
\end{cases}
$$
#### (2) 指数分布 $X \sim \text{Exp}(\lambda)$
$$
f(x) = \begin{cases}
\lambda e^{-\lambda x}, & x \geq 0 \\[4pt]
0, & x < 0
\end{cases},\quad \lambda > 0
$$

$$
F(x) = \begin{cases}
1 - e^{-\lambda x}, & x \geq 0 \\[4pt]
0, & x < 0
\end{cases}
$$
**无记忆性：** $P\{X > s+t \mid X > s\} = P\{X > t\}$

#### (3) 正态分布 $X \sim N(\mu, \sigma^2)$
$$
f(x) = \frac{1}{\sqrt{2\pi}\,\sigma} e^{-\frac{(x-\mu)^2}{2\sigma^2}},\quad -\infty < x < +\infty
$$
**标准正态分布** $X \sim N(0, 1)$：

$$
\varphi(x) = \frac{1}{\sqrt{2\pi}} e^{-\frac{x^2}{2}},\quad \Phi(x) = \int_{-\infty}^x \varphi(t)\,dt
$$
**标准化：** 若 $X \sim N(\mu, \sigma^2)$，则 $Z = \dfrac{X - \mu}{\sigma} \sim N(0, 1)$

**重要分位点：**
- $\Phi(1.645) = 0.95$，$\Phi(1.96) = 0.975$，$\Phi(2.576) = 0.995$
- $P\{|X-\mu| < \sigma\} \approx 68.27\%$
- $P\{|X-\mu| < 2\sigma\} \approx 95.45\%$
- $P\{|X-\mu| < 3\sigma\} \approx 99.73\%$

#### (4) $\chi^2$ 分布 $X \sim \chi^2(n)$
$$
f(x) = \begin{cases}
\dfrac{1}{2^{n/2}\Gamma(n/2)} x^{n/2-1} e^{-x/2}, & x > 0 \\[8pt]
0, & x \leq 0
\end{cases}
$$
#### (5) $t$ 分布 $X \sim t(n)$
$$
f(x) = \frac{\Gamma\left(\dfrac{n+1}{2}\right)}{\sqrt{n\pi}\,\Gamma\left(\dfrac{n}{2}\right)} \left(1 + \frac{x^2}{n}\right)^{-\frac{n+1}{2}},\quad -\infty < x < +\infty
$$
**性质：** $t$ 分布的密度函数关于 $x=0$ 对称；$n \to \infty$ 时 $t(n) \to N(0,1)$

#### (6) $F$ 分布 $X \sim F(n_1, n_2)$

**性质：** 若 $X \sim F(n_1, n_2)$，则 $\dfrac{1}{X} \sim F(n_2, n_1)$

## 4. 随机变量函数的分布

### 离散型
若 $Y = g(X)$，则 $P\{Y = y\} = \sum\limits_{x: g(x) = y} P\{X = x\}$

### 连续型

**方法一（分布函数法——通用）：**

$$
F_Y(y) = P\{Y \leq y\} = P\{g(X) \leq y\} = \int_{\{x: g(x) \leq y\}} f_X(x)\,dx
$$

然后 $f_Y(y) = F_Y'(y)$

**方法二（公式法——$Y = g(X)$ 单调可导）：**

$$
f_Y(y) = f_X(h(y)) \cdot |h'(y)|,\quad y \in \text{值域}
$$

其中 $h(y)$ 是 $g(x)$ 的反函数。

若 $g$ 不是单调函数，方程 $g(x)=y$ 在值域内有多个可导反函数分支 $x_i=x_i(y)$，则要把所有原像相加：
$$
f_Y(y)=\sum_i f_X\bigl(x_i(y)\bigr)\left|x_i'(y)\right|.
$$

> **方法选择**：不等式区域复杂时用分布函数法；变换单调且反函数简单时用公式法；多分支时不能漏掉任何一支。

**常用变换：**
- $Y = aX + b$：$f_Y(y) = \dfrac{1}{|a|} f_X\left(\dfrac{y-b}{a}\right)$
- $Y = X^2$：$f_Y(y) = \begin{cases}\dfrac{1}{2\sqrt{y}}\left[f_X(\sqrt{y}) + f_X(-\sqrt{y})\right], & y > 0 \\ 0, & y \leq 0\end{cases}$

---

# 三、多维随机变量及其分布

## 1. 二维随机变量

### 1.1 联合分布函数：最一般的描述

$$
F(x,y)=P\{X\le x,\ Y\le y\}.
$$

它描述随机点 $(X,Y)$ 落在左下矩形 $(-\infty,x]\times(-\infty,y]$ 内的概率。任何二维随机变量都有联合分布函数，不要求它有密度。

**基本性质：**

1. 对 $x,y$ 分别单调不减，且关于每个变量右连续；
2. $F(-\infty,y)=F(x,-\infty)=0$，$F(+\infty,+\infty)=1$；
3. 对任意 $a<b,c<d$，矩形概率由四个角作差：
$$
P\{a<X\le b,\ c<Y\le d\}
=F(b,d)-F(a,d)-F(b,c)+F(a,c).
$$
边界取开、闭对连续型变量没有影响，但离散型必须按题目端点逐项处理。

### 1.2 联合分布律：离散型

$$
p_{ij}=P\{X=x_i,Y=y_j\},\qquad p_{ij}\ge0,\qquad \sum_i\sum_jp_{ij}=1.
$$

联合分布函数是联合分布律的累加：
$$
F(x,y)=\sum_{x_i\le x}\sum_{y_j\le y}p_{ij}.
$$
因此离散型中“求概率”通常是查表并求和，“求分布函数”是对左下区域的所有表格元素累加。

### 1.3 联合概率密度：连续型

若存在非负函数 $f(x,y)$ 使得任意平面区域 $D$ 满足
$$
P\{(X,Y)\in D\}=\iint_Df(x,y)\,dxdy,
$$
则称 $f$ 为 $(X,Y)$ 的联合概率密度。必须满足
$$
f(x,y)\ge0,\qquad \iint_{\mathbb R^2}f(x,y)\,dxdy=1.
$$

联合分布函数由密度累积：
$$
F(x,y)=\int_{-\infty}^{x}\int_{-\infty}^{y}f(u,v)\,dv\,du.
$$
若 $F$ 在相应处二阶偏导存在且连续，则
$$
\boxed{f(x,y)=\frac{\partial^2F(x,y)}{\partial x\,\partial y}}.
$$
这里是先对 $x$、再对 $y$（混合偏导在连续条件下次序可交换）。**不能**把 $F_x$ 或 $F_y$ 单独当成联合密度；它们对应的是边缘密度的累积形式。

> 连续型变量满足 $P\{X=x,Y=y\}=0$。密度值 $f(x,y)$ 不是“点的概率”，而是单位面积附近的概率强度；概率必须对区域积分。

### 1.4 三种对象的关系总表

| 对象 | 离散型 | 连续型 |
|:---|:---|:---|
| 联合分布函数 | 对 $p_{ij}$ 在左下区域求和 | 对 $f(u,v)$ 在左下区域二重积分 |
| 联合分布本身 | $p_{ij}=P(X=x_i,Y=y_j)$ | 区域概率 $\iint_Df\,dxdy$ |
| 从联合到边缘 | 对另一个变量求和 | 对另一个变量积分 |
| 从分布函数到密度 | 一般不存在普通导数密度 | $f=F_{xy}$（需满足相应光滑条件） |

## 2. 边缘分布：把另一个变量“消去”

边缘分布只描述 $X$ 或只描述 $Y$，本质是把联合分布沿另一个方向全部加总。

### 2.1 边缘分布函数

无论离散还是连续，都有
$$
F_X(x)=P(X\le x)=\lim_{y\to+\infty}F(x,y),\qquad
F_Y(y)=P(Y\le y)=\lim_{x\to+\infty}F(x,y).
$$

若变量连续且边缘密度存在，则
$$
f_X(x)=F_X'(x),\qquad f_Y(y)=F_Y'(y).
$$

### 2.2 由联合分布求边缘分布

**离散型：**
$$
p_{i\cdot}=P(X=x_i)=\sum_jp_{ij},\qquad
p_{\cdot j}=P(Y=y_j)=\sum_ip_{ij}.
$$

**连续型：**
$$
\boxed{f_X(x)=\int_{-\infty}^{+\infty}f(x,y)\,dy},\qquad
\boxed{f_Y(y)=\int_{-\infty}^{+\infty}f(x,y)\,dx}.
$$

积分上下限看似是 $(-\infty,+\infty)$，实际只需在联合密度非零的支持域上积分。若支持域为 $D$，例如 $0<x<y<1$，则
$$
f_X(x)=\int_x^1f(x,y)\,dy\ (0<x<1),\qquad
f_Y(y)=\int_0^yf(x,y)\,dx\ (0<y<1).
$$
**关键：上下限由支持域决定，不能机械地把另一个变量从 $-\infty$ 积到 $+\infty$后不管区域限制。**

### 2.3 边缘与联合的可逆性辨析

- 联合分布可以推出两个边缘分布；但仅知道两个边缘分布，通常不能唯一确定联合分布，因为变量之间还可能存在不同的依赖关系。
- 若已知联合密度，先积分得到边缘密度；若已知联合分布函数，先令另一个变量趋于 $+\infty$ 得边缘分布函数，再求导得到边缘密度。
- 边缘密度满足 $\int f_X(x)dx=\int f_Y(y)dy=1$，但 $f(x,y)$ 不等于 $f_X(x)+f_Y(y)$；独立时才是乘积 $f_X(x)f_Y(y)$。

## 3. 条件分布：固定一个变量后重新归一化

### 3.1 离散型

在 $p_{\cdot j}>0$ 时，
$$
P(X=x_i\mid Y=y_j)=\frac{p_{ij}}{p_{\cdot j}},\qquad
P(Y=y_j\mid X=x_i)=\frac{p_{ij}}{p_{i\cdot}}.
$$

### 3.2 连续型

在 $f_Y(y)>0$（或 $f_X(x)>0$）处，
$$
\boxed{f_{X\mid Y}(x\mid y)=\frac{f(x,y)}{f_Y(y)}},\qquad
\boxed{f_{Y\mid X}(y\mid x)=\frac{f(x,y)}{f_X(x)}}.
$$

条件密度对被固定的值仍要积分为 $1$：
$$
\int_{-\infty}^{+\infty}f_{X\mid Y}(x\mid y)\,dx=1.
$$
对应的条件分布函数为
$$
F_{X\mid Y}(x\mid y)=\int_{-\infty}^{x}f_{X\mid Y}(u\mid y)\,du.
$$
常见错误是把 $f(x,y)$ 直接当作 $f_{X\mid Y}(x\mid y)$；除以边缘密度的作用正是把固定 $Y=y$ 后的切片重新归一化。

### 3.3 混合型二维分布：先看条件分布属于哪一类

题目可能给出一个连续变量和一个离散变量，也可能给出“给定 $X=x$ 后，$Y$ 的类型”。这时不能机械地套用纯连续的二重密度公式，必须先确定联合分布的组成方式。

统一的思想是：先求条件分布，再对条件变量的分布作加权平均。对任意集合 $B$，
$$
P(Y\in B)=\int P(Y\in B\mid X=x)\,dF_X(x)
$$
或在 $X$ 离散时写成
$$
P(Y\in B)=\sum_iP(X=x_i)\,P(Y\in B\mid X=x_i).
$$
这里的积分或求和就是全概率公式的随机变量版本。

| 已知情况 | 联合分布的写法 | 求 $Y$ 的边缘分布 | 是否有普通二维密度 |
|:---|:---|:---|:---|
| $X,Y$ 都离散 | $p_{ij}=P(X=x_i,Y=y_j)$ | $p_Y(y_j)=\sum_i p_{ij}$ | 不需要 |
| $X,Y$ 都连续 | $f_X(x)f_{Y\mid X}(y\mid x)$ | $f_Y(y)=\int f_X(x)f_{Y\mid X}(y\mid x)\,dx$ | 有，前提是条件密度存在 |
| $X$ 连续，$Y\mid X=x$ 离散 | $f_X(x)p_{Y\mid X}(y_j\mid x)\,dx$ | $P(Y=y_j)=\int f_X(x)p_{Y\mid X}(y_j\mid x)\,dx$ | 一般没有，概率集中在若干水平线 $y=y_j$ 上 |
| $X$ 离散，$Y\mid X=x_i$ 连续 | $p_X(x_i)f_{Y\mid X}(y\mid x_i)\,dy$ | $f_Y(y)=\sum_i p_X(x_i)f_{Y\mid X}(y\mid x_i)$ | 一般没有，概率集中在若干竖直线 $x=x_i$ 上 |

表中 $f_X(x)p_{Y\mid X}(y_j\mid x)\,dx$ 的含义是
$$
P\{x<X\le x+dx,\ Y=y_j\}
\approx f_X(x)p_{Y\mid X}(y_j\mid x)\,dx.
$$
它是“对 $x$ 有密度、对 $y$ 有点质量”的密度—概率质量混合写法，不是可以对 $dx\,dy$ 做二重积分的普通联合密度。

#### 情形一：$X$ 连续，给定 $X=x$ 后 $Y$ 离散

设 $X$ 的密度为 $f_X(x)$，且
$$
P(Y=y_j\mid X=x)=p_j(x),\qquad \sum_jp_j(x)=1.
$$
则
$$
P(Y=y_j)=\int_{-\infty}^{+\infty}f_X(x)p_j(x)\,dx.
$$
若题目反过来要求在观察到 $Y=y_j$ 后求 $X$ 的条件分布，则 $X\mid Y=y_j$ 是连续型，且
$$
\boxed{
f_{X\mid Y}(x\mid y_j)
=\frac{f_X(x)p_j(x)}
{\displaystyle\int_{-\infty}^{+\infty}f_X(u)p_j(u)\,du}
}
$$
（分母就是 $P(Y=y_j)$）。因此“$X$ 连续、$Y$ 离散”并不意味着所有条件分布都离散：固定 $X=x$ 时看 $Y$ 的分布，固定 $Y=y_j$ 时看 $X$ 的分布。
对任意函数 $g$，
$$
E[g(X,Y)]
=\int_{-\infty}^{+\infty}
f_X(x)\sum_jg(x,y_j)p_j(x)\,dx.
$$
特别地，
$$
E(Y\mid X=x)=\sum_jy_jp_j(x),\qquad
E(Y)=\int E(Y\mid X=x)f_X(x)\,dx.
$$

典型模型：$X\sim U(0,1)$，且 $Y\mid X=x\sim B(1,x)$。此时
$$
P(Y=1)=\int_0^1x\,dx=\frac12,\qquad
P(Y=0)=\frac12.
$$
虽然 $X$ 连续、$Y$ 离散，但 $(X,Y)$ 没有普通二维密度；不能写成 $\iint f(x,y)\,dx\,dy$ 来求 $P(Y=1)$。

#### 情形二：$X$ 离散，给定 $X=x_i$ 后 $Y$ 连续

设
$$
P(X=x_i)=p_i,\qquad
f_{Y\mid X}(y\mid x_i)
$$
为条件密度，则 $Y$ 的边缘密度是条件密度的混合：
$$
\boxed{f_Y(y)=\sum_i p_i f_{Y\mid X}(y\mid x_i).}
$$
注意这是求和，不是积分。反过来，在 $f_Y(y)>0$ 处，
$$
P(X=x_i\mid Y=y)
=\frac{p_i f_{Y\mid X}(y\mid x_i)}{f_Y(y)}.
$$
这说明观察到连续变量 $Y=y$ 后，离散变量 $X$ 仍然用概率质量描述，只是概率由贝叶斯公式重新归一化。

#### 情形三：$X,Y$ 都连续

若 $X$ 有密度 $f_X(x)$，且 $Y\mid X=x$ 有条件密度 $f_{Y\mid X}(y\mid x)$，则
$$
\boxed{f_{X,Y}(x,y)=f_X(x)f_{Y\mid X}(y\mid x).}
$$
于是
$$
f_Y(y)=\int_{-\infty}^{+\infty}f_X(x)f_{Y\mid X}(y\mid x)\,dx.
$$
先写支持域再积分；如果条件密度只在 $a(x)<y<b(x)$ 上非零，积分上下限必须随 $x$ 的支持域变化。

> **做题顺序：** 先判断 $X$ 是离散还是连续，再判断 $Y\mid X=x$ 是分布律还是条件密度；离散方向求和，连续方向积分。看到“给定 $X=x$”时，条件分布的类型可能与 $X$ 本身不同。

## 4. 随机变量的独立性

### 4.1 判定条件

$X,Y$ 相互独立，当且仅当对所有 $x,y$ 有
$$
\boxed{F(x,y)=F_X(x)F_Y(y)}.
$$

等价地：

- 离散型：$\boxed{p_{ij}=p_{i\cdot}p_{\cdot j}}$；
- 连续型：$\boxed{f(x,y)=f_X(x)f_Y(y)}$（在几乎处处意义下）。

连续型判断时有一个高频陷阱：若 $f(x,y)=g(x)h(y)$，还必须检查支持域是否为矩形直积 $D_X\times D_Y$，并检查归一化常数；三角形区域 $0<x<y<1$ 即使表达式看似可分离，也通常不独立。

### 4.2 独立性的等价理解与结论

若 $X,Y$ 独立，则
$$
f_{X\mid Y}(x\mid y)=f_X(x),\qquad f_{Y\mid X}(y\mid x)=f_Y(y),
$$
即知道一个变量的取值不会改变另一个变量的分布；反过来条件分布等于边缘分布也可作为独立性的判据（在定义有意义处）。

1. 若 $X,Y$ 独立，则任意函数 $g(X)$ 与 $h(Y)$ 独立；
2. 独立一定不相关，但不相关一般不能推出独立；
3. 二维正态分布中有特殊结论：$X,Y$ 独立 $\Longleftrightarrow\rho=0$；
4. 独立性是联合分布层面的性质，不能仅凭两个边缘分布判断。

## 5. 二维随机变量函数的分布

### 定义法：先求分布函数
设 $Z=g(X,Y)$，连续型情形下令

$$
D_z=\{(x,y):g(x,y)\le z\},
$$

则

$$
F_Z(z)=P\{Z\le z\}=P\{g(X,Y)\le z\}
=\iint_{D_z}f_{X,Y}(x,y)\,dxdy,
$$

再对 $z$ 求导得到 $f_Z(z)=F_Z'(z)$。离散型则直接按所有满足 $g(x_i,y_j)=z$ 的点求和：

$$
P\{Z=z\}=\sum_{g(x_i,y_j)=z}p_{ij}.
$$

### 公式法：二元换元与雅可比
若要同时研究两个新变量

$$
U=g_1(X,Y),\qquad V=g_2(X,Y),
$$

且可以反解为 $x=x(u,v),y=y(u,v)$，则

$$
f_{U,V}(u,v)=f_{X,Y}\bigl(x(u,v),y(u,v)\bigr)
\left|\frac{\partial(x,y)}{\partial(u,v)}\right|.
$$

最后边缘化得到单个变量的密度：

$$
f_U(u)=\int_{-\infty}^{+\infty}f_{U,V}(u,v)\,dv.
$$

若逆变换有多个分支，应将所有分支的密度相加。做题时通常先选一个辅助变量，把 $(X,Y)$ 变成 $(U,V)$，算出雅可比后再积分消去辅助变量。

### (1) 和的分布：卷积定理

当题目要求求
$$
Z=X+Y
$$
的分布，尤其是 $X,Y$ 独立且已知各自分布时，优先考虑**卷积定理**。它的本质是：固定 $X=x$，则必须有 $Y=z-x$，再对所有可能的 $x$ 加总。

#### 连续型

一般情形（不要求独立）为
$$
\boxed{f_Z(z)=\int_{-\infty}^{+\infty}f_{X,Y}(x,z-x)\,dx.}
$$
若 $X,Y$ 独立，则联合密度可以拆成乘积，得到通常所说的卷积公式：

$$
 \boxed{f_Z(z)=\int_{-\infty}^{+\infty}f_X(x)f_Y(z-x)\,dx}
=\int_{-\infty}^{+\infty}f_X(z-y)f_Y(y)\,dy.
$$

实际积分范围不是永远整个实轴，而是满足
$$
x\in\operatorname{supp}(X),\qquad z-x\in\operatorname{supp}(Y)
$$
的交集。若 $X\in[a,b]$、$Y\in[c,d]$，则
$$
\max(a,z-d)\le x\le\min(b,z-c).
$$
当下限大于上限时，$f_Z(z)=0$；当 $z$ 跨过不同区间时，通常需要分段写出结果。

#### 离散型

若 $X,Y$ 独立且取值为整数（或可列取值），则
$$
\boxed{P(Z=z)=\sum_xP(X=x)P(Y=z-x).}
$$
非独立时不能拆成两个边缘概率，必须使用联合分布：

$$
P(Z=z)=\sum_xP(X=x,Y=z-x).
$$

#### 什么时候用、什么时候不要硬套

- **独立和、独立重复试验的总和**：已知 $X,Y$ 的密度或分布律，求 $X+Y$，首选卷积。
- **题目给出联合密度**：即使不独立，也可用 $f_{X,Y}(x,z-x)$ 积分；不能擅自改成 $f_X(x)f_Y(z-x)$。
- **最大值、最小值**：通常用分布函数乘法，不用卷积。
- **线性组合 $aX+bY$**：先把变量缩放；若 $a\ne0$，可令 $U=aX$，再与 $bY$ 做卷积，或直接用分布函数法。
- **支持域复杂、分段很多**：先画出直线 $x+y=z$ 与支持域的交线，确定积分范围，再计算。

若 $X_1,\ldots,X_n$ 相互独立，$S_n=X_1+\cdots+X_n$，则总和密度是各密度的连续卷积；同分布时可递推
$$
f_{S_n}=f_{S_{n-1}}*f_X,\qquad
(f*g)(z)=\int_{-\infty}^{+\infty}f(x)g(z-x)\,dx.
$$
离散型则把积分换成求和。卷积满足交换律 $f*g=g*f$ 和结合律 $(f*g)*h=f*(g*h)$，所以多项独立和可以按方便的顺序逐步计算。

> **速记：** 求“和”看卷积，求“最大/最小”看分布函数；独立才能把联合密度（或联合分布律）拆成边缘乘积；范围由支持域决定。

### (2) 积与商的分布：换元法
对 $Z=XY$，取 $U=X,V=XY$，则 $x=u,y=v/u$，
$\left|\dfrac{\partial(x,y)}{\partial(u,v)}\right|=\dfrac1{|u|}$，从而

$$
f_Z(z)=\int_{-\infty}^{+\infty}\frac1{|x|}
f_{X,Y}\left(x,\frac zx\right)dx.
$$

对 $Z=X/Y$，取 $U=Y,V=X/Y$，则 $x=uv,y=u$，雅可比绝对值为 $|u|$，从而

$$
f_Z(z)=\int_{-\infty}^{+\infty}|y|f_{X,Y}(yz,y)\,dy.
$$

积分范围还要结合 $(X,Y)$ 的取值区域确定，不能机械地写成整个实轴。

### (3) 最大值与最小值
若 $X,Y$ 独立，则

$$
F_{\max(X,Y)}(z)=F_X(z)F_Y(z),\qquad
F_{\min(X,Y)}(z)=1-[1-F_X(z)][1-F_Y(z)].
$$

## 6. 常见二维分布

### 二维均匀分布
区域 $D$ 上的均匀分布：
$$f(x, y) = \begin{cases}
\dfrac{1}{S(D)}, & (x, y) \in D \\[8pt]
0, & \text{其他}
\end{cases}$$

### 二维正态分布 $(X, Y) \sim N(\mu_1, \mu_2, \sigma_1^2, \sigma_2^2, \rho)$

$$f(x, y) = \frac{1}{2\pi\sigma_1\sigma_2\sqrt{1-\rho^2}} \exp\left\{-\frac{1}{2(1-\rho^2)}\left[\frac{(x-\mu_1)^2}{\sigma_1^2} - 2\rho\frac{(x-\mu_1)(y-\mu_2)}{\sigma_1\sigma_2} + \frac{(y-\mu_2)^2}{\sigma_2^2}\right]\right\}$$

**重要性质：**
- 边缘分布：$X \sim N(\mu_1, \sigma_1^2)$，$Y \sim N(\mu_2, \sigma_2^2)$
- 条件分布：$Y \mid X = x \sim N\left(\mu_2 + \rho\frac{\sigma_2}{\sigma_1}(x-\mu_1),\; \sigma_2^2(1-\rho^2)\right)$
- 独立 $\Leftrightarrow \rho = 0$
- 若 $(X, Y)$ 服从二维正态，则 $aX + bY$ 也服从正态分布

---

# 四、随机变量的数字特征

## 1. 数学期望

### 定义
- **离散型：** $E(X) = \sum\limits_k x_k p_k$
- **连续型：** $E(X) = \displaystyle\int_{-\infty}^{+\infty} x f(x)\,dx$

### 随机变量函数的期望
- **离散型：** $E[g(X)] = \sum\limits_k g(x_k)p_k$
- **连续型：** $E[g(X)] = \displaystyle\int_{-\infty}^{+\infty} g(x) f(x)\,dx$
- **二维：** $E[g(X, Y)] = \begin{cases}\sum\limits_i\sum\limits_j g(x_i, y_j)p_{ij} \\[4pt] \displaystyle\iint g(x, y)f(x, y)\,dxdy\end{cases}$

### 性质
1. $E(C) = C$（$C$ 为常数）
2. $E(kX) = kE(X)$
3. $E(X \pm Y) = E(X) \pm E(Y)$（**无条件成立**）
4. 若 $X, Y$ 独立，则 $E(XY) = E(X)E(Y)$（反之不成立！）

## 2. 方差

### 定义
$$D(X) = \text{Var}(X) = E\left[(X - E(X))^2\right] = E(X^2) - [E(X)]^2$$

### 性质
1. $D(C) = 0$
2. $D(kX) = k^2 D(X)$
3. $D(X \pm Y) = D(X) + D(Y) \pm 2\text{Cov}(X, Y)$
4. 若 $X, Y$ 独立，则 $D(X \pm Y) = D(X) + D(Y)$
5. $D(X) = 0 \;\Leftrightarrow\; P\{X = C\} = 1$

### 全期望与全方差公式（重期望、重方差）

把一个复杂随机变量按另一个随机变量分层，是处理混合型分布、条件分布题和分层抽样题的统一方法。常把下面两式合称为“亚当夏娃公式”：
$$
\boxed{E(Y)=E\bigl[E(Y\mid X)\bigr]}
$$
以及
$$
\boxed{D(Y)=E\bigl[D(Y\mid X)\bigr]
+D\bigl(E(Y\mid X)\bigr).}
$$

第一式称为**全期望公式**（重期望公式），第二式称为**全方差公式**（重方差公式）。它们不要求 $X,Y$ 独立，只要相应期望和方差存在。

当 $X$ 为离散型，$P(X=x_i)=p_i$ 时，
$$
E(Y)=\sum_iE(Y\mid X=x_i)p_i,
$$
$$
D(Y)=\sum_iD(Y\mid X=x_i)p_i
+\sum_i\bigl(E(Y\mid X=x_i)-E(Y)\bigr)^2p_i.
$$
当 $X$ 为连续型，密度为 $f_X(x)$ 时，
$$
E(Y)=\int_{-\infty}^{+\infty}E(Y\mid X=x)f_X(x)\,dx,
$$
$$
D(Y)=\int_{-\infty}^{+\infty}D(Y\mid X=x)f_X(x)\,dx
+\int_{-\infty}^{+\infty}
\bigl(E(Y\mid X=x)-E(Y)\bigr)^2f_X(x)\,dx.
$$

对任意可积函数 $g(X,Y)$，全期望公式同样成立：
$$
\boxed{E[g(X,Y)]
=E\left[E\bigl(g(X,Y)\mid X\bigr)\right].}
$$
例如在“$X$ 连续、$Y\mid X=x$ 离散”的情形，
$$
E(Y\mid X=x)=\sum_jy_jP(Y=y_j\mid X=x),
$$
外层再对 $x$ 积分；在“$X$ 离散、$Y\mid X=x_i$ 连续”的情形，内层先对 $y$ 积分，外层对 $i$ 求和。

全方差公式可拆成两部分理解：
$$
D(Y)=
\underbrace{E[D(Y\mid X)]}_{\text{层内波动}}
+\underbrace{D(E[Y\mid X])}_{\text{层间均值差异}}.
$$
第一项表示固定 $X$ 后各层内部的平均波动，第二项表示不同 $X$ 层的条件均值彼此不同所带来的波动。只写第一项会漏掉层间差异，只有在 $E(Y\mid X)$ 为常数时，才有 $D(Y)=E[D(Y\mid X)]$。

若按互斥完备事件组 $\{A_i\}$ 分层，则对应形式为
$$
E(Y)=\sum_iE(Y\mid A_i)P(A_i),
$$
$$
D(Y)=\sum_i\left[D(Y\mid A_i)
+\bigl(E(Y\mid A_i)-E(Y)\bigr)^2\right]P(A_i).
$$

### 线性组合、和与平方项
对常数 $a_i,b$，有

$$
E\left(\sum_{i=1}^n a_iX_i+b\right)
=\sum_{i=1}^n a_iE(X_i)+b.
$$

一般情形下

$$
D\left(\sum_{i=1}^n a_iX_i\right)
=\sum_{i=1}^n a_i^2D(X_i)
+2\sum_{1\le i<j\le n}a_ia_j\operatorname{Cov}(X_i,X_j).
$$

若 $X_1,\ldots,X_n$ 相互独立，协方差项为 $0$，于是
$$
D\left(\sum_{i=1}^n a_iX_i\right)=\sum_{i=1}^n a_i^2D(X_i).
$$

平方项常用公式为
$$
E(X^2)=D(X)+[E(X)]^2,\qquad
D(X^2)=E(X^4)-[E(X^2)]^2.
$$

若 $X_1,\ldots,X_n$ 独立同分布，记 $S_n=\sum_{i=1}^nX_i$，$E(X_i)=\mu$，$D(X_i)=\sigma^2$，则
$$
E(S_n)=n\mu,\qquad D(S_n)=n\sigma^2,
$$
$$
E(S_n^2)=n\sigma^2+n^2\mu^2,\qquad
E\left(\sum_{i=1}^nX_i^2\right)=n(\sigma^2+\mu^2),
$$
$$
D\left(\sum_{i=1}^nX_i^2\right)=nD(X^2).
$$

### 标准差
$$\sigma(X) = \sqrt{D(X)}$$

## 3. 常见分布的期望与方差

| 分布 | 参数 | $E(X)$ | $D(X)$ | 备注 |
|:---|:---:|:---:|:---:|:---|
| 0-1 分布 | $p$ | $p$ | $p(1-p)$ | $X\sim B(1,p)$ |
| 二项分布 | $n, p$ | $np$ | $np(1-p)$ | $X\sim B(n,p)$ |
| 泊松分布 | $\lambda$ | $\lambda$ | $\lambda$ | $X\sim P(\lambda)$ |
| 几何分布 | $p$ | $\dfrac{1}{p}$ | $\dfrac{1-p}{p^2}$ | $X\sim G(p)$ |
| 超几何 | $N,M,n$ | $n\dfrac{M}{N}$ | $n\dfrac{M}{N}\left(1-\dfrac{M}{N}\right)\dfrac{N-n}{N-1}$ | |
| 均匀分布 | $a, b$ | $\dfrac{a+b}{2}$ | $\dfrac{(b-a)^2}{12}$ | $X\sim U(a,b)$ |
| 指数分布 | $\lambda$ | $\dfrac{1}{\lambda}$ | $\dfrac{1}{\lambda^2}$ | $X\sim\text{Exp}(\lambda)$ |
| 正态分布 | $\mu, \sigma^2$ | $\mu$ | $\sigma^2$ | $X\sim N(\mu,\sigma^2)$ |
| $\chi^2$ 分布 | $n$ | $n$ | $2n$ | $X\sim\chi^2(n)$ |
| $t$ 分布 | $n$ | $0\;(n>1)$ | $\dfrac{n}{n-2}\;(n>2)$ | $X\sim t(n)$ |
| $F$ 分布 | $n_1, n_2$ | $\dfrac{n_2}{n_2-2}\;(n_2>2)$ | $\dfrac{2n_2^2(n_1+n_2-2)}{n_1(n_2-2)^2(n_2-4)}\;(n_2>4)$ | $X\sim F(n_1,n_2)$ |

## 4. 协方差与相关系数

### 协方差
$$\text{Cov}(X, Y) = E\left[(X - E(X))(Y - E(Y))\right] = E(XY) - E(X)E(Y)$$

**性质：**
1. $\text{Cov}(X, Y) = \text{Cov}(Y, X)$
2. $\text{Cov}(aX, bY) = ab\,\text{Cov}(X, Y)$
3. $\text{Cov}(X_1+X_2, Y) = \text{Cov}(X_1, Y) + \text{Cov}(X_2, Y)$
4. $\text{Cov}(X, X) = D(X)$
5. $\text{Cov}(X, C) = 0$

### 相关系数
$$\rho_{XY} = \frac{\text{Cov}(X, Y)}{\sqrt{D(X)}\sqrt{D(Y)}}$$

**性质：**
1. $|\rho_{XY}| \leq 1$
2. $|\rho_{XY}| = 1 \;\Leftrightarrow\;$ 存在常数 $a, b$ 使 $P\{Y = aX + b\} = 1$（线性相关）
3. $\rho_{XY} = 0 \;\Leftrightarrow\; X$ 与 $Y$ 不相关
4. 若 $X, Y$ 独立，则 $\rho_{XY} = 0$（反之不成立，除非正态）

### 协方差矩阵
对于 $(X_1, X_2, \cdots, X_n)$：
$$\Sigma = \begin{pmatrix}
D(X_1) & \text{Cov}(X_1, X_2) & \cdots & \text{Cov}(X_1, X_n) \\
\text{Cov}(X_2, X_1) & D(X_2) & \cdots & \text{Cov}(X_2, X_n) \\
\vdots & \vdots & \ddots & \vdots \\
\text{Cov}(X_n, X_1) & \text{Cov}(X_n, X_2) & \cdots & D(X_n)
\end{pmatrix}$$

## 5. 矩与协方差矩阵

### 原点矩
$$\nu_k = E(X^k),\quad k = 1, 2, \cdots$$

### 中心矩
$$\mu_k = E\left[(X - E(X))^k\right],\quad k = 1, 2, \cdots$$

### 混合矩
- $k+l$ 阶混合原点矩：$E(X^k Y^l)$
- $k+l$ 阶混合中心矩：$E\left[(X - E(X))^k (Y - E(Y))^l\right]$

---

# 五、大数定律与中心极限定理

## 1. 切比雪夫不等式

设 $E(X) = \mu$，$D(X) = \sigma^2 < \infty$，则对任意 $\varepsilon > 0$：
$$\boxed{P\{|X - \mu| \geq \varepsilon\} \leq \frac{\sigma^2}{\varepsilon^2}}$$

等价形式：
$$P\{|X - \mu| < \varepsilon\} \geq 1 - \frac{\sigma^2}{\varepsilon^2}$$

## 2. 大数定律

### 依概率收敛
$X_n \xrightarrow{P} a$：$\displaystyle\lim_{n\to\infty} P\{|X_n - a| < \varepsilon\} = 1$

### 伯努利大数定律
$n$ 次独立重复试验，事件 $A$ 出现 $n_A$ 次，$P(A) = p$：
$$\frac{n_A}{n} \xrightarrow{P} p$$

### 切比雪夫大数定律
$X_1, X_2, \cdots$ 两两不相关，方差有公共上界 $C$：
$$\frac{1}{n}\sum_{i=1}^n X_i \xrightarrow{P} \frac{1}{n}\sum_{i=1}^n E(X_i)$$

### 辛钦大数定律（独立同分布）
$X_1, X_2, \cdots$ 独立同分布，$E(X_i) = \mu$：
$$\frac{1}{n}\sum_{i=1}^n X_i \xrightarrow{P} \mu$$

## 3. 中心极限定理

### 林德伯格-列维（独立同分布）中心极限定理
$X_1, X_2, \cdots, X_n$ 独立同分布，$E(X_i) = \mu$，$D(X_i) = \sigma^2$：
$$\boxed{\frac{\sum\limits_{i=1}^n X_i - n\mu}{\sqrt{n}\,\sigma} \xrightarrow{d} N(0, 1)}$$

即当 $n$ 充分大时：
$$\sum_{i=1}^n X_i \;\dot{\sim}\; N(n\mu, n\sigma^2)$$

### 棣莫弗-拉普拉斯（二项分布逼近）中心极限定理
$X \sim B(n, p)$：
$$\boxed{\frac{X - np}{\sqrt{np(1-p)}} \xrightarrow{d} N(0, 1)}$$

即当 $n$ 充分大时：
$$X \;\dot{\sim}\; N(np, np(1-p))$$

### 使用注意事项
- $n$ 至少 $\geq 30$ 才能有较好的近似
- 二项分布用正态近似时，有时会用到**连续性校正**：

  $$
  P\{k_1 \leq X \leq k_2\} \approx \Phi\left(\frac{k_2 + 0.5 - np}{\sqrt{np(1-p)}}\right) - \Phi\left(\frac{k_1 - 0.5 - np}{\sqrt{np(1-p)}}\right)
  $$

---

# 六、数理统计

## 1. 基本概念

### 总体与样本
- **总体：** 研究对象的全体（随机变量 $X$）
- **样本：** $X_1, X_2, \cdots, X_n$ 相互独立且与 $X$ 同分布

### 统计量
不含未知参数的样本函数 $T = g(X_1, \cdots, X_n)$

### 常用统计量

| 统计量 | 公式 | 期望 | 方差 |
|:---|:---|:---|:---|
| 样本均值 | $\overline{X} = \dfrac{1}{n}\sum\limits_{i=1}^n X_i$ | $\mu$ | $\dfrac{\sigma^2}{n}$ |
| 样本方差 | $S^2 = \dfrac{1}{n-1}\sum\limits_{i=1}^n (X_i - \overline{X})^2$ | $\sigma^2$ | — |
| 样本标准差 | $S = \sqrt{S^2}$ | — | — |
| $k$ 阶样本矩 | $A_k = \dfrac{1}{n}\sum\limits_{i=1}^n X_i^k$ | $\mu_k$ | — |
| $k$ 阶样本中心矩 | $B_k = \dfrac{1}{n}\sum\limits_{i=1}^n (X_i - \overline{X})^k$ | — | — |

> ⚠️ **注意区分：**
> - $S^2 = \dfrac{1}{n-1}\sum (X_i - \overline{X})^2$ 是**无偏样本方差**（$E(S^2) = \sigma^2$）
> - $\hat\sigma^2 = \dfrac{1}{n}\sum (X_i - \overline{X})^2$ 是**有偏样本方差**（有偏）

## 2. 三大抽样分布

### 先记住正态总体的基本分解
设 $X_1,\ldots,X_n$ 来自 $N(\mu,\sigma^2)$，记

$$
\overline X=\frac1n\sum_{i=1}^nX_i,\qquad
S^2=\frac1{n-1}\sum_{i=1}^n(X_i-\overline X)^2.
$$

则

$$
\overline X\sim N\left(\mu,\frac{\sigma^2}{n}\right),\qquad
\frac{(n-1)S^2}{\sigma^2}\sim\chi^2(n-1),\qquad
\overline X\perp S^2.
$$

这三个结论是后面 $\chi^2,t,F$ 三大分布的共同来源。

### (1) $\chi^2$ 分布：标准正态平方和
若 $Z_1,\ldots,Z_\nu$ 相互独立且 $Z_i\sim N(0,1)$，则

$$
U=\sum_{i=1}^{\nu}Z_i^2\sim\chi^2(\nu).
$$

其中 $\nu$ 称为自由度。核心性质为

$$
E(U)=\nu,\qquad D(U)=2\nu.
$$

若 $U_1\sim\chi^2(\nu_1)$、$U_2\sim\chi^2(\nu_2)$ 且相互独立，则

$$
U_1+U_2\sim\chi^2(\nu_1+\nu_2).
$$

因此，**独立标准正态变量的平方和仍为 $\chi^2$，自由度相加**；正态总体中样本离差平方和正是
$$
\frac{(n-1)S^2}{\sigma^2}\sim\chi^2(n-1).
$$

### (2) $t$ 分布：正态变量除以独立的卡方平方根
若 $Z\sim N(0,1)$，$U\sim\chi^2(\nu)$，且 $Z\perp U$，则

$$
T=\frac{Z}{\sqrt{U/\nu}}\sim t(\nu).
$$

与正态总体联系起来：

$$
T=\frac{\overline X-\mu}{S/\sqrt n}\sim t(n-1).
$$

常用性质为

$$
E(T)=0\quad(\nu>1),\qquad
D(T)=\frac{\nu}{\nu-2}\quad(\nu>2),
$$

$t(\nu)$ 关于 $0$ 对称，且自由度越大越接近 $N(0,1)$。

### (3) $F$ 分布：两个独立卡方的方差比
若 $U_1\sim\chi^2(\nu_1)$、$U_2\sim\chi^2(\nu_2)$ 且相互独立，则

$$
F=\frac{U_1/\nu_1}{U_2/\nu_2}\sim F(\nu_1,\nu_2).
$$

常用性质为

$$
E(F)=\frac{\nu_2}{\nu_2-2}\quad(\nu_2>2),
$$
$$
D(F)=\frac{2\nu_2^2(\nu_1+\nu_2-2)}
{\nu_1(\nu_2-2)^2(\nu_2-4)}\quad(\nu_2>4).
$$

正态总体样本方差比为

$$
\frac{S_1^2/\sigma_1^2}{S_2^2/\sigma_2^2}
\sim F(n_1-1,n_2-1),
$$

其中两组样本相互独立。另有

$$
T\sim t(\nu)\Longrightarrow T^2\sim F(1,\nu),
$$
$$
F(\nu_1,\nu_2)^{-1}\sim F(\nu_2,\nu_1),\qquad
F_{1-\alpha}(\nu_1,\nu_2)=\frac1{F_{\alpha}(\nu_2,\nu_1)}.
$$

### 三大分布的关系速记

$$
\chi^2=\text{独立标准正态的平方和},\qquad
t=\frac{N(0,1)}{\sqrt{\chi^2/\nu}},\qquad
F=\frac{\chi^2/\nu_1}{\chi^2/\nu_2}.
$$

记自由度时注意：单正态总体的 $t$ 统计量自由度为 $n-1$；两总体方差比的 $F$ 分布自由度分别为 $n_1-1,n_2-1$。

---

# 七、参数估计

## 1. 点估计

### 矩估计法（数字特征法）

**核心思想：** 用样本矩估计总体矩，即令：
$$
\frac{1}{n}\sum_{i=1}^n X_i^k = E(X^k) \quad(k=1,2,\cdots)
$$

**解题步骤：**
```
① 写出总体矩（用参数表示）：E(X), E(X²), ...
② 令样本矩 = 总体矩：Ā_k = μ_k
③ 解方程组，得到参数的估计量
```

> 常用：令 $\overline{X} = E(X)$ 即可得到 1 个方程；若有 2 个参数，再令 $\dfrac{1}{n}\sum X_i^2 = E(X^2)$

### 极大似然估计法（MLE）

**解题步骤：**
```
① 写出似然函数 L(θ) = ∏ f(x_i; θ)（连续型）或 ∏ p(x_i; θ)（离散型）
② 取对数 ln L(θ)
③ 令 d[ln L(θ)]/dθ = 0，解出 θ̂
④ 验证二阶导 < 0（确保是极大值）
```

### 常见分布的矩估计与极大似然估计

| 分布 | 参数 | 矩估计 $\hat\theta$ | 极大似然估计 $\hat\theta$ |
|:---|:---:|:---|:---|
| 0-1 分布 $B(1,p)$ | $p$ | $\overline{X}$ | $\overline{X}$ |
| 二项分布 $B(n,p)$（$n$ 已知） | $p$ | $\overline{X}/n$ | $\overline{X}/n$ |
| 泊松分布 $P(\lambda)$ | $\lambda$ | $\overline{X}$ | $\overline{X}$ |
| 均匀分布 $U(a,b)$ | $a,b$ | $\hat a = \overline{X} - \sqrt{3}S$，$\hat b = \overline{X} + \sqrt{3}S$ | $\hat a = X_{(1)}$，$\hat b = X_{(n)}$ |
| 指数分布 $\text{Exp}(\lambda)$ | $\lambda$ | $1/\overline{X}$ | $1/\overline{X}$ |
| 正态分布 $N(\mu,\sigma^2)$ | $\mu,\sigma^2$ | $\hat\mu = \overline{X}$，$\hat\sigma^2 = \dfrac{1}{n}\sum(X_i-\overline{X})^2$ | $\hat\mu = \overline{X}$，$\hat\sigma^2 = \dfrac{1}{n}\sum(X_i-\overline{X})^2$ |

> ⚠️ 正态分布的 $\sigma^2$ 极大似然估计是**有偏的**（$E(\hat\sigma^2) = \dfrac{n-1}{n}\sigma^2$），常用无偏样本方差 $S^2$ 代替。

## 2. 估计量的评选标准

### (1) 无偏性
$$E(\hat\theta) = \theta$$

- $\overline{X}$ 是 $\mu$ 的无偏估计 ✅
- $S^2 = \dfrac{1}{n-1}\sum (X_i - \overline{X})^2$ 是 $\sigma^2$ 的无偏估计 ✅
- $\hat\sigma^2 = \dfrac{1}{n}\sum (X_i - \overline{X})^2$ 是 $\sigma^2$ 的**有偏**估计 ❌

### (2) 有效性
若 $E(\hat\theta_1) = E(\hat\theta_2) = \theta$，且 $D(\hat\theta_1) < D(\hat\theta_2)$，则 $\hat\theta_1$ 比 $\hat\theta_2$ **更有效**。

### (3) 一致性（相合性）
$$\hat\theta \xrightarrow{P} \theta \quad\text{（依概率收敛）}$$

- $\overline{X}$ 是 $\mu$ 的一致估计 ✅
- $S^2$ 是 $\sigma^2$ 的一致估计 ✅

## 3. 区间估计

### 基本概念
置信区间 $[\hat\theta_L, \hat\theta_U]$ 满足：

$$
P\{\hat\theta_L < \theta < \hat\theta_U\} = 1 - \alpha
$$
其中 $1 - \alpha$ 为**置信水平**，$\alpha$ 为**显著性水平**。

### 正态总体均值的置信区间

| 情况 | $\sigma^2$ 已知 | $\sigma^2$ 未知 |
|:---|:---|:---|
| **枢轴量** | $Z = \dfrac{\overline{X} - \mu}{\sigma/\sqrt{n}} \sim N(0,1)$ | $T = \dfrac{\overline{X} - \mu}{S/\sqrt{n}} \sim t(n-1)$ |
| **置信区间** | $\left(\overline{X} \pm z_{\alpha/2}\dfrac{\sigma}{\sqrt{n}}\right)$ | $\left(\overline{X} \pm t_{\alpha/2}(n-1)\dfrac{S}{\sqrt{n}}\right)$ |

### 正态总体方差的置信区间

| 情况 | $\mu$ 已知 | $\mu$ 未知 |
|:---|:---|:---|
| **枢轴量** | $\dfrac{\sum (X_i - \mu)^2}{\sigma^2} \sim \chi^2(n)$ | $\dfrac{(n-1)S^2}{\sigma^2} \sim \chi^2(n-1)$ |
| **置信区间** | $\left(\dfrac{\sum (X_i - \mu)^2}{\chi^2_{\alpha/2}(n)},\; \dfrac{\sum (X_i - \mu)^2}{\chi^2_{1-\alpha/2}(n)}\right)$ | $\left(\dfrac{(n-1)S^2}{\chi^2_{\alpha/2}(n-1)},\; \dfrac{(n-1)S^2}{\chi^2_{1-\alpha/2}(n-1)}\right)$ |

### 常用分位点速查

> $\Phi(1.645) = 0.95$，$\Phi(1.96) = 0.975$，$\Phi(2.576) = 0.995$

| 置信水平 $1-\alpha$ | $\alpha$ | $z_{\alpha/2}$ |
|:---:|:---:|:---:|
| 90% | 0.10 | 1.645 |
| 95% | 0.05 | 1.96 |
| 99% | 0.01 | 2.576 |

---

# 八、假设检验

## 1. 基本概念

### 原假设与备择假设
- **原假设 $H_0$：** 需要检验的假设（通常为"相等"或"无差异"）
- **备择假设 $H_1$：** 与原假设对立的假设

### 两类错误

|  | 接受 $H_0$ | 拒绝 $H_0$ |
|:---|:---:|:---:|
| $H_0$ 为真 | ✅ 正确 | ❌ **第一类错误**（$\alpha$） |
| $H_0$ 为假 | ❌ **第二类错误**（$\beta$） | ✅ 正确 |

- $\alpha$ = $P\{\text{拒绝 }H_0 \mid H_0\text{ 为真}\}$（**显著性水平**）
- $\beta$ = $P\{\text{接受 }H_0 \mid H_0\text{ 为假}\}$
- **检验功效** = $1 - \beta$（正确拒绝 $H_0$ 的概率）

### 双边检验与单边检验

| 类型 | $H_0$ | $H_1$ | 拒绝域形式 |
|:---|:---|:---|:---|
| **双边检验** | $\mu = \mu_0$ | $\mu \neq \mu_0$ | $\|Z\| > z_{\alpha/2}$ |
| **左边检验** | $\mu \geq \mu_0$ | $\mu < \mu_0$ | $Z < -z_\alpha$ |
| **右边检验** | $\mu \leq \mu_0$ | $\mu > \mu_0$ | $Z > z_\alpha$ |

## 2. 正态总体均值的假设检验

### 单个总体 $N(\mu, \sigma^2)$

| 条件 | $H_0$ | 检验统计量 | 分布 | 拒绝域（显著性水平 $\alpha$） |
|:---|:---|:---|:---:|:---|
| $\sigma^2$ **已知** | $\mu = \mu_0$ | $Z = \dfrac{\overline{X} - \mu_0}{\sigma/\sqrt{n}}$ | $N(0,1)$ | $\|Z\| > z_{\alpha/2}$ |
| $\sigma^2$ **未知** | $\mu = \mu_0$ | $T = \dfrac{\overline{X} - \mu_0}{S/\sqrt{n}}$ | $t(n-1)$ | $\|T\| > t_{\alpha/2}(n-1)$ |

### 两个总体 $N(\mu_1, \sigma_1^2)$ 与 $N(\mu_2, \sigma_2^2)$

| 条件 | $H_0$ | 检验统计量 | 分布 |
|:---|:---|:---|:---:|
| $\sigma_1^2, \sigma_2^2$ 已知 | $\mu_1 = \mu_2$ | $Z = \dfrac{\overline{X} - \overline{Y}}{\sqrt{\frac{\sigma_1^2}{n_1} + \frac{\sigma_2^2}{n_2}}}$ | $N(0,1)$ |
| $\sigma_1^2 = \sigma_2^2$ 未知 | $\mu_1 = \mu_2$ | $T = \dfrac{\overline{X} - \overline{Y}}{S_w\sqrt{\frac{1}{n_1} + \frac{1}{n_2}}}$ | $t(n_1+n_2-2)$ |

其中 $S_w^2 = \dfrac{(n_1-1)S_1^2 + (n_2-1)S_2^2}{n_1 + n_2 - 2}$（**合并样本方差**）

## 3. 正态总体方差的假设检验

| 条件 | $H_0$ | 检验统计量 | 分布 | 拒绝域 |
|:---|:---|:---|:---:|:---|
| 单个总体 | $\sigma^2 = \sigma_0^2$ | $\chi^2 = \dfrac{(n-1)S^2}{\sigma_0^2}$ | $\chi^2(n-1)$ | $\chi^2 > \chi^2_{\alpha/2}$ 或 $< \chi^2_{1-\alpha/2}$ |
| 两个总体 | $\sigma_1^2 = \sigma_2^2$ | $F = \dfrac{S_1^2}{S_2^2}$ | $F(n_1-1, n_2-1)$ | $F > F_{\alpha/2}$ 或 $< F_{1-\alpha/2}$ |

## 4. $p$ 值检验法

$p$ 值 = 在原假设成立下，观察到比当前样本更极端结果的概率。

- $p < \alpha$ $\Rightarrow$ 拒绝 $H_0$（结果显著）
- $p > \alpha$ $\Rightarrow$ 接受 $H_0$（结果不显著）

---

# 九、易错点与高频考点总结

## ⚠️ 易错点清单

| 易错点 | 正解 |
|:-------|:-----|
| **独立 ≠ 不相关，不相关 ≠ 独立** | 独立 $\Rightarrow$ 不相关 ✅；不相关 $\not\Rightarrow$ 独立（正态除外） |
| **两两独立 ≠ 相互独立** | 相互独立要求任意组合的乘积公式都成立 |
| **$P(AB)=0$ 不能说明 $A,B$ 互斥** | 连续型中 $P(X=a)=0$，但 $\{X=a\}$ 非空 |
| **$E(XY) = E(X)E(Y)$ 不能说明独立** | 这只是**不相关**，不是独立 |
| **$D(X\pm Y) = D(X)+D(Y)$ 需要独立** | 否则还要 $\pm 2\text{Cov}(X,Y)$ |
| **$S^2$ 与 $\hat\sigma^2$ 混淆** | $S^2$ 无偏（分母 $n-1$），$\hat\sigma^2$ 有偏（分母 $n$） |
| **置信区间解释错误** | ❌ "$\mu$ 有 95% 概率落在区间内"；✅ "区间有 95% 的概率包含 $\mu$" |
| **假设检验中 $\alpha$ 和 $\beta$ 的关系** | 样本量固定时，$\alpha\downarrow$ 则 $\beta\uparrow$（此消彼长） |
| **正态近似 $n$ 不够大就使用** | 中心极限定理一般要求 $n \geq 30$ |
| **矩估计与 MLE 算错** | 矩估计用样本矩 = 总体矩；MLE 先写出似然函数再取对数求导 |

## 📌 高频考点

1. **全概率公式与贝叶斯公式**（尤其是"先验→后验"问题）
2. **常见分布的期望与方差**（二项、泊松、均匀、指数、正态必背）
3. **二维随机变量边缘分布与条件分布**（求积分、判断独立性）
4. **二维随机变量函数的分布**（$Z=X+Y$ 卷积公式、$\max/\min$ 分布）
5. **协方差与相关系数的计算**（利用 $E(XY)-E(X)E(Y)$）
6. **极大似然估计**（写出似然函数、取对数、求导、解方程）
7. **正态总体均值和方差的区间估计与假设检验**
8. **切比雪夫不等式与中心极限定理的应用**

## 💡 常用速记口诀

> **独立相乘，期望可拆；方差相加，需加协差**
> **正态加正态，仍是正态；期望方差，直接相加**
> **矩估用样本等于总体，MLE 取对数求导为零**
> **全概率由因导果，贝叶斯执果寻因**
> **大数定律收敛于期望，中心极限逼近正态**

## 📊 常用分位点与概率速查

| $z$ | 0.00 | 0.01 | 0.02 | 0.05 | 0.10 |
|:---:|:---:|:---:|:---:|:---:|:---:|
| $\Phi(z)$ | 0.5000 | 0.5040 | 0.5080 | 0.5199 | 0.5398 |
| **$z$** | **1.28** | **1.645** | **1.96** | **2.33** | **2.576** |
| $\Phi(z)$ | 0.8997 | 0.9500 | 0.9750 | 0.9900 | 0.9950 |

---

*本文档整理自《考研数学一 · 概率论与数理统计》考纲内全部知识点，适用于基础复习与考前速查。*
