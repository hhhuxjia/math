# 考研数学一 · 级数专题

---

> 本专题只保留级数的概念、性质、判别思路和幂级数收敛半径/收敛域。常用函数的幂级数展开式另见《积分级数公式合集》。

# 一、数项级数：先把“收敛”说清楚

## 1. 定义与必要条件

数项级数写作
$$
\sum_{n=1}^{\infty}a_n=a_1+a_2+\cdots,
$$
其部分和为
$$
S_n=\sum_{k=1}^na_k.
$$

若 $\lim\limits_{n\to\infty}S_n=S$ 为有限值，则称 $\sum a_n$ **收敛**，其和为 $S$；否则称其发散。

由
$$
a_n=S_n-S_{n-1}
$$
立刻得到最基本的必要条件：
$$
\boxed{\sum a_n\text{ 收敛}\ \Longrightarrow\ \lim_{n\to\infty}a_n=0.}
$$

若 $\lim a_n\ne0$ 或极限不存在，级数必发散。反过来不成立：$\sum 1/n$ 的通项趋于 $0$，但级数发散。

## 2. 基本性质

**线性性。** 若
$$
\sum a_n,\qquad \sum b_n
$$
都收敛，则
$$
\sum(\alpha a_n+\beta b_n)
=\alpha\sum a_n+\beta\sum b_n.
$$

**有限项不影响敛散性。** 添加、删除或修改有限项不改变收敛/发散，只会改变级数的和。

**柯西准则。**
$$
\sum a_n\text{ 收敛}
\Longleftrightarrow
\forall\varepsilon>0,\ \exists N,\ \forall m>n\ge N,
\quad
\left|\sum_{k=n+1}^{m}a_k\right|<\varepsilon.
$$
它表示：从足够靠后的任意一段“尾和”都能任意小。

**分组。** 原级数收敛时，对相邻有限项加括号不改变收敛性和级数和；反过来，分组后的级数收敛并不能推出原级数收敛。

## 3. 三类基准级数

后续多数判别，最后都要比较到以下基准。公式单独写出，避免表格中公式显示不完整。

**$p$ 级数**
$$
\sum_{n=1}^{\infty}\frac{1}{n^p}\text{ 收敛}
\Longleftrightarrow p>1.
$$
当 $p\le1$ 时，该级数发散。

**等比级数**
$$
\sum_{n=0}^{\infty}aq^n\text{ 收敛}
\Longleftrightarrow |q|<1.
$$
当 $|q|\ge1$ 时，该级数发散。

**对数型级数**
$$
\sum_{n=2}^{\infty}\frac{1}{n(\ln n)^p}\text{ 收敛}
\Longleftrightarrow p>1.
$$
当 $p\le1$ 时，该级数发散。

> 判断时先看通项的主导部分。含多项式、根式、对数时，通常先化成与 $1/n^p$ 或 $1/[n(\ln n)^p]$ 可比的形式；含阶乘、指数、$n^n$ 时，通常优先用比值或根值判别。

---

# 二、正项级数：核心是比较大小

正项级数指从某项起 $a_n\ge0$ 的级数。它的部分和单调不减，因此判断收敛，本质是判断部分和是否有上界。

### 正项级数的三个等价视角

对正项级数，
$$
\sum_{n=1}^{\infty}a_n\text{ 收敛}
\Longleftrightarrow
\{S_n\}\text{ 有上界}
\Longleftrightarrow
\{S_n\}\text{ 收敛}.
$$

这解释了比较判别法的本质：正项级数没有正负抵消，通项越大，部分和增长越快。

## 1. 比较判别法

若从某项起 $0\le a_n\le b_n$，则：

$$
\sum b_n\text{ 收敛}\Longrightarrow\sum a_n\text{ 收敛};
\qquad
\sum a_n\text{ 发散}\Longrightarrow\sum b_n\text{ 发散}.
$$

使用方向不能颠倒：

- 要证明 $\sum a_n$ 收敛，找一个**更大的收敛**级数；
- 要证明 $\sum a_n$ 发散，找一个**更小的发散**级数。

### 极限比较判别法

若 $a_n,b_n>0$，且
$$
\lim_{n\to\infty}\frac{a_n}{b_n}=l,\qquad 0<l<+\infty,
$$
则 $\sum a_n$ 与 $\sum b_n$ 同敛散。即 $a_n\sim b_n$ 时可直接替换。

两个常用单向结论：

$$
\lim\frac{a_n}{b_n}=0,\ \sum b_n\text{ 收敛}\Longrightarrow\sum a_n\text{ 收敛};
$$
$$
\lim\frac{a_n}{b_n}=+\infty,\ \sum b_n\text{ 发散}\Longrightarrow\sum a_n\text{ 发散}.
$$

> 极限比较法针对正项。一般项级数需要先研究 $\sum|a_n|$，不能直接拿变号通项比大小判定原级数。

### 主导项与增长速度

比较之前先识别主导项：多项式之比只看最高次项；根式先提出最高次幂或有理化；含阶乘、指数和 $n^n$ 时通常优先考虑比值或根值。

常用增长层级是
$$
\ln n\ll n^\alpha\ll a^n\ll n!\ll n^n
\qquad(\alpha>0,\ a>1).
$$
倒数的趋零快慢顺序相反。这个层级帮助选择比较对象，但最终仍应写出比较不等式或极限比较。

## 2. 比值判别法与根值判别法

对正项级数，若
$$
\lim_{n\to\infty}\frac{a_{n+1}}{a_n}=\rho,
$$
则

$$
\rho<1\Rightarrow\sum a_n\text{ 收敛},\qquad
\rho>1\Rightarrow\sum a_n\text{ 发散}.
$$

若
$$
\lim_{n\to\infty}\sqrt[n]{a_n}=\rho,
$$
也有同样结论。两种方法在 $\rho=1$ 时都不能判断。

| 特征 | 优先方法 |
|:---|:---|
| 含 $n!$、连乘、$n^n$、$a^n$ | 比值判别法 |
| 整体带 $n$ 次幂，如 $[u_n]^n$ | 根值判别法 |
| 判别结果为 $1$ | 换比较法、积分法或其他结构性方法 |

## 3. 积分判别法

若 $a_n=f(n)$，其中 $f(x)$ 在 $[N,+\infty)$ 上连续、正且单调递减，则
$$
\sum_{n=N}^{\infty}f(n)
\quad\text{与}\quad
\int_N^{+\infty}f(x)\,dx
$$
同敛散。

它尤其适合含 $\ln n$、$(\ln n)^p$、$\ln\ln n$ 的级数。使用前必须检查“正、连续、递减”；若积分原函数很难求，通常不如比较法直接。

## 4. 等价无穷小与主项比较

若 $a_n,b_n>0$ 且
$$
a_n\sim b_n,
$$
则两级数同敛散。这是极限比较判别法在极限为 $1$ 时的直接应用。

它的作用是把复杂通项化为“主导项”，再比较到三类基准级数。常见可用情形：
$$
\sin x\sim x,\qquad \tan x\sim x,\qquad \arcsin x\sim x,\qquad \arctan x\sim x,
$$
$$
\ln(1+x)\sim x,\qquad e^x-1\sim x,\qquad 1-\cos x\sim\frac{x^2}{2}\qquad(x\to0).
$$

使用限制：

1. 等价替换针对正项或最终同号通项；
2. 只能在“求敛散性”中替换，不能在求级数和时把每一项直接替换；
3. 若有相减导致主项抵消，必须继续展开或有理化，不能只看每一项的等价式。

## 5. 拉贝判别法与柯西凝聚判别法（补充）

### 拉贝判别法

对正项级数，令
$$
\lim_{n\to\infty}n\left(\frac{a_n}{a_{n+1}}-1\right)=r.
$$
则
$$
r>1\Rightarrow\sum a_n\text{ 收敛},\qquad
r<1\Rightarrow\sum a_n\text{ 发散}.
$$

它是比值判别法在极限等于 $1$ 时的细化，常用于通项接近 $1/n^p$ 的情形；当 $r=1$ 时仍不能判断。

### 柯西凝聚判别法

若 $a_n\ge0$ 且单调递减，则
$$
\sum_{n=1}^{\infty}a_n
\quad\text{与}\quad
\sum_{k=0}^{\infty}2^ka_{2^k}
$$
同敛散。

它特别适合 $\ln n$ 层数较多而积分不便的正项级数。本质是把 $[2^k,2^{k+1})$ 内约 $2^k$ 项用端点值进行比较。

## 6. 正项级数的解题顺序

1. 先确认从某项起非负。
2. 看是否已经是 $p$ 级数、等比级数或对数型基准。
3. 含阶乘/指数优先试比值或根值。
4. 含根式、三角、对数的小量时，用等价无穷小或有理化提取主项，再做极限比较。
5. 遇到 $\ln n$ 类优先考虑积分判别。
6. 比值、根值或拉贝判别落在临界值时，立即换比较或积分，不要继续硬算。

---

# 三、一般项级数：绝对收敛、交错与结构

一般项级数的通项可以正负交替。此时不能只比较 $a_n$ 的大小，先区分绝对收敛与条件收敛。

## 1. 绝对收敛与条件收敛

$$
\sum|a_n|\text{ 收敛}\Longrightarrow\sum a_n\text{ 收敛}.
$$

- 若 $\sum|a_n|$ 收敛，称 $\sum a_n$ **绝对收敛**；
- 若 $\sum a_n$ 收敛而 $\sum|a_n|$ 发散，称 **条件收敛**。

绝对收敛的判断，完全转化为正项级数 $\sum|a_n|$ 的判断。对于没有明显相消结构的变号级数，应优先尝试绝对收敛。

### 绝对收敛的稳定性

若
$$
\sum|a_n|
$$
收敛，则：

1. 任意子级数都绝对收敛；
2. 拆成有限个级数后，每一部分都绝对收敛；
3. 任意重排仍收敛，且和不变；
4. 若 $b_n$ 有界，则
$$
\sum a_nb_n
$$
绝对收敛。

条件收敛级数没有这些稳定性，不能随意拆奇偶项、重排或交换求和顺序。

## 2. 交错级数与莱布尼茨判别法

交错级数形如
$$
\sum_{n=1}^{\infty}(-1)^{n-1}u_n,\qquad u_n\ge0.
$$

若从某项起
$$
u_{n+1}\le u_n,\qquad \lim_{n\to\infty}u_n=0,
$$
则原级数收敛。这是莱布尼茨判别法；并且余项满足
$$
\boxed{|R_n|\le u_{n+1}.}
$$

判断顺序：

1. 先把正负号提出，确认是交错形式；
2. 验证 $u_n\to0$；
3. 验证 $u_n$ 从某项起递减；
4. 再检查 $\sum u_n$：若它也收敛，则绝对收敛；若发散，则通常得到条件收敛。

## 3. 狄利克雷与阿贝尔判别法

它们处理“一个因子提供振荡相消，另一个因子缓慢变化”的级数。

### 狄利克雷判别法

若

1. 部分和 $B_n=\sum_{k=1}^nb_k$ 有界；
2. $a_n$ 单调趋于 $0$；

则
$$
\sum_{n=1}^{\infty}a_nb_n
$$
收敛。

典型信号是 $b_n=(-1)^n$、$\sin(n\theta)$、$\cos(n\theta)$ 等。它们的部分和在合适条件下有界；$a_n$ 则通常是 $1/n^p$ 一类单调趋零因子。

### 阿贝尔判别法

若 $\sum a_n$ 收敛，且 $b_n$ 单调有界，则
$$
\sum a_nb_n
$$
收敛。

两者都来自分部求和。答题时先判定是否确实满足“部分和有界”或“单调有界”，不要看到振荡因子就直接套用。

## 4. 常见的相消结构

### 裂项

若能写成
$$
a_n=b_n-b_{n+1},
$$
则部分和
$$
S_n=b_1-b_{n+1}
$$
发生望远镜相消。关键不是强行裂项，而是观察相邻项是否有“平移后相消”的形状。

考研数学一中，优先掌握交错、裂项和绝对收敛；遇到 $\sin n,\cos n$ 等振荡因子时，再考虑狄利克雷或阿贝尔判别法。

## 5. 奇偶项级数：拆分、分组与陷阱

设
$$
\sum_{n=1}^{\infty}a_n
=a_1+a_2+a_3+a_4+\cdots.
$$
定义奇项级数、偶项级数与两项一组的配对级数：
$$
\sum_{n=1}^{\infty}a_{2n-1},\qquad
\sum_{n=1}^{\infty}a_{2n},\qquad
\sum_{n=1}^{\infty}\bigl(a_{2n-1}+a_{2n}\bigr).
$$

必须区分以下结论：

1. 若原级数收敛，则配对级数一定收敛，且和与原级数相同。这只是合法分组。
2. 若奇项级数与偶项级数都收敛，则原级数收敛，且
$$
\sum a_n=\sum a_{2n-1}+\sum a_{2n}.
$$
3. 原级数收敛**不能**推出奇项级数、偶项级数各自收敛。条件收敛时，原有的正负相消可能正是跨奇偶项发生的。
4. 若原级数绝对收敛，则奇项级数、偶项级数都绝对收敛，并可以任意拆分、重排。
5. 对正项级数，原级数收敛当且仅当奇项级数、偶项级数都收敛。

> 所以遇到奇偶项题，先判断题目是在“相邻两项分组”，还是“把奇项和偶项彻底拆开”。前者由普通收敛就能处理，后者通常需要绝对收敛或额外条件。

---

# 四、抽象级数：只凭已知条件能推出什么

抽象级数题不提供具体通项，结论必须来自线性性质、绝对收敛、比较不等式或部分和关系。最重要的原则是：**条件不够时不能强行判断，优先找反例。**

## 1. 抽象级数收敛性速查表

以下默认已知
$$
\sum_{n=1}^{\infty}a_n
$$
收敛。表中的“不能推出”表示存在反例，不是说该级数一定发散。

| 变形后的级数 | 结论 | 需要记住的条件或理由 |
|:---|:---|:---|
| $\sum \lvert a_n\rvert$ | 不能推出 | 原级数可能只是条件收敛。 |
| $\sum a_n^2$ | 不能推出 | 例如交错级数收敛，但平方后可变成调和级数。 |
| $\sum a_n a_{n+1}$ | 不能推出 | $a_n=(-1)^n/\sqrt n$ 时，乘积与 $-1/n$ 同阶。 |
| $\sum a_n/n$ | 一定收敛 | 阿贝尔判别法；$1/n$ 单调且有界。 |
| $\sum a_n/n^p$（$p>0$） | 一定收敛 | 同理，$1/n^p$ 单调有界。 |
| $\sum n a_n$ | 不能推出 | 乘上无界因子后，通项甚至可能不趋于 $0$。 |
| $\sum (a_n+a_{n+1})$ | 一定收敛 | 原级数与移位后的级数都收敛。 |
| $\sum (a_n-a_{n+1})$ | 一定收敛 | 望远镜：部分和为 $a_1-a_{N+1}$。 |
| $\sum(a_{2n-1}+a_{2n})$ | 一定收敛 | 这是相邻两项的合法分组。 |
| $\sum a_{2n-1}$ | 不能推出 | 条件收敛时，拆出奇项会破坏相消。 |
| $\sum a_{2n}$ | 不能推出 | 与奇项相同，不能从原级数收敛推出。 |
| $\sum(-1)^na_n$ | 不能推出 | 新符号可能消除原有相消。 |
| $\sum c_na_n$（$c_n$ 有界） | 不能推出 | 仅有界还不够；若 $c_n$ 单调，则可用阿贝尔。 |
| $\sum c_na_n$（$c_n$ 单调有界） | 一定收敛 | 阿贝尔判别法。 |

### 已知绝对收敛时的加强版

若已知
$$
\sum_{n=1}^{\infty}\lvert a_n\rvert
$$
收敛，则上表中涉及乘积、奇偶拆分和有界因子的结论都可以加强：

- $\sum a_{2n-1}$、$\sum a_{2n}$ 都绝对收敛；
- 若 $c_n$ 有界，则 $\sum c_na_n$ 绝对收敛；
- $\sum a_na_{n+1}$ 绝对收敛，因为 $a_n\to0$ 后有 $\lvert a_{n+1}\rvert\le1$，从而
$$
\lvert a_na_{n+1}\rvert\le\lvert a_n\rvert;
$$
- 任意重排、分组都不改变收敛性和级数和。

> 一句话：普通收敛只允许使用“线性、移位、单调有界乘子”；绝对收敛才允许稳定地拆分、重排和做绝对值比较。

## 2. 必然成立的结论

**线性组合。** 若
$$
\sum a_n,\qquad\sum b_n
$$
都收敛，则对任意常数 $\alpha,\beta$，
$$
\sum(\alpha a_n+\beta b_n)
$$
收敛。若 $\sum a_n$ 收敛而 $\sum b_n$ 发散，则
$$
\sum(a_n+b_n)
$$
必发散。

**正项比较。** 若从某项起
$$
0\le b_n\le a_n,\qquad \sum a_n\text{ 收敛},
$$
则 $\sum b_n$ 收敛；若从某项起
$$
0\le a_n\le b_n,\qquad \sum a_n\text{ 发散},
$$
则 $\sum b_n$ 发散。

**绝对收敛与幂次。** 若
$$
\sum|a_n|
$$
收敛，则 $\sum a_n$ 收敛。若 $\sum a_n$ 收敛且从某项起 $a_n\ge0$，则对任意 $p>1$，
$$
\sum a_n^p
$$
也收敛，因为 $a_n\to0$ 后有 $0\le a_n^p\le a_n$。

**相邻项构成的新级数。** 若 $\sum a_n$ 收敛，则
$$
\sum(a_n-a_{n+1})
$$
收敛，且其和为 $a_1$；另外
$$
\sum(a_{2n-1}+a_{2n})
$$
也收敛，且和与原级数相同。

其中 $\sum(a_n-a_{n+1})$ 的结论来自
$$
\sum_{n=1}^N(a_n-a_{n+1})=a_1-a_{N+1},\qquad a_n\to0.
$$

## 3. 不能直接推出的结论

以下都是抽象题常见陷阱：

$$
\sum a_n\text{ 收敛}
\not\Longrightarrow
\sum|a_n|\text{ 收敛}.
$$
原级数可能只是条件收敛。

$$
\sum a_n\text{ 收敛}
\not\Longrightarrow
\sum a_n^2\text{ 收敛}.
$$
平方会消掉符号，可能变成发散的正项级数。

$$
\sum a_n\text{ 收敛}
\not\Longrightarrow
\sum a_{2n}\text{ 收敛},
$$
也不能推出奇项级数收敛；抽取子级数会破坏相消。

$$
\sum a_n^2\text{ 收敛}
\not\Longrightarrow
\sum a_n\text{ 收敛},
\qquad
a_n\to0
\not\Longrightarrow
\sum a_n\text{ 收敛}.
$$

## 4. 高频变形的判断思路

### 4.1 $\sum a_n/n$ 与 $\sum a_n/n^p$

不能把它们当作“比较级数”直接处理，因为 $a_n$ 可能变号。正确思路是：

1. 已知 $\sum a_n$ 收敛；
2. 取 $b_n=1/n$ 或 $b_n=1/n^p$；
3. $b_n$ 单调且有界；
4. 由阿贝尔判别法，$\sum a_nb_n$ 收敛。

若题目改为 $\sum n a_n$，因 $n$ 无界，阿贝尔判别法失效，且新通项可能不趋于 $0$，所以不能推出收敛。

### 4.2 $\sum a_na_{n+1}$

普通收敛只给出 $a_n\to0$，不能给出 $\sum\lvert a_na_{n+1}\rvert$ 收敛。判断时分两种情况：

- 若只知 $\sum a_n$ 收敛：不能推出；
- 若知 $\sum\lvert a_n\rvert$ 收敛，或从某项起 $a_n\ge0$ 且 $\sum a_n$ 收敛：有
$$
\lvert a_na_{n+1}\rvert\le\lvert a_n\rvert
\quad\text{或}\quad
0\le a_na_{n+1}\le a_n,
$$
因此收敛。

### 4.3 奇项、偶项与两项分组

要区分：

$$
\sum(a_{2n-1}+a_{2n})
\quad\text{和}\quad
\sum a_{2n-1}+\sum a_{2n}.
$$
前者是相邻分组，原级数收敛即可；后者要求奇项级数和偶项级数分别存在，普通收敛通常不够。若原级数绝对收敛，二者都可以成立。

### 4.4 乘以 $a_n$ 的高次幂

若 $\sum a_n$ 收敛且从某项起 $a_n\ge0$，则对 $p>1$，
$$
0\le a_n^p\le a_n
$$
（充分大时 $a_n<1$），所以 $\sum a_n^p$ 收敛。

若 $a_n$ 可以变号，通常只能得到
$$
\sum a_n\text{ 收敛}\not\Rightarrow\sum |a_n|^p\text{ 收敛};
$$
必须另知绝对收敛，或直接比较具体的 $\lvert a_n\rvert^p$。

### 4.5 乘以振荡因子

形如
$$
\sum a_n(-1)^n,\qquad
\sum a_n\sin(n\theta),\qquad
\sum a_n\cos(n\theta)
$$
时，不能仅凭 $\lvert\sin(n\theta)\rvert\le1$ 判定，因为有界不等于单调。若 $a_n$ 本身构成收敛级数，可检查阿贝尔条件；若 $a_n$ 单调趋零且振荡因子的部分和有界，可检查狄利克雷条件。

## 5. 抽象题的操作顺序

1. 把“已知收敛”翻译为：$a_n\to0$、部分和有极限、尾和可控。
2. 先看新通项是否可拆成已知收敛级数的线性组合，或能否裂项。
3. 若通项非负，寻找比较不等式；若有绝对值，优先比较 $\sum|a_n|$。
4. 遇到“是否一定”而无法直接证明时，用三类典型对象试探：绝对收敛级数、条件收敛级数、调和级数。

---

# 五、幂级数：收敛半径、收敛域与性质

幂级数是
$$
\sum_{n=0}^{\infty}a_n(x-x_0)^n.
$$

它是把“级数敛散性”随自变量 $x$ 的取值变化来研究。中心 $x_0$、系数 $a_n$ 和端点行为是三件核心事。

## 1. 收敛半径与收敛区间

存在 $R\in[0,+\infty]$，使得：

$$
|x-x_0|<R\Rightarrow\text{绝对收敛};
$$
$$
|x-x_0|>R\Rightarrow\text{发散}.
$$

当 $|x-x_0|=R$ 时，不能由一般定理直接判定，两个端点必须分别代回原级数，按数项级数的方法判断。

因此最终收敛域的固定步骤是：

1. 先求 $R$；
2. 写开区间 $(x_0-R,x_0+R)$；
3. 分别代入 $x=x_0-R$ 与 $x=x_0+R$；
4. 用比较、交错、绝对收敛等方法判端点；
5. 写成区间，明确端点的开闭。

## 2. 收敛半径的常用公式

对 $a_n\ne0$，若相邻系数比值极限存在：
$$
\boxed{R=\lim_{n\to\infty}\left|\frac{a_n}{a_{n+1}}\right|.}
$$

更一般的柯西-阿达马公式：
$$
\boxed{\frac1R=\limsup_{n\to\infty}\sqrt[n]{|a_n|}.}
$$

实际做题常将
$$
u_n=a_n(x-x_0)^n
$$
代入比值法：
$$
\left|\frac{u_{n+1}}{u_n}\right|
=\left|\frac{a_{n+1}}{a_n}\right||x-x_0|.
$$
令该极限小于 $1$，即可得到 $|x-x_0|<R$。这比死记公式更稳，尤其适合含 $n!$、多项式或递推系数的题。

## 3. 端点判别：为什么不能省

半径只给出内部绝对收敛、外部发散；端点处比值法通常恰好等于 $1$，必须回到原级数。

常见结果包括：

- 两端都收敛；
- 两端都发散；
- 一端收敛、一端发散；
- 一端绝对收敛，另一端条件收敛。

例如端点代入后出现 $\sum1/n$，则发散；出现 $\sum(-1)^{n-1}/n$，则条件收敛；出现 $\sum1/n^p$ 且 $p>1$，则绝对收敛。

## 4. 幂级数的基本性质

在任意闭区间
$$
|x-x_0|\le r<R
$$
内，幂级数一致收敛，因此可逐项求导、逐项积分：

$$
\left(\sum_{n=0}^{\infty}a_n(x-x_0)^n\right)'
=\sum_{n=1}^{\infty}na_n(x-x_0)^{n-1},
$$

$$
\int\sum_{n=0}^{\infty}a_n(x-x_0)^n\,dx
=C+\sum_{n=0}^{\infty}\frac{a_n}{n+1}(x-x_0)^{n+1}.
$$

求导、积分后的幂级数收敛半径仍为 $R$，但端点收敛性可能改变，因此讨论端点时要重新检查。

### 幂级数的唯一性与系数

若两个幂级数在中心附近表示同一个函数，则同次幂的系数必相同。这是“比较系数法”的依据。

若函数能在 $x_0$ 邻域展开为幂级数，则
$$
a_n=\frac{f^{(n)}(x_0)}{n!}.
$$
这给出泰勒系数；但能写出全部导数不自动保证泰勒级数等于原函数，考研题一般会给出可展开条件或从已知幂级数出发变形。

### 幂级数的代数运算

在公共收敛区间内，幂级数可以逐项相加、相减和相乘。乘积满足柯西乘积公式：
$$
\left(\sum_{n=0}^{\infty}a_n(x-x_0)^n\right)
\left(\sum_{n=0}^{\infty}b_n(x-x_0)^n\right)
=
\sum_{n=0}^{\infty}
\left(\sum_{k=0}^{n}a_kb_{n-k}\right)(x-x_0)^n.
$$

用它求系数时，先明确目标幂次 $(x-x_0)^n$，再枚举下标满足 $k+(n-k)=n$ 的所有配对。

## 5. 幂级数解题思路

1. **求收敛半径**：对一般项做比值法；必要时用根值法或 $\limsup$ 公式。
2. **求收敛域**：半径后必须逐个检验两个端点。
3. **求和函数**：只在收敛域内，通过逐项求导、积分、乘以 $(x-x_0)$、换指标化到已知基础级数。
4. **求系数**：优先比较系数；若函数在 $x_0$ 可泰勒展开，再用 $a_n=f^{(n)}(x_0)/n!$。
5. **判断等式成立范围**：区分形式变形和收敛域内的函数等式；端点是否成立需单独确认。

---

# 六、傅里叶级数：系数、奇偶性与半区间展开

傅里叶级数研究周期函数。考试重点是：先确定周期和积分区间，再用奇偶性删去不必要的系数，最后根据题意选择原函数、奇延拓或偶延拓。

## 1. 周期为 $2\pi$ 的傅里叶级数

若 $f(x)$ 以 $2\pi$ 为周期，在 $[-\pi,\pi]$ 上满足常见的分段光滑条件，则其傅里叶级数写作
$$
f(x)\sim\frac{a_0}{2}+\sum_{n=1}^{\infty}\bigl(a_n\cos nx+b_n\sin nx\bigr),
$$
其中
$$
\boxed{a_0=\frac1\pi\int_{-\pi}^{\pi}f(x)\,dx,}
$$
$$
\boxed{a_n=\frac1\pi\int_{-\pi}^{\pi}f(x)\cos nx\,dx,\qquad
b_n=\frac1\pi\int_{-\pi}^{\pi}f(x)\sin nx\,dx.}
$$

这里 $\sim$ 表示函数可由该三角级数展开；在连续点，级数和等于函数值。不要把 $a_0/2$ 误写为 $a_0$。

## 2. 周期为 $2l$ 的公式

若周期为 $2l$，在 $[-l,l]$ 上展开：
$$
f(x)\sim\frac{a_0}{2}
+\sum_{n=1}^{\infty}
\left[
a_n\cos\frac{n\pi x}{l}
+b_n\sin\frac{n\pi x}{l}
\right],
$$
$$
\boxed{a_0=\frac1l\int_{-l}^{l}f(x)\,dx,}
$$
$$
\boxed{
a_n=\frac1l\int_{-l}^{l}f(x)\cos\frac{n\pi x}{l}\,dx,\qquad
b_n=\frac1l\int_{-l}^{l}f(x)\sin\frac{n\pi x}{l}\,dx.}
$$

> 先看题目给的是周期 $2\pi$ 还是 $2l$。周期变化时，三角函数中的角频率必须变成 $n\pi/l$，系数前的 $1/\pi$ 也必须变成 $1/l$。

## 3. 奇偶性：最快的化简

若 $f(-x)=f(x)$，即 $f$ 为偶函数，则
$$
b_n=0,
$$
傅里叶级数只含常数项和余弦项。

若 $f(-x)=-f(x)$，即 $f$ 为奇函数，则
$$
a_0=0,\qquad a_n=0,
$$
傅里叶级数只含正弦项。

利用
$$
\int_{-l}^{l}g(x)\,dx=
\begin{cases}
2\int_0^lg(x)\,dx,&g(x)\text{ 为偶函数},\\
0,&g(x)\text{ 为奇函数},
\end{cases}
$$
可把积分区间缩到 $[0,l]$。

判断时要看**整个被积函数**的奇偶性：

- $f(x)$ 偶时，$f(x)\cos(n\pi x/l)$ 为偶函数，$f(x)\sin(n\pi x/l)$ 为奇函数；
- $f(x)$ 奇时，结论相反。

### 正交性与 Parseval 等式（补充）

三角函数系数公式来自正交性：在 $[-l,l]$ 上，不同频率的正弦、余弦积分为零；同频率平方的积分为 $l$。计算时不必重新证明，但要理解它保证了每一个 $a_n,b_n$ 可以被单独“筛出”。

若
$$
f(x)\sim\frac{a_0}{2}+
\sum_{n=1}^{\infty}
\left(
a_n\cos\frac{n\pi x}{l}
+b_n\sin\frac{n\pi x}{l}
\right),
$$
则 Parseval 等式为
$$
\boxed{
\frac1l\int_{-l}^{l}[f(x)]^2\,dx
=\frac{a_0^2}{2}+\sum_{n=1}^{\infty}(a_n^2+b_n^2).}
$$

它常用于由已求出的傅里叶系数反求某些数项级数的和。若函数是奇函数，只剩 $b_n^2$；若函数是偶函数，只剩 $a_n^2$。

## 4. 半区间展开：奇延拓与偶延拓

若只给出 $0<x<l$ 上的 $f(x)$，它本身还不是定义在 $[-l,l]$ 上的周期函数。必须先按题意向左延拓，再作周期为 $2l$ 的延拓。

### 4.1 正弦级数：奇延拓

定义
$$
F(x)=
\begin{cases}
f(x),&0<x<l,\\
-f(-x),&-l<x<0.
\end{cases}
$$
则 $F$ 为奇函数，傅里叶级数只剩正弦项：
$$
\boxed{f(x)\sim\sum_{n=1}^{\infty}b_n\sin\frac{n\pi x}{l},\qquad
b_n=\frac2l\int_0^lf(x)\sin\frac{n\pi x}{l}\,dx.}
$$

题目出现“展开为正弦级数”时，默认做奇延拓；不是因为原函数在 $(0,l)$ 上一定是奇函数。

### 4.2 余弦级数：偶延拓

定义
$$
F(x)=
\begin{cases}
f(x),&0<x<l,\\
f(-x),&-l<x<0.
\end{cases}
$$
则 $F$ 为偶函数，傅里叶级数只剩余弦项：
$$
\boxed{
f(x)\sim\frac{a_0}{2}+
\sum_{n=1}^{\infty}a_n\cos\frac{n\pi x}{l},}
$$
$$
\boxed{
a_0=\frac2l\int_0^lf(x)\,dx,\qquad
a_n=\frac2l\int_0^lf(x)\cos\frac{n\pi x}{l}\,dx.}
$$

题目出现“展开为余弦级数”时，默认做偶延拓。

## 5. 级数和取什么值

对分段光滑的周期延拓函数，傅里叶级数在 $x$ 处收敛到
$$
\boxed{\frac{F(x-0)+F(x+0)}2.}
$$

- 在连续点，和为 $F(x)$；
- 在跳跃点，和为左右极限的平均值；
- 半区间正弦/余弦展开考查 $x=0,l$ 时，必须看对应的奇延拓或偶延拓后的左右极限，不能只代原来的 $f(x)$。

## 6. 傅里叶题的做题顺序

1. 确认区间和周期：$[-\pi,\pi]$ 对应 $2\pi$；$[-l,l]$ 对应 $2l$。
2. 若函数已定义在对称区间，先查奇偶性，决定是否只算 $a_n$ 或 $b_n$。
3. 若仅定义在 $(0,l)$，看题目要求正弦级数还是余弦级数，分别做奇延拓或偶延拓。
4. 写对系数公式后再计算积分，避免先算后发现周期系数写错。
5. 若问级数在端点或间断点的和，使用左右极限平均值。

---

# 七、总复习：判别方法怎么选

1. **先检查通项极限。** $\lim a_n\ne0$，立即发散。
2. **通项非负。** 想比较、比值、根值、积分。
3. **交替正负。** 先查绝对收敛；不绝对收敛再看莱布尼茨或相消结构。
4. **已知某级数的敛散性。** 这是抽象题，优先用线性性、绝对值、比较和部分和；结论不充分时找反例。
5. **含 $(x-x_0)^n$。** 这是幂级数：先半径，再端点，最后才写收敛域。

## 高频误区

1. $a_n\to0$ 只是级数收敛的必要条件，不是充分条件。
2. 比值/根值判别的极限等于 $1$ 时，判别法失效，不能直接判定发散。
3. 变号级数不能直接用正项级数的极限比较；通常先考察绝对值级数。
4. 幂级数求出 $R$ 后不能停止，两个端点必须分别代回原级数。
5. 原级数收敛不能推出任意子级数收敛；条件收敛时尤其危险。
6. 半区间正弦/余弦级数不能直接套全区间公式，必须先按题意做奇延拓或偶延拓。

> 记忆主线：正项看比较，变号先绝对；交错查单调趋零；幂级数先半径、后端点；傅里叶先看周期与对称/延拓。
