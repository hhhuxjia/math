# 考研数学：积分压轴题专题

> 本专题面向冲击高分（130+/140+）的考研数学考生，重点梳理积分中值定理、积分等式与积分不等式的核心题型与解题方法。
>
> 题型特点：综合性强、技巧性高、常作为压轴题出现。

---

## 1. 积分中值定理

### 1.1 基本定理回顾

#### 定理一（积分第一中值定理）
设 $f(x)$ 在 $[a,b]$ 上连续，则存在 $\xi \in [a,b]$，使得

$$
\int_a^b f(x) \, dx = f(\xi) (b-a)
$$

#### 定理二（推广的积分第一中值定理）
设 $f(x), g(x)$ 在 $[a,b]$ 上连续，$g(x)$ 在 $[a,b]$ 上不变号，则存在 $\xi \in [a,b]$，使得

$$
\int_a^b f(x) g(x) \, dx = f(\xi) \int_a^b g(x) \, dx
$$

#### 定理三（积分第二中值定理）
设 $f(x)$ 在 $[a,b]$ 上单调，$g(x)$ 在 $[a,b]$ 上可积，则存在 $\xi \in [a,b]$，使得

$$
\int_a^b f(x) g(x) \, dx = f(a) \int_a^\xi g(x) \, dx + f(b) \int_\xi^b g(x) \, dx
$$

### 1.2 核心题型与技巧

#### 题型一：证明存在 $\xi$ 满足某个等式

**方法**：构造辅助函数，利用罗尔定理或拉格朗日中值定理。

**例 1.1** 设 $f(x)$ 在 $[0,1]$ 上连续，且 $\int_0^1 f(x) \, dx = 0$。证明：存在 $\xi \in (0,1)$，使得 $\int_0^\xi f(x) \, dx = \xi f(\xi)$。

> **分析**：令 $F(x) = e^{-x} \int_0^x f(t) \, dt$，则 $F(0) = F(1) = 0$，由罗尔定理得证。

---

#### 题型二：含 $f(\xi)$ 与 $\int f$ 的双中值问题

**方法**：构造辅助函数 $F(x) = \int_a^x f(t) \, dt$，利用柯西中值定理。

**例 1.2** 设 $f(x)$ 在 $[a,b]$ 上可导，且 $f(a) = f(b) = 0$。证明：存在 $\xi \in (a,b)$，使得 $f(\xi) = \frac{1}{b-a} \int_a^b f(x) \, dx$。

---

#### 题型三：端点含参的积分中值

**例 1.3** 设 $f(x)$ 在 $[0,1]$ 上连续，且 $f(0) > 0$，$f(1) < 1$。证明：存在 $\xi \in (0,1)$，使得 $f(\xi) = \int_0^\xi f(t) \, dt$。

> **思路**：构造 $F(x) = e^{-x} \int_0^x f(t) \, dt$ 或其变体。

---

## 2. 积分等式

### 2.1 换元法构造等式

**核心思想**：通过变量替换 $x = \phi(t)$，将已知积分转化为目标形式。

**常用代换**：

| 条件 | 可选代换 |
|------|----------|
| 积分区间关于 $0$ 对称 | $x = -t$ |
| 积分区间为 $[0,a]$ | $x = a - t$ |
| 含 $\sin x, \cos x$ 的 $[0,\pi/2]$ | $x = \frac{\pi}{2} - t$ |
| 含 $\sin x$ 的 $[0,\pi]$ | $x = \pi - t$ |
| 含 $e^x$、$a^x$ | $x = \ln t$ 或 $t = a^x$ |
| 分母含 $1+e^x$ 型 | $x = -t$（对称区间） |

**例 2.1** 证明：

$$
\int_0^{\pi/2} \frac{\sin x}{\sin x + \cos x} \, dx = \frac{\pi}{4}
$$

**例 2.2** 计算 $\int_{-\pi/2}^{\pi/2} \frac{\sin^3 x}{1 + e^x} \, dx$。

> **技巧**：令 $x = -t$，与原式相加消去分母中的 $e^x$。

**例 2.3** 证明：

$$
\int_0^1 x^m (1-x)^n \, dx = \int_0^1 x^n (1-x)^m \, dx
$$

**例 2.4** 设 $f$ 为偶函数，$g$ 为奇函数，且都在 $[-a,a]$ 上连续，证明：

$$
\int_{-a}^a \frac{f(x)}{1 + e^{g(x)}} \, dx = \int_0^a f(x) \, dx
$$

---

### 2.2 分部积分法构造等式

**核心公式**：

$$
\int_a^b u \, dv = uv\Big|_a^b - \int_a^b v \, du
$$

**常用模式**：

1. $\int_a^b f(x) \, dx$ 中令 $u = f(x), dv = dx$
2. $\int_a^b x^n f(x) \, dx$，反复分部积分
3. $\int_a^b f''(x)g(x) \, dx$ 配合边界条件

**例 2.5** 设 $f(x)$ 在 $[0,1]$ 上有二阶连续导数，$f(0) = f(1) = 0$。证明：

$$
\int_0^1 f(x) \, dx = -\frac{1}{2} \int_0^1 x(1-x) f''(x) \, dx
$$

> **证明**：分部积分两次即可得。

**例 2.6** 证明定积分等式（华里士公式变体）：

$$
\int_0^{\pi/2} \sin^{2n} x \, dx = \frac{(2n-1)!!}{(2n)!!} \cdot \frac{\pi}{2}
$$

---

### 2.3 巧用对称性

| 对称类型 | 结论 |
|----------|------|
| $f$ 为偶函数 | $\int_{-a}^a f(x) \, dx = 2 \int_0^a f(x) \, dx$ |
| $f$ 为奇函数 | $\int_{-a}^a f(x) \, dx = 0$ |
| $f(a-x) = f(x)$ | $\int_0^a f(x) \, dx = \frac{a}{2} \int_0^a f(x) + f(a-x) \, dx$ |
| $f(a-x) = -f(x)$ | $\int_0^a f(x) \, dx = 0$ |

**例 2.7** 计算 $\int_0^\pi x \sin^4 x \, dx$。

> **技巧**：令 $x = \pi - t$，与原积分相加。

---

## 3. 积分不等式

### 3.1 利用单调性与最值

**方法**：直接比较被积函数大小，或构造辅助函数求单调性。

**例 3.1** 证明：当 $0 < x < \frac{\pi}{2}$ 时，有：

$$
\frac{2}{\pi} x < \sin x < x
$$

> **证明**：令 $F(x) = \frac{\sin x}{x}$ 或 $G(x) = \sin x - \frac{2}{\pi} x$ 判单调性。

**例 3.2** 比较 $\int_0^1 e^x \, dx$ 与 $\int_0^1 e^{x^2} \, dx$ 的大小。

---

### 3.2 柯西-施瓦茨不等式（积分形式）

**定理**：设 $f(x), g(x)$ 在 $[a,b]$ 上连续，则

$$
\left( \int_a^b f(x)g(x) \, dx \right)^2 \le \int_a^b f^2(x) \, dx \cdot \int_a^b g^2(x) \, dx
$$

等号成立当且仅当 $f(x)$ 与 $g(x)$ 线性相关。

**例 3.3** 设 $f(x)$ 在 $[0,1]$ 上连续，证明：

$$
\left( \int_0^1 f(x) \, dx \right)^2 \le \int_0^1 f^2(x) \, dx
$$

> **证明**：取 $g(x) \equiv 1$，直接应用柯西-施瓦茨。

**例 3.4** 设 $f(x)$ 在 $[a,b]$ 上有连续导数，$f(a) = 0$，证明：

$$
\int_a^b f^2(x) \, dx \le \frac{(b-a)^2}{2} \int_a^b [f'(x)]^2 \, dx
$$

> **证明**：
>
> **Step 1**：由 $f(a)=0$，用牛顿-莱布尼茨公式将 $f(x)$ 用导数表示：
> $$f(x) = \int_a^x f'(t) \, dt$$
>
> **Step 2**：对 $\int_a^x 1 \cdot f'(t)\,dt$ 用柯西-施瓦茨不等式：
> $$\begin{aligned}
> [f(x)]^2 &= \left( \int_a^x 1 \cdot f'(t) \, dt \right)^2 \\
> &\le \int_a^x 1^2 \, dt \cdot \int_a^x [f'(t)]^2 \, dt \\
> &= (x-a) \int_a^x [f'(t)]^2 \, dt
> \end{aligned}$$
>
> **Step 3**：两边对 $x$ 从 $a$ 到 $b$ 积分：
> $$\int_a^b f^2(x) \, dx \le \int_a^b (x-a) \left( \int_a^x [f'(t)]^2 \, dt \right) dx$$
>
> **Step 4**：交换积分次序。积分区域为 $a \le t \le x \le b$，交换后先固定 $t$，$x$ 从 $t$ 到 $b$：
> $$\begin{aligned}
> \int_a^b f^2(x) \, dx &\le \int_a^b [f'(t)]^2 \left( \int_t^b (x-a) \, dx \right) dt \\
> &= \int_a^b [f'(t)]^2 \cdot \frac{1}{2}(b-t)^2 \, dt
> \end{aligned}$$
>
> **Step 5**：因为 $t \in [a,b]$ 时 $(b-t)^2 \le (b-a)^2$，所以：
> $$\int_a^b f^2(x) \, dx \le \frac{(b-a)^2}{2} \int_a^b [f'(t)]^2 \, dt$$
>
> $\square$
>
> **思路拆解**：这个证明的核心套路是"**$f(a)=0$ → 柯西-施瓦茨 → 交换积分次序**"三步走。其中交换积分次序是很多同学想不到的关键步骤——它将一个混合了 $f^2$ 和 $(f')^2$ 的二重积分，转化成了纯粹的 $(f')^2$ 积分乘上一个系数，从而得证。

---

### 3.3 泰勒公式与积分余项

**方法**：将被积函数展开至适当阶数，利用余项估计。

**例 3.5** 设 $f(x)$ 在 $[0,1]$ 上有二阶连续导数，$f(0) = f(1) = 0$。证明：

$$
\left| \int_0^1 f(x) \, dx \right| \le \frac{1}{12} \max_{0 \le x \le 1} |f''(x)|
$$

**例 3.6** 设 $f(x)$ 在 $[a,b]$ 上有二阶导数，$f'(a) = f'(b) = 0$，证明：

$$
\left| \int_a^b f(x) \, dx - \frac{b-a}{2}[f(a) + f(b)] \right| \le \frac{(b-a)^3}{12} \max_{a \le x \le b} |f''(x)|
$$

> 此为梯形公式的误差估计。

**例 3.7** 证明贝塞尔不等式的一种积分形式：若 $f(x)$ 在 $[-\pi,\pi]$ 上可积且平方可积，则

$$
\frac{1}{\pi} \int_{-\pi}^\pi f^2(x) \, dx \ge \frac{a_0^2}{2} + \sum_{n=1}^\infty (a_n^2 + b_n^2)
$$

其中 $a_n, b_n$ 为傅里叶系数。

---

### 3.4 詹森不等式（积分形式）

**定理**：设 $\varphi$ 为 $[a,b]$ 上的凸函数，$p(x) \ge 0$，则

$$
\varphi\left( \frac{\int_a^b f(x) p(x) \, dx}{\int_a^b p(x) \, dx} \right) \le \frac{\int_a^b \varphi(f(x)) p(x) \, dx}{\int_a^b p(x) \, dx}
$$

当 $\varphi$ 为凹函数时不等号反向。

**例 3.8** 证明：若 $f(x) > 0$，则

$$
\ln\left( \frac{1}{b-a} \int_a^b f(x) \, dx \right) \ge \frac{1}{b-a} \int_a^b \ln f(x) \, dx
$$

---

### 3.5 利用积分中值定理

**方法**：当要证明的不等式形如 $m \le \int_a^b f(x) g(x) \, dx \le M$ 时，可利用推广的积分第一中值定理。

**例 3.9** 证明：

$$
\frac{2}{5} \le \int_0^1 \frac{1}{1 + x^3} \, dx \le \frac{5}{6}
$$

> **思路**：利用 $1 + x^3$ 在 $[0,1]$ 上的范围。

**例 3.10** 估计积分 $\int_0^1 \frac{\sin x}{1 + x^2} \, dx$ 的范围。

---

## 4. 综合压轴题精选

### 4.1 构造辅助函数类

**例 4.1** 设 $f(x)$ 在 $[a,b]$ 上连续，且 $\int_a^b f(x) \, dx = 0$。证明：
存在 $\xi \in (a,b)$，使得 $\int_a^\xi f(x) \, dx = \int_\xi^b f(x) \, dx$。

**例 4.2** 设 $f(x)$ 在 $[0,1]$ 上连续可导，$f(0) = 0$。证明：

$$
\int_0^1 |f(x) f'(x)| \, dx \le \frac{1}{2} \int_0^1 [f'(x)]^2 \, dx
$$

> **提示**：使用柯西-施瓦茨和牛顿-莱布尼茨公式。

**例 4.3** 设 $f(x)$ 在 $[0,+\infty)$ 上连续，$\int_0^{+\infty} f(x) \, dx$ 收敛，且 $f(x)$ 单调递减。证明：

$$
\lim_{x \to +\infty} x f(x) = 0
$$

---

### 4.2 双变量中值问题

**例 4.4** 设 $f(x), g(x)$ 在 $[a,b]$ 上连续，$g(x) > 0$。证明：存在 $\xi \in (a,b)$，使得

$$
\frac{\int_a^b f(x) g(x) \, dx}{\int_a^b g(x) \, dx} = f(\xi)
$$

**例 4.5** 设 $f(x)$ 在 $[0,1]$ 上有连续导数，$f(0) = f(1) = 0$。证明：存在 $\xi \in (0,1)$，使得

$$
\int_0^1 f(x) \, dx = \frac{f'(\xi)}{6}
$$

---

### 4.3 周期性积分

**例 4.6** 设 $f(x)$ 是以 $T$ 为周期的连续函数。证明：

$$
\lim_{n \to \infty} \int_0^T f(x) |\sin nx| \, dx = \frac{2}{\pi} \int_0^T f(x) \, dx
$$

**例 4.7** 计算 $\lim_{n \to \infty} \int_0^1 \frac{nx}{1 + n^2 x^2} f(x) \, dx$，其中 $f(x)$ 在 $[0,1]$ 上连续。

---

### 4.4 含参变量积分

**例 4.8** 设 $F(t) = \int_0^t \sin(x^2) \, dx$，证明 $F(t)$ 在 $(0,+\infty)$ 上有界。

**例 4.9** 研究函数 $F(x) = \int_0^x e^{t^2} \, dt$ 的渐近行为。

---

## 5. 常用技巧汇编

### 5.1 "加一项减一项"法

$$
\int_a^b f(x) \, dx = \int_a^b [f(x) - g(x)] \, dx + \int_a^b g(x) \, dx
$$

常用于将复杂积分拆分为容易处理的已知积分与一个小量之和。

### 5.2 积分号下求导（莱布尼茨法则）

设 $F(x) = \int_{a(x)}^{b(x)} f(x,t) \, dt$，则

$$
F'(x) = f(x, b(x)) b'(x) - f(x, a(x)) a'(x) + \int_{a(x)}^{b(x)} \frac{\partial f}{\partial x}(x,t) \, dt
$$

### 5.3 积分放缩常用不等式

| 不等式 | 使用场景 |
|--------|----------|
| $\sin x \le x \le \tan x$（$0 \le x < \frac{\pi}{2}$） | 三角函数积分 |
| $1 - \frac{x^2}{2} \le \cos x \le 1$ | 余弦积分 |
| $e^x \ge 1 + x$ | 指数积分 |
| $\ln(1+x) \le x$ | 对数积分 |
| $\frac{x}{1+x} \le \ln(1+x) \le x$（$x > -1$） | 对数积分精细估计 |
| $|ab| \le \frac{a^2 + b^2}{2}$ | 乘积放缩 |

### 5.4 分数次与无理数积分换元

| 被积函数形式 | 换元方案 |
|-------------|----------|
| $\sqrt{a^2 - x^2}$ | $x = a\sin t$ |
| $\sqrt{a^2 + x^2}$ | $x = a\tan t$ |
| $\sqrt{x^2 - a^2}$ | $x = a\sec t$ |
| $\sqrt[3]{x}$、$\sqrt{x}$ 混合 | $x = t^6$（取最小公倍数） |
| $\sqrt{ax+b}$ 线性 | $t = \sqrt{ax+b}$ |

---

## 6. 专项练习题

### 基础巩固

1. 证明：$\int_0^1 \frac{dx}{1 + e^x} = \ln \frac{2e}{1+e}$。

2. 计算：$\int_{-\pi}^{\pi} \frac{x \sin x}{1 + \cos^2 x} \, dx$。

3. 证明：$\int_0^\pi x f(\sin x) \, dx = \frac{\pi}{2} \int_0^\pi f(\sin x) \, dx$。

4. 证明：$\left| \int_0^1 \frac{\sin x}{1 + x} \, dx \right| \le \ln 2$。

### 进阶训练

5. 设 $f(x)$ 在 $[0,1]$ 上连续，且 $f(x) \ge 0$，$\int_0^1 f(x) \, dx = 1$。证明：存在 $\xi \in (0,1)$，使得 $f(\xi) = \xi$。

6. 设 $f(x)$ 在 $[a,b]$ 上连续且单调递增。证明：

$$
\int_a^b x f(x) \, dx \ge \frac{a+b}{2} \int_a^b f(x) \, dx
$$

7. 设 $f(x)$ 在 $[0,1]$ 上有二阶连续导数，$f(0) = f(1) = 0$，且 $\max_{[0,1]} |f''(x)| \le M$。证明：

$$
\left| \int_0^1 f(x) \, dx \right| \le \frac{M}{12}
$$

8. 设 $f(x)$ 在 $[0,1]$ 上可导，$f(0) = 0$，$0 \le f'(x) \le 1$。证明：

$$
\left( \int_0^1 f(x) \, dx \right)^2 \ge \int_0^1 f^3(x) \, dx
$$

### 挑战压轴

9. （2023考研真题改编）设 $f(x)$ 在 $[0,1]$ 上可导，$f(0) = 0$，$f(1) = 1$。证明：存在 $\xi \in (0,1)$，使得

$$
\int_0^1 |f(x) - x| \, dx \le \frac{1}{4}
$$

10. 设 $f(x)$ 在 $[0,1]$ 上有连续导数，$f(0) = 0$。证明：

$$
\int_0^1 |f(x) f'(x)| \, dx \le \frac{1}{2} \int_0^1 [f'(x)]^2 \, dx
$$

11. 证明：$\int_0^1 \frac{dx}{\sqrt{1-x^4}} = \frac{1}{4} \int_0^1 \frac{dx}{\sqrt[4]{x(1-x)^3}}$。

12. 设 $f(x)$ 在 $[a,b]$ 上有连续导数，$f(a) = f(b) = 0$。证明：

$$
\max_{a \le x \le b} |f(x)| \le \frac{\sqrt{b-a}}{2} \sqrt{\int_a^b [f'(x)]^2 \, dx}
$$

---

## 7. 知识框架图

```
积分中值定理与不等式 ────────────────── 积分中值定理
│                                       ├── 第一中值定理
│                                       ├── 推广的第一中值定理
│                                       ├── 第二中值定理
│                                       └── 双中值问题（柯西中值定理）
│
├── 积分等式
│   ├── 换元法
│   │   ├── 对称区间代换 x = -t
│   │   ├── 互补区间代换 x = a - t
│   │   └── 三角代换
│   ├── 分部积分法
│   │   ├── 直接分部
│   │   └── 多次分部（降阶）
│   └── 对称性利用
│
├── 积分不等式
│   ├── 单调性与最值法
│   ├── 柯西-施瓦茨不等式
│   ├── 泰勒展开法
│   ├── 詹森不等式
│   └── 积分中值定理法
│
└── 综合技巧
    ├── 辅助函数构造
    ├── 加一项减一项
    ├── 积分号下求导
    └── 积分放缩
```

---

> **复习建议**：本章节应重点掌握辅助函数构造法（用于中值定理证明题）、换元与对称性技巧（用于等式计算）、柯西-施瓦茨与泰勒展开（用于不等式证明）。建议在完成高数一轮复习后，集中使用本专题进行拔高训练。
