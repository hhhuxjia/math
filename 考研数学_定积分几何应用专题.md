# 考研数学一 · 定积分几何应用专题整理

---

# 一、平面图形面积

## 1. 直角坐标系

- 曲线 $y = f(x) \ge 0$，$x \in [a,b]$：
  $$
  A = \int_a^b f(x)\,dx
  $$

- 两条曲线 $y = f(x)$（上）与 $y = g(x)$（下），$f(x) \ge g(x)$：
  $$
  A = \int_a^b [f(x) - g(x)]\,dx
  $$

- 对 $y$ 积分（方便处理 $x = \varphi(y)$ 形式）：  
  曲线 $x = \varphi(y)$，$y \in [c,d]$：
  $$
  A = \int_c^d \varphi(y)\,dy
  $$

## 2. 参数方程

曲线 $\begin{cases}x = x(t)\\ y = y(t)\end{cases}$，$t \in [\alpha,\beta]$，$y(t) \ge 0$，$x'(t) \ge 0$：
$$
A = \int_{\alpha}^{\beta} y(t) \, x'(t) \, dt
$$
> 若 $x'(t) < 0$，则加绝对值，公式变为 $A = \int_{\alpha}^{\beta} y(t) |x'(t)| \, dt$

## 3. 极坐标

曲线 $r = r(\theta)$，$\theta \in [\alpha,\beta]$：
$$
A = \frac12 \int_{\alpha}^{\beta} r^2(\theta) \, d\theta
$$

> 对于封闭图形要特别留意 $\alpha,\beta$ 的取法（通常取一个完整周期或两相邻交点间的角度）

---

# 二、旋转体体积

## 1. 圆盘法（绕 $x$ 轴）

曲线 $y = f(x) \ge 0$，$x \in [a,b]$，绕 $x$ 轴旋转：
$$
V = \pi \int_a^b [f(x)]^2 \, dx
$$

## 2. 柱壳法（绕 $y$ 轴）

曲线 $y = f(x) \ge 0$，$x \in [a,b]$，绕 $y$ 轴旋转：
$$
V = 2\pi \int_a^b x \, f(x) \, dx
$$

## 3. 绕水平线 $y = c$

曲线 $y = f(x)$，$x \in [a,b]$，绕 $y = c$：
$$
V = \pi \int_a^b [f(x) - c]^2 \, dx
$$
> 需保证 $f(x) - c$ 不变号（一般取上方曲线减去 $c$）

## 4. 绕竖直线 $x = c$

曲线 $y = f(x)$，$x \in [a,b]$，绕 $x = c$（用柱壳法）：
$$
V = 2\pi \int_a^b |x - c| \, f(x) \, dx
$$
> 若 $x \ge c$ 则绝对值可去掉，公式为 $2\pi \int_a^b (x - c) f(x) dx$

## 5. 垫圈法（两曲线绕 $x$ 轴）

上曲线 $y = f(x)$，下曲线 $y = g(x)$，$f(x) \ge g(x) \ge 0$，绕 $x$ 轴：
$$
V = \pi \int_a^b [f^2(x) - g^2(x)] \, dx
$$

## 6. 武忠祥二重积分万能法（绕任意直线旋转）⭐核心秒杀

### 公式

平面区域 $D$ 绕**不穿过 $D$** 的任意直线 $L$ 旋转一周所得旋转体体积：

$$
\boxed{V = 2\pi \iint_D r(x,y)\,d\sigma}
$$

其中 $r(x,y)$ 是 $D$ 内点 $(x,y)$ 到直线 $L$ 的距离。

### 关键：点到直线距离公式

若 $L: Ax + By + C = 0$，则：

$$
r(x,y) = \frac{|Ax + By + C|}{\sqrt{A^2 + B^2}}
$$

> 💡 选择 $L$ 的某一侧区域，绝对值可按区域内的符号直接去掉。

### 为什么是万能？（与圆盘法、柱壳法统一）

- **绕 $x$ 轴**：$r = y$，则 $V = 2\pi\iint_D y\,d\sigma = 2\pi\int_a^b\int_0^{f(x)} y\,dy\,dx = \pi\int_a^b f^2(x)\,dx$（推出**圆盘法**）
- **绕 $y$ 轴**：$r = x$，则 $V = 2\pi\iint_D x\,d\sigma = 2\pi\int_a^b x\,f(x)\,dx$（推出**柱壳法**）

### 适用范围

绕任意斜线（如 $y = x$、$y = x + 1$）时，传统圆盘/柱壳法需要旋转变换，而此法直接算距离，**一法通杀**。

### 例题（武忠祥经典）

**例**：求曲线 $y = x^2$ 与 $y = x$ 所围成的平面图形绕直线 $y = x$ 旋转一周所得旋转体的体积。

**解**：

区域 $D: 0 \le x \le 1,\ x^2 \le y \le x$（位于直线 $y = x$ 下方）

**Step 1**：直线 $L: x - y = 0$，点到直线距离：
$$
r = \frac{|x-y|}{\sqrt{2}} = \frac{x-y}{\sqrt{2}}
$$
（区域内 $x \ge y$，绝对值可去掉）

**Step 2**：由武忠祥公式：
$$
V = 2\pi \iint_D r\,d\sigma = \frac{2\pi}{\sqrt{2}}\int_0^1\int_{x^2}^x (x-y)\,dy\,dx
$$

**Step 3**：计算：
$$
\int_{x^2}^x (x-y)\,dy = \left[xy - \frac{y^2}{2}\right]_{x^2}^x = \frac{x^2}{2} - x^3 + \frac{x^4}{2}
$$
$$
V = \sqrt{2}\pi \int_0^1 \left(\frac{x^2}{2} - x^3 + \frac{x^4}{2}\right)dx = \sqrt{2}\pi\left(\frac16 - \frac14 + \frac1{10}\right) = \boxed{\frac{\sqrt{2}\pi}{60}}
$$

> ⚠️ 传统方法做此题：需将坐标系旋转 45° 或用垂直于 $y=x$ 的截面法，计算量巨大；用二重积分法只需算一次点到直线距离，效率极高！

---

## 7. 已知平行截面面积的体积

垂直于 $x$ 轴的截面面积 $A(x)$，$x \in [a,b]$：
$$
V = \int_a^b A(x) \, dx
$$

---

# 三、平面曲线弧长

## 1. 直角坐标

$y = f(x)$，$x \in [a,b]$：
$$
L = \int_a^b \sqrt{1 + [f'(x)]^2} \, dx
$$

## 2. 参数方程

$\begin{cases}x = x(t)\\ y = y(t)\end{cases}$，$t \in [\alpha,\beta]$：
$$
L = \int_{\alpha}^{\beta} \sqrt{[x'(t)]^2 + [y'(t)]^2} \, dt
$$

## 3. 极坐标

$r = r(\theta)$，$\theta \in [\alpha,\beta]$：
$$
L = \int_{\alpha}^{\beta} \sqrt{r^2 + \left(\frac{dr}{d\theta}\right)^2} \, d\theta
$$

---

# 四、旋转曲面侧面积

曲线绕 $x$ 轴旋转一周所得侧面积：

## 1. 直角坐标
$$
S = 2\pi \int_a^b f(x) \sqrt{1 + [f'(x)]^2} \, dx
$$

## 2. 参数方程
$$
S = 2\pi \int_{\alpha}^{\beta} y(t) \sqrt{[x'(t)]^2 + [y'(t)]^2} \, dt
$$

## 3. 极坐标
$$
S = 2\pi \int_{\alpha}^{\beta} r(\theta)\sin\theta \sqrt{r^2 + \left(\frac{dr}{d\theta}\right)^2} \, d\theta
$$

---

# 五、形心（质心）公式

设平面图形由曲线 $y = f(x) \ge 0$、$x=a$、$x=b$ 及 $x$ 轴围成，面积 $A$。

**形心坐标 $(\bar{x}, \bar{y})$：**
$$
\bar{x} = \frac{1}{A} \int_a^b x \, f(x) \, dx,\qquad
\bar{y} = \frac{1}{A} \int_a^b \frac12 [f(x)]^2 \, dx
$$

## 参数方程形式

由参数曲线 $\begin{cases}x = x(t)\\ y = y(t)\end{cases}$ 与 $x$ 轴围成的图形（$t$ 从 $\alpha$ 到 $\beta$，且 $y(t) \ge 0$）：
$$
\bar{x} = \frac{\int_{\alpha}^{\beta} x(t) \, y(t) \, x'(t) \, dt}{\int_{\alpha}^{\beta} y(t) \, x'(t) \, dt},\qquad
\bar{y} = \frac{\frac12\int_{\alpha}^{\beta} [y(t)]^2 \, x'(t) \, dt}{\int_{\alpha}^{\beta} y(t) \, x'(t) \, dt}
$$

> 分母即为面积 $A$。

---

# 六、旋转体侧面积与体积的极坐标推广

若曲线用极坐标 $r = r(\theta)$，$\theta \in [\alpha,\beta]$，绕极轴（$x$ 轴）旋转：

- 体积：$V = \frac{2\pi}{3} \int_{\alpha}^{\beta} r^3(\theta) \sin\theta \, d\theta$（常用公式，推导自柱壳法）
- 侧面积：$S = 2\pi \int_{\alpha}^{\beta} r(\theta)\sin\theta \sqrt{r^2 + (r')^2} \, d\theta$

---

# 七、古尔丁定理（帕普斯质心定理）

1. 平面图形绕与其不相交的轴旋转所得**体积**等于图形面积乘以形心所经路程（$2\pi \bar{y}$ 绕 $x$ 轴）：
   $$
   V = A \cdot (2\pi \cdot \bar{y})
   $$

2. 平面曲线绕与其不相交的轴旋转所得**侧面积**等于曲线弧长乘以形心所经路程：
   $$
   S = L \cdot (2\pi \cdot \bar{y})
   $$

> 用于快速求形心或验证结果。

---

# 八、常见曲线图形面积弧长速查

| 曲线名称 | 方程 | 面积（封闭区域） | 弧长（一个周期/全长） |
|:---|:---|:---|:---|
| 圆 | $x^2+y^2=R^2$ | $\pi R^2$ | $2\pi R$ |
| 椭圆 | $\frac{x^2}{a^2}+\frac{y^2}{b^2}=1$ | $\pi ab$ | 无初等表达式（椭圆积分） |
| 心形线 | $r=a(1+\cos\theta)$ | $\frac{3}{2}\pi a^2$ | $8a$ |
| 双纽线 | $r^2=a^2\cos2\theta$ | $a^2$ | $2a\int_0^{\pi/4}\frac{d\theta}{\sqrt{\cos2\theta}}$（椭圆积分） |
| 摆线一拱 | $\begin{cases}x=a(t-\sin t)\\ y=a(1-\cos t)\end{cases}$（$t\in[0,2\pi]$） | $3\pi a^2$ | $8a$ |
| 阿基米德螺线（$0\to2\pi$） | $r=a\theta$ | $\frac{4}{3}\pi^3 a^2$ | $\frac{a}{2}[2\pi\sqrt{1+4\pi^2}+\ln(2\pi+\sqrt{1+4\pi^2})]$ |

---

# 九、解题思路总结

```
1. 画图 → 明确积分区域与边界线
2. 选择坐标系（直角/参数/极坐标）
3. 选择积分变量（x 或 y），注意是否需要分段
4. 套用公式，注意绝对值与符号
5. 计算定积分（有时需换元、分部等技巧）
```

> **易错点：**
> - 旋转体体积：分清圆盘法与柱壳法的适用条件
> - 绕非坐标轴的直线：先平移图形使旋转轴变成坐标轴
> - 参数方程求体积：注意 $x'(t)$ 的符号，必要时加绝对值
> - 极坐标面积：不要忘记 $\frac12$ 因子
> - 侧面积：弧微分 $\sqrt{1+(y')^2}$ 容易漏掉平方
> - 形心公式：面积 $A$ 在分母，小心求 $A$ 时算错

---

# 十、公式速查表

| 内容 | 直角坐标 | 参数方程 | 极坐标 |
|:---|:---|:---|:---|
| 面积 | $\int_a^b f(x)dx$ | $\int_\alpha^\beta y(t)x'(t)dt$ | $\frac12\int_\alpha^\beta r^2 d\theta$ |
| 弧长 | $\int_a^b \sqrt{1+(y')^2}\,dx$ | $\int_\alpha^\beta \sqrt{(x')^2+(y')^2}\,dt$ | $\int_\alpha^\beta \sqrt{r^2+(r')^2}\,d\theta$ |
| 旋转体体积（绕 $x$ 轴） | $\pi\int_a^b f^2 dx$ | $\pi\int_\alpha^\beta y^2 x' dt$ | $\frac{2\pi}{3}\int_\alpha^\beta r^3\sin\theta\,d\theta$ |
| 旋转曲面面积（绕 $x$ 轴） | $2\pi\int_a^b f\sqrt{1+(y')^2}\,dx$ | $2\pi\int_\alpha^\beta y\sqrt{(x')^2+(y')^2}\,dt$ | $2\pi\int_\alpha^\beta r\sin\theta\sqrt{r^2+(r')^2}\,d\theta$ |
| 形心 $\bar{x}$ | $\frac1A\int_a^b xf\,dx$ | $\frac{\int_\alpha^\beta x y x' dt}{\int_\alpha^\beta y x' dt}$ | — |
| 形心 $\bar{y}$ | $\frac1A\int_a^b \frac12 f^2\,dx$ | $\frac{\frac12\int_\alpha^\beta y^2 x' dt}{\int_\alpha^\beta y x' dt}$ | — |

---

**结束语：** 定积分的几何应用重在**理解微元法**，分清"切片"（圆盘）还是"取壳"（柱壳），并灵活选取坐标系。熟练掌握公式后，多练几道综合题即可应对考研。