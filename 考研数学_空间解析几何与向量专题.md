# 考研数学一 · 空间解析几何与向量代数专题整理

---

# 一、向量代数基础

## 1. 向量的基本概念

- **向量**：既有大小又有方向的量，记作 $\vec{a}$ 或 $\mathbf{a}$
- **零向量**：$\vec{0}$，长度为 0，方向任意
- **单位向量**：$|\vec{a}| = 1$ 的向量
- **方向角与方向余弦**：向量 $\vec{a} = (a_1,a_2,a_3)$ 与三坐标轴正向夹角 $\alpha,\beta,\gamma$ 满足：

  $$
  \cos\alpha = \frac{a_1}{|\vec{a}|},\quad \cos\beta = \frac{a_2}{|\vec{a}|},\quad \cos\gamma = \frac{a_3}{|\vec{a}|}
  $$

  **性质**：$\cos^2\alpha + \cos^2\beta + \cos^2\gamma = 1$

## 2. 向量的线性运算

### 加减法

$$
\vec{a} \pm \vec{b} = (a_1\pm b_1,\; a_2\pm b_2,\; a_3\pm b_3)
$$

### 数乘

$$
\lambda\vec{a} = (\lambda a_1,\; \lambda a_2,\; \lambda a_3)
$$

### 运算法则

- 交换律：$\vec{a} + \vec{b} = \vec{b} + \vec{a}$
- 结合律：$(\vec{a}+\vec{b})+\vec{c} = \vec{a}+(\vec{b}+\vec{c})$
- 分配律：$(\lambda+\mu)\vec{a} = \lambda\vec{a} + \mu\vec{a}$，$\lambda(\vec{a}+\vec{b}) = \lambda\vec{a} + \lambda\vec{b}$

## 3. 向量的数量积（点积、内积）

### 定义

$$
\vec{a}\cdot\vec{b} = |\vec{a}||\vec{b}|\cos\theta \quad (\theta\text{ 为夹角})
$$

### 坐标形式

$$
\vec{a}\cdot\vec{b} = a_1b_1 + a_2b_2 + a_3b_3
$$

### 性质

| 性质 | 公式 |
|:---|:---|
| 正定性 | $\vec{a}\cdot\vec{a} = |\vec{a}|^2 \ge 0$ |
| 交换律 | $\vec{a}\cdot\vec{b} = \vec{b}\cdot\vec{a}$ |
| 分配律 | $(\vec{a}+\vec{b})\cdot\vec{c} = \vec{a}\cdot\vec{c} + \vec{b}\cdot\vec{c}$ |
| 垂直条件 | $\vec{a}\cdot\vec{b} = 0 \iff \vec{a} \perp \vec{b}$ |

### 投影公式

向量 $\vec{a}$ 在 $\vec{b}$ 方向上的投影：

$$
\boxed{\text{Proj}_{\vec{b}} \vec{a} = \frac{\vec{a}\cdot\vec{b}}{|\vec{b}|}} = |\vec{a}|\cos\theta
$$

投影向量：

$$
\boxed{\text{Proj}_{\vec{b}} \vec{a}\; \text{(向量形式)} = \frac{\vec{a}\cdot\vec{b}}{|\vec{b}|^2}\,\vec{b}}
$$

## 4. 向量的向量积（叉积、外积）

### 定义

$$
\vec{a}\times\vec{b} = |\vec{a}||\vec{b}|\sin\theta \; \vec{n}
$$

其中 $\vec{n}$ 是垂直于 $\vec{a},\vec{b}$ 所在平面的单位向量，方向由**右手法则**确定。

### 坐标形式

$$
\vec{a}\times\vec{b} = \begin{vmatrix}
\mathbf{i} & \mathbf{j} & \mathbf{k} \\
a_1 & a_2 & a_3 \\
b_1 & b_2 & b_3
\end{vmatrix} = (a_2b_3 - a_3b_2,\; a_3b_1 - a_1b_3,\; a_1b_2 - a_2b_1)
$$

### 性质

| 性质 | 公式 |
|:---|:---|
| 反交换律 | $\vec{a}\times\vec{b} = -(\vec{b}\times\vec{a})$ |
| 数乘 | $(\lambda\vec{a})\times\vec{b} = \vec{a}\times(\lambda\vec{b}) = \lambda(\vec{a}\times\vec{b})$ |
| 分配律 | $\vec{a}\times(\vec{b}+\vec{c}) = \vec{a}\times\vec{b} + \vec{a}\times\vec{c}$ |
| 平行条件 | $\vec{a}\times\vec{b} = \vec{0} \iff \vec{a} \parallel \vec{b}$ |
| 模的几何意义 | $|\vec{a}\times\vec{b}|$ = 以 $\vec{a},\vec{b}$ 为邻边的平行四边形面积 |

## 5. 混合积

### 定义

$$
(\vec{a},\vec{b},\vec{c}) = \vec{a}\cdot(\vec{b}\times\vec{c})
$$

### 坐标形式

$$
\vec{a}\cdot(\vec{b}\times\vec{c}) = \begin{vmatrix}
a_1 & a_2 & a_3 \\
b_1 & b_2 & b_3 \\
c_1 & c_2 & c_3
\end{vmatrix}
$$

### 性质

| 性质 | 描述 |
|:---|:---|
| 几何意义 | $|\vec{a}\cdot(\vec{b}\times\vec{c})|$ = 以 $\vec{a},\vec{b},\vec{c}$ 为棱的平行六面体体积 |
| 共面条件 | $\vec{a}\cdot(\vec{b}\times\vec{c}) = 0 \iff \vec{a},\vec{b},\vec{c}$ 共面 |
| 轮换对称 | $\vec{a}\cdot(\vec{b}\times\vec{c}) = \vec{b}\cdot(\vec{c}\times\vec{a}) = \vec{c}\cdot(\vec{a}\times\vec{b})$ |
| 交换任两个 | 交换两个向量，混合积变号 |

### 判断向量共面

三个向量 $\vec{a},\vec{b},\vec{c}$ 共面 $\Longleftrightarrow$

$$
\begin{vmatrix}
a_1 & a_2 & a_3 \\
b_1 & b_2 & b_3 \\
c_1 & c_2 & c_3
\end{vmatrix} = 0
$$

> 💡 **典型应用**：判断四点 $A,B,C,D$ 共面 ⇔ 向量 $\overrightarrow{AB},\overrightarrow{AC},\overrightarrow{AD}$ 的混合积为 0。

## 6. 向量运算的记忆口诀

> **数量积**：对应坐标相乘后相加，交换不变号
> **向量积**：行列式展开，交换要变号
> **混合积**：三向量行列式，等于零时共面

---

# 二、空间平面方程

## 1. 平面的几种方程形式

### ① 点法式

过点 $M_0(x_0,y_0,z_0)$，法向量 $\vec{n} = (A,B,C)$：

$$
\boxed{A(x-x_0) + B(y-y_0) + C(z-z_0) = 0}
$$

### ② 一般式

$$
\boxed{Ax + By + Cz + D = 0}
$$

其中 $(A,B,C)$ 为法向量。

### ③ 截距式

平面在 $x,y,z$ 轴上的截距分别为 $a,b,c$：

$$
\boxed{\frac{x}{a} + \frac{y}{b} + \frac{z}{c} = 1}
$$

### ④ 三点式

过不共线的三点 $M_1(x_1,y_1,z_1), M_2(x_2,y_2,z_2), M_3(x_3,y_3,z_3)$：

法向量 $\vec{n} = \overrightarrow{M_1M_2} \times \overrightarrow{M_1M_3}$，再用点法式。

也可以用行列式直接写出：

$$
\begin{vmatrix}
x-x_1 & y-y_1 & z-z_1 \\
x_2-x_1 & y_2-y_1 & z_2-z_1 \\
x_3-x_1 & y_3-y_1 & z_3-z_1
\end{vmatrix} = 0
$$

### ⑤ 参数式

过点 $M_0$，两个方向向量 $\vec{v}_1, \vec{v}_2$（不平行）：

$$
\vec{r} = \vec{r}_0 + u\vec{v}_1 + v\vec{v}_2
$$

即：

$$
\begin{cases}
x = x_0 + u\,v_{11} + v\,v_{21} \\
y = y_0 + u\,v_{12} + v\,v_{22} \\
z = z_0 + u\,v_{13} + v\,v_{23}
\end{cases}
$$

## 2. 两平面的位置关系

设两平面 $\Pi_1: A_1x + B_1y + C_1z + D_1 = 0$，$\Pi_2: A_2x + B_2y + C_2z + D_2 = 0$

| 位置关系 | 条件 |
|:---|:---|
| **平行** | $\dfrac{A_1}{A_2} = \dfrac{B_1}{B_2} = \dfrac{C_1}{C_2} \neq \dfrac{D_1}{D_2}$ |
| **重合** | $\dfrac{A_1}{A_2} = \dfrac{B_1}{B_2} = \dfrac{C_1}{C_2} = \dfrac{D_1}{D_2}$ |
| **相交** | $\dfrac{A_1}{A_2},\dfrac{B_1}{B_2},\dfrac{C_1}{C_2}$ 不全相等 |
| **垂直** | $A_1A_2 + B_1B_2 + C_1C_2 = 0$ |

### 两平面夹角

$$
\cos\varphi = \frac{|\vec{n}_1\cdot\vec{n}_2|}{|\vec{n}_1||\vec{n}_2|} = \frac{|A_1A_2 + B_1B_2 + C_1C_2|}{\sqrt{A_1^2+B_1^2+C_1^2}\sqrt{A_2^2+B_2^2+C_2^2}}
$$

> ⚠️ 两平面夹角取锐角，故分子取绝对值。

---

# 三、空间直线方程

## 1. 直线的几种方程形式

### ① 对称式（点向式）

过点 $M_0(x_0,y_0,z_0)$，方向向量 $\vec{s} = (m,n,p)$：

$$
\boxed{\frac{x-x_0}{m} = \frac{y-y_0}{n} = \frac{z-z_0}{p}}
$$

> ⚠️ 若 $m,n,p$ 中有零，如 $m=0$，则理解为 $x - x_0 = 0$，即 $\dfrac{x-x_0}{0}$ 是形式记号，表示分子也为 0。

### ② 参数式

$$
\boxed{\begin{cases}
x = x_0 + mt \\
y = y_0 + nt \\
z = z_0 + pt
\end{cases}}
$$

### ③ 一般式（两平面交线）

直线是两平面的交线：

$$
\boxed{\begin{cases}
A_1x + B_1y + C_1z + D_1 = 0 \\
A_2x + B_2y + C_2z + D_2 = 0
\end{cases}}
$$

此时直线的方向向量 $\vec{s} = \vec{n}_1 \times \vec{n}_2$，即：

$$
\vec{s} = \begin{vmatrix}
\mathbf{i} & \mathbf{j} & \mathbf{k} \\
A_1 & B_1 & C_1 \\
A_2 & B_2 & C_2
\end{vmatrix}
$$

### ④ 两点式

过两点 $M_1(x_1,y_1,z_1)$ 和 $M_2(x_2,y_2,z_2)$：

$$
\frac{x-x_1}{x_2-x_1} = \frac{y-y_1}{y_2-y_1} = \frac{z-z_1}{z_2-z_1}
$$

## 2. 两直线的位置关系

设两直线 $L_1$（过 $M_1$，方向 $\vec{s}_1 = (m_1,n_1,p_1)$），$L_2$（过 $M_2$，方向 $\vec{s}_2 = (m_2,n_2,p_2)$）

| 位置关系 | 条件 |
|:---|:---|
| **平行** | $\vec{s}_1 \parallel \vec{s}_2$ 且 $M_1 \notin L_2$ |
| **重合** | $\vec{s}_1 \parallel \vec{s}_2$ 且 $M_1 \in L_2$ |
| **相交** | $\vec{s}_1 \nparallel \vec{s}_2$ 且 $\overrightarrow{M_1M_2}$ 与 $\vec{s}_1,\vec{s}_2$ 共面 |
| **异面** | $\vec{s}_1 \nparallel \vec{s}_2$ 且 $\overrightarrow{M_1M_2}$ 与 $\vec{s}_1,\vec{s}_2$ 不共面 |

**判断共面/异面（混合积法）**：

$$
(\overrightarrow{M_1M_2},\vec{s}_1,\vec{s}_2) = \begin{vmatrix}
x_2-x_1 & y_2-y_1 & z_2-z_1 \\
m_1 & n_1 & p_1 \\
m_2 & n_2 & p_2
\end{vmatrix}
$$

- 混合积 $= 0$ $\Rightarrow$ 共面（相交或平行）
- 混合积 $\neq 0$ $\Rightarrow$ 异面

### 两直线夹角

$$
\cos\varphi = \frac{|\vec{s}_1\cdot\vec{s}_2|}{|\vec{s}_1||\vec{s}_2|} = \frac{|m_1m_2 + n_1n_2 + p_1p_2|}{\sqrt{m_1^2+n_1^2+p_1^2}\sqrt{m_2^2+n_2^2+p_2^2}}
$$

### 两直线垂直条件

$$
m_1m_2 + n_1n_2 + p_1p_2 = 0
$$

## 3. 直线与平面的位置关系

设直线 $L$：过点 $M_0$，方向 $\vec{s} = (m,n,p)$
平面 $\Pi$：法向量 $\vec{n} = (A,B,C)$

| 位置关系 | 条件 |
|:---|:---|
| **平行** | $\vec{s} \perp \vec{n}$（即 $\vec{s}\cdot\vec{n} = 0$）且 $M_0$ 不在平面上 |
| **直线在平面上** | $\vec{s} \perp \vec{n}$ 且 $M_0$ 在平面上 |
| **相交** | $\vec{s} \not\perp \vec{n}$ |
| **垂直** | $\vec{s} \parallel \vec{n}$（即 $\dfrac{m}{A} = \dfrac{n}{B} = \dfrac{p}{C}$） |

### 直线与平面夹角

直线与平面夹角 $\varphi$ 定义为直线与其在平面上的投影之间的锐角：

$$
\sin\varphi = \frac{|\vec{s}\cdot\vec{n}|}{|\vec{s}||\vec{n}|} = \frac{|Am + Bn + Cp|}{\sqrt{A^2+B^2+C^2}\sqrt{m^2+n^2+p^2}}
$$

> 💡 注意是 $\sin\varphi$ 不是 $\cos\varphi$（与线线角、面面角不同）！

---

# 四、点到直线与点到平面的距离

## 1. 点到平面的距离

点 $P_0(x_0,y_0,z_0)$ 到平面 $\Pi: Ax + By + Cz + D = 0$ 的距离：

$$
\boxed{d = \frac{|Ax_0 + By_0 + Cz_0 + D|}{\sqrt{A^2 + B^2 + C^2}}}
$$

### 记忆方法

将点坐标代入平面方程取绝对值，除以法向量的模。

### 应用：两平行平面的距离

设两平行平面 $Ax + By + Cz + D_1 = 0$ 和 $Ax + By + Cz + D_2 = 0$，距离为：

$$
d = \frac{|D_1 - D_2|}{\sqrt{A^2 + B^2 + C^2}}
$$

## 2. 点到直线的距离

点 $P_0$ 到直线 $L$（过点 $M_0$，方向 $\vec{s}$）的距离：

### 公式一（向量积法）

$$
\boxed{d = \frac{|\overrightarrow{M_0P_0} \times \vec{s}|}{|\vec{s}|}}
$$

**几何意义**：以 $\overrightarrow{M_0P_0}$ 和 $\vec{s}$ 为邻边的平行四边形面积除以底边长 $|\vec{s}|$。

### 公式二（坐标计算）

若直线为对称式 $\dfrac{x-x_0}{m} = \dfrac{y-y_0}{n} = \dfrac{z-z_0}{p}$，点 $P_1(x_1,y_1,z_1)$，则：

$$
d = \sqrt{\frac{\begin{vmatrix}
y_1-y_0 & z_1-z_0 \\
n & p
\end{vmatrix}^2 + \begin{vmatrix}
z_1-z_0 & x_1-x_0 \\
p & m
\end{vmatrix}^2 + \begin{vmatrix}
x_1-x_0 & y_1-y_0 \\
m & n
\end{vmatrix}^2}{m^2+n^2+p^2}}
$$

> 实际做题直接用向量积公式更方便。

## 3. 异面直线的距离

设两异面直线 $L_1$（过 $M_1$，方向 $\vec{s}_1$），$L_2$（过 $M_2$，方向 $\vec{s}_2$）

### 公式

$$
\boxed{d = \frac{|(\overrightarrow{M_1M_2},\vec{s}_1,\vec{s}_2)|}{|\vec{s}_1 \times \vec{s}_2|} = \frac{|\overrightarrow{M_1M_2}\cdot(\vec{s}_1\times\vec{s}_2)|}{|\vec{s}_1 \times \vec{s}_2|}}
$$

**几何意义**：混合积的绝对值等于以 $\overrightarrow{M_1M_2},\vec{s}_1,\vec{s}_2$ 为棱的平行六面体体积，除以底面积 $|\vec{s}_1\times\vec{s}_2|$，得到高（即距离）。

### 求公垂线的方法

1. 公垂线的方向向量 $\vec{s} = \vec{s}_1 \times \vec{s}_2$
2. 过 $L_1$ 和 $\vec{s}$ 作平面 $\Pi_1$，过 $L_2$ 和 $\vec{s}$ 作平面 $\Pi_2$
3. $\Pi_1 \cap \Pi_2$ 即为公垂线

> **记忆提示**：三种距离公式中的分母都是"法线方向"的模长，分子都是代入的绝对值：
> - 点→平面：$|Ax_0+By_0+Cz_0+D|$ / $|\vec{n}|$
> - 点→直线：$|\overrightarrow{M_0P_0}\times\vec{s}|$ / $|\vec{s}|$
> - 异面直线：$|(\overrightarrow{M_1M_2},\vec{s}_1,\vec{s}_2)|$ / $|\vec{s}_1\times\vec{s}_2|$

---

# 五、平面束

## 1. 平面束的定义

过同一条直线 $L$ 的所有平面的全体称为**平面束**（或称平面族）。

## 2. 平面束方程

### 一般形式

设直线 $L$ 由两平面交线给出：

$$
\begin{cases}
A_1x + B_1y + C_1z + D_1 = 0 \\
A_2x + B_2y + C_2z + D_2 = 0
\end{cases}
$$

则过 $L$ 的平面束方程为：

$$
\boxed{A_1x + B_1y + C_1z + D_1 + \lambda(A_2x + B_2y + C_2z + D_2) = 0}
$$

其中 $\lambda$ 为参数（$\lambda \in \mathbb{R}$），且 $\lambda = \infty$ 时对应第二个平面（即 $A_2x + B_2y + C_2z + D_2 = 0$）。

> ⚠️ 该形式**缺**第二个平面本身（当 $\lambda\to\infty$ 时），所以需考虑 $\lambda = \infty$ 情况，在实际做题中常将其单独讨论。

### 另一种等价形式

$$
\mu(A_1x + B_1y + C_1z + D_1) + \lambda(A_2x + B_2y + C_2z + D_2) = 0
$$

其中 $\mu,\lambda$ 不全为 0，这种形式包含了所有过 $L$ 的平面（包括两个已知平面本身）。

## 3. 平面束的经典应用

### 应用一：求过直线且满足某条件的平面

**例**：求过直线 $\begin{cases} x + y - z - 1 = 0 \\ x - y + 2z + 2 = 0 \end{cases}$ 且与平面 $x + 2y + z = 1$ 垂直的平面方程。

> **解**：
> 设平面束方程：$(x+y-z-1) + \lambda(x-y+2z+2) = 0$
> 整理：$(1+\lambda)x + (1-\lambda)y + (-1+2\lambda)z + (-1+2\lambda) = 0$
> 法向量 $\vec{n} = (1+\lambda,\; 1-\lambda,\; -1+2\lambda)$
> 与已知平面 $(1,2,1)$ 垂直 $\Rightarrow$ 点积为 0：
> $(1+\lambda)\cdot1 + (1-\lambda)\cdot2 + (-1+2\lambda)\cdot1 = 0$
> $1+\lambda + 2-2\lambda -1+2\lambda = 0 \Rightarrow 2 + \lambda = 0 \Rightarrow \lambda = -2$
> 代入得：$(-1)x + 3y + (-5)z + (-5) = 0$
> 即：$x - 3y + 5z + 5 = 0$

### 应用二：求过直线且与某平面平行的平面

**例**：求过直线 $\begin{cases} x+y+z-1=0 \\ 2x-y+3z=0 \end{cases}$ 且平行于平面 $x-y+z+1=0$ 的平面。

> **解**：
> 设平面束：$(x+y+z-1) + \lambda(2x-y+3z) = 0$
> 整理：$(1+2\lambda)x + (1-\lambda)y + (1+3\lambda)z - 1 = 0$
> 与 $x-y+z+1=0$ 平行 ⇒ 法向量共线：
> $\dfrac{1+2\lambda}{1} = \dfrac{1-\lambda}{-1} = \dfrac{1+3\lambda}{1}$
> 由 $\dfrac{1+2\lambda}{1} = \dfrac{1-\lambda}{-1}$ 得 $1+2\lambda = -(1-\lambda) \Rightarrow \lambda = -\dfrac{2}{3}$
> 代入验证第三项：$\dfrac{1+3(-\frac23)}{1} = \dfrac{1-2}{1} = -1$，与第一项 $1+2(-\frac23) = -\frac13$ 不相等，矛盾！
> 说明 $\lambda = -\frac23$ 不满足全部比例 ⇒ 该直线平行于目标平面。
> 改用另一种方法...

> ⚠️ 遇到此类无解情形，说明直线与目标平面平行，需重新处理方法。

---

# 六、投影问题

## 1. 点在平面上的投影

点 $P_0(x_0,y_0,z_0)$ 在平面 $\Pi: Ax+By+Cz+D=0$ 上的投影点 $P'$：

**方法**：过点 $P_0$ 作平面的垂线，求直线与平面的交点。

1. 垂线方向 = 法向量 $\vec{n} = (A,B,C)$
2. 垂线参数方程：$\begin{cases} x = x_0 + At \\ y = y_0 + Bt \\ z = z_0 + Ct \end{cases}$
3. 代入平面方程解出 $t$：
   $A(x_0+At) + B(y_0+Bt) + C(z_0+Ct) + D = 0$
   $\Rightarrow t = -\dfrac{Ax_0+By_0+Cz_0+D}{A^2+B^2+C^2}$
4. 将 $t$ 代回参数方程即得投影点 $P'$

## 2. 点在直线上的投影

点 $P_0$ 在直线 $L$（过 $M_0$，方向 $\vec{s}$）上的投影点 $P'$：

**方法**：过点 $P_0$ 作直线的垂面，求平面与直线的交点。

1. 垂面法向量 = 直线方向 $\vec{s} = (m,n,p)$
2. 垂面方程：$m(x-x_0) + n(y-y_0) + p(z-z_0) = 0$
3. 将直线参数式代入垂面方程解出参数 $t$
4. 代回得投影点 $P'$

**或直接用投影公式**（向量法）：

$$
\overrightarrow{M_0P'} = \frac{\overrightarrow{M_0P_0}\cdot\vec{s}}{|\vec{s}|^2}\,\vec{s}
$$

即 $P'$ 是 $P_0$ 在直线方向上的投影点。

## 3. 直线在平面上的投影

直线 $L$ 在平面 $\Pi$ 上的投影直线 $L'$（或称射影直线）：

### 方法一（投影平面法）

1. 过直线 $L$ 作垂直于平面 $\Pi$ 的平面（称为**投影平面**）
2. 投影直线 $L'$ = 投影平面 $\cap$ 已知平面 $\Pi$

### 方法二（平面束法）

**步骤**：
1. 写出过 $L$ 的平面束方程：$F_1 + \lambda F_2 = 0$
2. 利用**垂直条件**求 $\lambda$：平面束中某平面的法向量 $\perp$ 平面 $\Pi$ 的法向量
3. 该平面即为投影平面
4. 投影直线 = 该平面 $\cap$ 已知平面

### 方法三（点投影法）

1. 在直线 $L$ 上取两点 $A,B$
2. 分别求 $A,B$ 在平面 $\Pi$ 上的投影点 $A',B'$
3. 过 $A',B'$ 的直线即为投影直线

**例**：求直线 $L:\dfrac{x-1}{1} = \dfrac{y}{1} = \dfrac{z+1}{-1}$ 在平面 $\Pi:x-y+2z-1=0$ 上的投影直线方程。

> **解（平面束法）**：
>
> **Step 1**：将直线表示为两平面交线
>
> 由对称式得：
> $\dfrac{x-1}{1} = \dfrac{y}{1} \Rightarrow x-1 = y \Rightarrow x - y - 1 = 0$
> $\dfrac{y}{1} = \dfrac{z+1}{-1} \Rightarrow y = -(z+1) \Rightarrow y + z + 1 = 0$
>
> 即 $L: \begin{cases} x - y - 1 = 0 \\ y + z + 1 = 0 \end{cases}$
>
> **Step 2**：设过 $L$ 的平面束
>
> $(x-y-1) + \lambda(y+z+1) = 0 \Rightarrow x + (\lambda-1)y + \lambda z + (\lambda-1) = 0$
>
> **Step 3**：求投影平面（与已知平面 $\Pi$ 垂直）
>
> 法向量垂直条件：$(1,\lambda-1,\lambda)\cdot(1,-1,2) = 0$
> $1 - (\lambda-1) + 2\lambda = 0 \Rightarrow 1 - \lambda + 1 + 2\lambda = 0 \Rightarrow \lambda = -2$
>
> **Step 4**：投影平面为：$x + (-3)y + (-2)z + (-3) = 0$，即 $x - 3y - 2z - 3 = 0$
>
> **Step 5**：投影直线 $L'$：
>
> $$
> \begin{cases}
> x - 3y - 2z - 3 = 0 \\
> x - y + 2z - 1 = 0
> \end{cases}
> $$

## 4. 直线在平面上的投影长度

设直线 $L$ 与平面 $\Pi$ 的夹角为 $\varphi$，$L$ 上线段长度为 $l$，其投影长度为：

$$
l' = l\cos\varphi
$$

其中 $\sin\varphi = \dfrac{|\vec{s}\cdot\vec{n}|}{|\vec{s}||\vec{n}|}$，$\cos\varphi = \sqrt{1-\sin^2\varphi}$。

---

# 七、切线、法平面与切平面、法线

## 1. 空间曲线的切线与法平面

### ① 参数方程情形

曲线 $\Gamma: \begin{cases} x = x(t) \\ y = y(t) \\ z = z(t) \end{cases}$，$t=t_0$ 对应点 $M_0(x_0,y_0,z_0)$

**切向量**：$\vec{T} = (x'(t_0),\, y'(t_0),\, z'(t_0))$

**切线方程**（点向式）：

$$
\boxed{\frac{x-x_0}{x'(t_0)} = \frac{y-y_0}{y'(t_0)} = \frac{z-z_0}{z'(t_0)}}
$$

**法平面方程**（过切点且与切线垂直的平面）：

$$
\boxed{x'(t_0)(x-x_0) + y'(t_0)(y-y_0) + z'(t_0)(z-z_0) = 0}
$$

### ② 一般式（两曲面交线）情形

曲线 $\Gamma: \begin{cases} F(x,y,z) = 0 \\ G(x,y,z) = 0 \end{cases}$，点 $M_0(x_0,y_0,z_0)$

**切向量**：

$$
\vec{T} = \nabla F(M_0) \times \nabla G(M_0) = \begin{vmatrix}
\mathbf{i} & \mathbf{j} & \mathbf{k} \\
F_x & F_y & F_z \\
G_x & G_y & G_z
\end{vmatrix}_{(x_0,y_0,z_0)}
$$

**切线方程**：用法向量 $\vec{T}$ 作为方向向量代入对称式。

**法平面方程**：用 $\vec{T}$ 作为法向量写出平面方程。

> 💡 曲线的一般式 = 两个曲面的交线，切向量正是两个曲面法向量的叉积。

### ③ 特殊情形：$y=y(x),\; z=z(x)$

曲线 $\Gamma: \begin{cases} y = y(x) \\ z = z(x) \end{cases}$，点 $M_0(x_0,y_0,z_0)$

**切线方程**：

$$
\frac{x-x_0}{1} = \frac{y-y_0}{y'(x_0)} = \frac{z-z_0}{z'(x_0)}
$$

**法平面方程**：

$$
(x-x_0) + y'(x_0)(y-y_0) + z'(x_0)(z-z_0) = 0
$$

## 2. 曲面的切平面与法线

### ① 显式方程 $z = f(x,y)$

曲面 $z = f(x,y)$ 在点 $M_0(x_0,y_0,z_0)$ 处（其中 $z_0 = f(x_0,y_0)$）

**法向量**：

$$
\vec{n} = \pm(f_x(x_0,y_0),\; f_y(x_0,y_0),\; -1)
$$

通常取 $\vec{n} = (f_x,\; f_y,\; -1)$（指向 $z$ 正向）。

**切平面方程**：

$$
\boxed{z - z_0 = f_x(x_0,y_0)(x-x_0) + f_y(x_0,y_0)(y-y_0)}
$$

或写成：

$$
f_x(x_0,y_0)(x-x_0) + f_y(x_0,y_0)(y-y_0) - (z-z_0) = 0
$$

**法线方程**：

$$
\boxed{\frac{x-x_0}{f_x(x_0,y_0)} = \frac{y-y_0}{f_y(x_0,y_0)} = \frac{z-z_0}{-1}}
$$

### ② 隐式方程 $F(x,y,z) = 0$

曲面 $F(x,y,z) = 0$ 在点 $M_0(x_0,y_0,z_0)$ 处，$F(M_0) = 0$

**法向量**：

$$
\vec{n} = \nabla F(M_0) = (F_x(M_0),\; F_y(M_0),\; F_z(M_0))
$$

**切平面方程**：

$$
\boxed{F_x(M_0)(x-x_0) + F_y(M_0)(y-y_0) + F_z(M_0)(z-z_0) = 0}
$$

**法线方程**：

$$
\boxed{\frac{x-x_0}{F_x(M_0)} = \frac{y-y_0}{F_y(M_0)} = \frac{z-z_0}{F_z(M_0)}}
$$

### ③ 参数方程情形（修正版）

曲面 $\Sigma: \begin{cases} x = x(u,v) \\ y = y(u,v) \\ z = z(u,v) \end{cases}$，$(u_0,v_0)$ 对应点 $M_0$

**切向量**：

$$
\vec{r}_u = (x_u,\; y_u,\; z_u), \quad \vec{r}_v = (x_v,\; y_v,\; z_v)
$$

**法向量**：



$$
\vec{n} = \vec{r}_u \times \vec{r}_v = \begin{vmatrix}
\mathbf{i} & \mathbf{j} & \mathbf{k} \\
x_u & y_u & z_u \\
x_v & y_v & z_v
\end{vmatrix}
$$

**法线方程**：

$$
\boxed{\frac{x-x_0}{n_x} = \frac{y-y_0}{n_y} = \frac{z-z_0}{n_z}}
$$

**切平面方程**：

$$
\boxed{n_x(x-x_0) + n_y(y-y_0) + n_z(z-z_0) = 0}
$$

> 🔁 参数曲面的法向量方向取决于 $(u,v)$ 参数的取向，必要时可调整符号。

---

# 八、梯度、散度、旋度与方向导数

> 本专题属于**向量场微积分**的基础内容，在考研数学一中常以选择题或填空题出现，也可与曲线/曲面积分结合出大题。梯度、散度、方向导数是联系**数量场**与**向量场**的桥梁。

## 1. 方向导数

### 定义

设函数 $u = f(x,y,z)$ 在点 $P_0(x_0,y_0,z_0)$ 的某邻域内有定义，$\vec{l}$ 为从 $P_0$ 出发的方向向量（单位向量 $\vec{l}_0 = (\cos\alpha,\cos\beta,\cos\gamma)$），则 $f$ 在 $P_0$ 处沿方向 $\vec{l}$ 的**方向导数**定义为：

$$
\boxed{\frac{\partial f}{\partial l}\bigg|_{P_0} = \lim_{\rho \to 0^+} \frac{f(x_0+\rho\cos\alpha,\; y_0+\rho\cos\beta,\; z_0+\rho\cos\gamma) - f(x_0,y_0,z_0)}{\rho}}
$$

其中 $\rho = \sqrt{(\Delta x)^2 + (\Delta y)^2 + (\Delta z)^2}$。

### 计算公式（可微条件下）

若 $f(x,y,z)$ 在 $P_0$ 处可微，则：

$$
\boxed{\frac{\partial f}{\partial l}\bigg|_{P_0} = f_x(P_0)\cos\alpha + f_y(P_0)\cos\beta + f_z(P_0)\cos\gamma}
$$

其中 $\cos\alpha,\cos\beta,\cos\gamma$ 是方向 $\vec{l}$ 的方向余弦。

> 💡 **记忆**：方向导数 = 梯度与方向单位向量的**点积**。

### 方向导数与偏导数的关系

- 沿 $x$ 轴正向：$\dfrac{\partial f}{\partial l} = f_x$
- 沿 $x$ 轴负向：$\dfrac{\partial f}{\partial l} = -f_x$
- 沿 $y$ 轴正向：$\dfrac{\partial f}{\partial l} = f_y$
- 一般方向：方向导数是各偏导数的有机组合

---

## 2. 梯度

### 定义

设函数 $u = f(x,y,z)$ 在区域 $D$ 内具有一阶连续偏导数，则对于每一点 $P(x,y,z) \in D$，定义**梯度** $\mathbf{grad}\, f$ 为：

$$
\boxed{\mathbf{grad}\, f = \left( \frac{\partial f}{\partial x},\; \frac{\partial f}{\partial y},\; \frac{\partial f}{\partial z} \right)}
$$

也记作 $\nabla f$，其中 $\nabla = \left( \dfrac{\partial}{\partial x},\; \dfrac{\partial}{\partial y},\; \dfrac{\partial}{\partial z} \right)$ 称为**哈密顿算子**。

### 梯度的性质

| 性质 | 公式 |
|:---|:---|
| 线性 | $\nabla(c_1f_1 + c_2f_2) = c_1\nabla f_1 + c_2\nabla f_2$ |
| 乘积法则 | $\nabla(fg) = f\nabla g + g\nabla f$ |
| 复合函数 | $\nabla(f(u)) = f'(u)\nabla u$ |

### 梯度的几何意义

1. **方向导数与梯度的关系**：
   $$
   \frac{\partial f}{\partial l} = \nabla f \cdot \vec{l}_0 = |\nabla f| \cos\theta
   $$
   其中 $\theta$ 为 $\nabla f$ 与 $\vec{l}_0$ 的夹角。

2. **梯度方向**：函数增长最快的方向，即方向导数取得**最大值**的方向。
   - 最大值：$\max\left(\dfrac{\partial f}{\partial l}\right) = |\nabla f|$
   - 此时 $\vec{l}_0$ 与 $\nabla f$ 同向

3. **梯度的模**：沿梯度方向的方向导数（即最大方向导数）：
   $$
   |\nabla f| = \sqrt{f_x^2 + f_y^2 + f_z^2}
   $$

4. **等高线/等位面**：梯度方向垂直于等值面（等位面）的法线方向，且指向函数值增加的方向。

> 📌 **与本章前面的联系**：
> 在 §七 曲面的切平面与法线中，隐式曲面 $F(x,y,z)=0$ 的法向量就是 $\nabla F$。梯度天然就是曲面的法向量。

---

## 3. 散度

### 定义

设有向量场 $\vec{A}(x,y,z) = (P(x,y,z),\; Q(x,y,z),\; R(x,y,z))$，其中 $P,Q,R$ 具有一阶连续偏导数，则**散度** $\mathrm{div}\, \vec{A}$ 定义为：

$$
\boxed{\mathrm{div}\, \vec{A} = \frac{\partial P}{\partial x} + \frac{\partial Q}{\partial y} + \frac{\partial R}{\partial z}}
$$

也记作 $\nabla \cdot \vec{A}$。

### 物理意义

散度表示向量场在某点处的**通量密度**（即单位体积内向外流出的净通量）：

- $\mathrm{div}\, \vec{A} > 0$：该点有**源**（向外发散），如正电荷、热源
- $\mathrm{div}\, \vec{A} < 0$：该点有**汇**（向内汇聚），如负电荷、热沉
- $\mathrm{div}\, \vec{A} = 0$：无源场，向量线是"管形"的（如磁场 $\nabla\cdot B = 0$）

### 散度的性质

| 性质 | 公式 |
|:---|:---|
| 线性 | $\nabla \cdot (c_1\vec{A}_1 + c_2\vec{A}_2) = c_1\nabla\cdot\vec{A}_1 + c_2\nabla\cdot\vec{A}_2$ |
| 乘积法则（标量 × 向量） | $\nabla \cdot (f\vec{A}) = \nabla f \cdot \vec{A} + f(\nabla \cdot \vec{A})$ |

### 重要特殊散度

1. **梯度场的散度**（拉普拉斯算子）：
   $$
   \nabla \cdot (\nabla f) = \frac{\partial^2 f}{\partial x^2} + \frac{\partial^2 f}{\partial y^2} + \frac{\partial^2 f}{\partial z^2} = \nabla^2 f = \Delta f
   $$

2. **调和函数**：如果 $\Delta f = 0$，则称 $f$ 是调和函数（如静电场中无电荷区域的电势）。

### 散度定理（高斯公式）

$$
\iiint_V \nabla \cdot \vec{A} \, \mathrm{d}V = \oiint_S \vec{A} \cdot \mathrm{d}\vec{S}
$$

即：向量场通过封闭曲面 $S$ 的**通量**等于其散度在曲面所围区域 $V$ 上的**三重积分**。

---

## 4. 旋度

### 定义

设有向量场 $\vec{A}(x,y,z) = (P(x,y,z),\; Q(x,y,z),\; R(x,y,z))$，其中 $P,Q,R$ 具有一阶连续偏导数，则**旋度** $\mathbf{rot}\, \vec{A}$（或 $\mathbf{curl}\, \vec{A}$）定义为：

$$
\boxed{\mathbf{rot}\, \vec{A} = \nabla \times \vec{A} = \begin{vmatrix}
\mathbf{i} & \mathbf{j} & \mathbf{k} \\
\dfrac{\partial}{\partial x} & \dfrac{\partial}{\partial y} & \dfrac{\partial}{\partial z} \\
P & Q & R
\end{vmatrix}}
$$

展开为坐标形式：

$$
\boxed{\mathbf{rot}\, \vec{A} = \left( \frac{\partial R}{\partial y} - \frac{\partial Q}{\partial z},\; \frac{\partial P}{\partial z} - \frac{\partial R}{\partial x},\; \frac{\partial Q}{\partial x} - \frac{\partial P}{\partial y} \right)}
$$

> 💡 **记忆**：旋度 = 哈密顿算子 $\nabla$ 与向量场的**叉积**；对比散度 = $\nabla$ 与向量场的**点积**。

### 物理意义

旋度表示向量场在该点处的**环量密度**（单位面积上的最大环量，方向由**右手法则**确定指向面积的法向）：

- $\mathbf{rot}\,\vec{A} \neq \vec{0}$：该点有**涡旋源**（如磁场中的电流）
- $\mathbf{rot}\,\vec{A} = \vec{0}$：**无旋场**（保守场），如静电场 $\nabla \times \vec{E} = \vec{0}$

### 旋度的性质

| 性质 | 公式 |
|:---|:---|
| 线性 | $\nabla \times (c_1\vec{A}_1 + c_2\vec{A}_2) = c_1\nabla\times\vec{A}_1 + c_2\nabla\times\vec{A}_2$ |
| 乘积法则（标量×向量） | $\nabla \times (f\vec{A}) = \nabla f \times \vec{A} + f(\nabla \times \vec{A})$ |
| 梯度场无旋 | $\nabla \times (\nabla f) = \vec{0}$ |
| 旋度场无散 | $\nabla \cdot (\nabla \times \vec{A}) = 0$ |

### 旋度定理（斯托克斯公式）

$$
\boxed{\oint_L \vec{A} \cdot \mathrm{d}\vec{l} = \iint_S (\nabla \times \vec{A}) \cdot \mathrm{d}\vec{S}}
$$

即：向量场沿封闭曲线 $L$ 的**环量**等于其旋度在以 $L$ 为边界的曲面 $S$ 上的**通量**。

---

## 5. 梯度、散度、旋度与方向导数的关系总结

| 运算 | 输入 | 输出 | 记法 | 几何/物理意义 |
|:---|:---|:---|:---|:---|
| **方向导数** | 数量场 + 方向 | 标量 | $\dfrac{\partial f}{\partial l}$ | 沿指定方向的变化率 |
| **梯度** | 数量场 | **向量** | $\nabla f$ | 方向导数最大的方向；等值面的法线方向 |
| **散度** | 向量场 | 标量 | $\nabla \cdot \vec{A}$ | 单位体积的净通量（源密度） |
| **旋度** | 向量场 | **向量** | $\nabla \times \vec{A}$ | 单位面积的环量（涡旋源密度） |

### 考研常考题型

**题型一：方向导数的计算**

> **例**：求函数 $u = xy + yz + zx$ 在点 $(1,1,1)$ 处沿方向 $\vec{l} = (2,1,-2)$ 的方向导数。

**解**：
1. 计算梯度：$\nabla u = (y+z,\; x+z,\; x+y)$，在 $(1,1,1)$ 处 $\nabla u = (2,2,2)$
2. 单位方向向量：$|\vec{l}| = \sqrt{4+1+4} = 3$，$\vec{l}_0 = \left(\dfrac{2}{3},\dfrac13,-\dfrac23\right)$
3. 方向导数：
   $$
   \frac{\partial u}{\partial l} = \nabla u \cdot \vec{l}_0 = 2\cdot\frac23 + 2\cdot\frac13 + 2\cdot\left(-\frac23\right) = \frac43 + \frac23 - \frac43 = \frac23
   $$

---

**题型二：梯度与最大方向导数**

> **例**：函数 $f(x,y,z) = x^2y + yz^2$ 在点 $(1,2,-1)$ 处，求函数增长最快的方向及该方向的方向导数。

**解**：
1. 梯度：$f_x = 2xy,\; f_y = x^2+z^2,\; f_z = 2yz$
   在 $(1,2,-1)$ 处：$f_x = 4,\; f_y = 1+1=2,\; f_z = 2\cdot2\cdot(-1) = -4$
   $\nabla f = (4,2,-4)$
2. 增长最快的方向：沿 $\nabla f$ 方向，即 $(4,2,-4)$ 方向
3. 最大方向导数：$|\nabla f| = \sqrt{16+4+16} = \sqrt{36} = 6$

---

**题型三：散度的计算**

> **例**：求向量场 $\vec{A} = (x^2yz,\; xy^2z,\; xyz^2)$ 的散度。

**解**：
$P = x^2yz,\; Q = xy^2z,\; R = xyz^2$
$$
\nabla \cdot \vec{A} = \frac{\partial P}{\partial x} + \frac{\partial Q}{\partial y} + \frac{\partial R}{\partial z}
= 2xyz + 2xyz + 2xyz = 6xyz
$$

---

> 📋 **小结**：梯度、散度、旋度、方向导数是向量微积分的四大核心工具，贯穿多元函数微分学与场论。复习时应做到：
> 1. 能熟练计算方向导数（注意单位化方向向量）
> 2. 理解梯度的几何意义（最大增长方向、等值面的法线）
> 3. 能计算散度与旋度，理解其物理含义（源/汇的强度、涡旋强度）
> 4. 掌握旋度定理（斯托克斯公式）与散度定理（高斯公式）的联系
> 5. 将梯度与本章 §七 中的切平面、法线知识点串联理解