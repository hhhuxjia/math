# 考研数学一 · 多元函数微分学专题整理

---

# 一、基本概念

## 1. 多元函数

- **定义**：$z = f(x,y)$，$(x,y) \in D \subset \mathbb{R}^2$，将 $\mathbb{R}^2$ 中的点映射到 $\mathbb{R}$
- **$n$ 元函数**：$u = f(x_1, x_2, \cdots, x_n)$

## 2. 邻域与内点

- **$\delta$ 邻域**：$U(P_0, \delta) = \{ (x,y) \mid \sqrt{(x-x_0)^2 + (y-y_0)^2} < \delta \}$
- **内点**：存在邻域完全包含在 $D$ 内的点
- **边界点**：任意邻域既含 $D$ 中的点也含非 $D$ 中的点
- **开集**：所有点都是内点的集合
- **闭集**：包含所有边界点的集合
- **有界集**：可以包含在一个有限半径圆盘内的集合

## 3. 二元函数的极限

$$
\lim_{\substack{x\to x_0 \\ y\to y_0}} f(x,y) = A
$$

**定义**：$\forall \varepsilon > 0$，$\exists \delta > 0$，当 $0 < \sqrt{(x-x_0)^2 + (y-y_0)^2} < \delta$ 时，$|f(x,y) - A| < \varepsilon$

> ⚠️ **二重极限与累次极限的区别**：
> - 二重极限：$(x,y)$ 以**任何方式**趋近 $(x_0,y_0)$ 时极限都存在且相等
> - 累次极限：$\lim_{x\to x_0}\lim_{y\to y_0} f(x,y)$ 或 $\lim_{y\to y_0}\lim_{x\to x_0} f(x,y)$
> - 二重极限存在 $\nRightarrow$ 累次极限存在
> - 累次极限存在且相等 $\nRightarrow$ 二重极限存在

**证明极限不存在的方法**：
1. 沿不同路径趋近，得到不同极限值
2. 常用路径：$y = kx$，$y = kx^2$，$y = x^2$ 等

**例**：证明 $\displaystyle\lim_{(x,y)\to(0,0)} \frac{xy}{x^2 + y^2}$ 不存在

> 沿 $y=kx$：$\displaystyle\frac{x\cdot kx}{x^2 + k^2x^2} = \frac{k}{1+k^2}$，极限值随 $k$ 变化 $\Rightarrow$ 不存在

---

# 二、偏导数

## 1. 偏导数的定义

### 二元函数

$$
f_x(x_0,y_0) = \left.\frac{\partial f}{\partial x}\right|_{(x_0,y_0)} = \lim_{\Delta x\to 0} \frac{f(x_0+\Delta x, y_0) - f(x_0,y_0)}{\Delta x}
$$

$$
f_y(x_0,y_0) = \left.\frac{\partial f}{\partial y}\right|_{(x_0,y_0)} = \lim_{\Delta y\to 0} \frac{f(x_0, y_0+\Delta y) - f(x_0,y_0)}{\Delta y}
$$

**核心思想**：偏导数是**一元函数的导数**——对 $x$ 求偏导时，将 $y$ 视为常数；对 $y$ 求偏导时，将 $x$ 视为常数。

### $n$ 元函数

对 $x_i$ 的偏导数：

$$
\frac{\partial f}{\partial x_i}(x_1,\cdots,x_n) = \lim_{h\to 0} \frac{f(x_1,\cdots,x_i+h,\cdots,x_n) - f(x_1,\cdots,x_n)}{h}
$$

## 2. 高阶偏导数

### 二阶偏导数

| 记号 | 表达式 |
|:---|:---|
| $f_{xx} = \dfrac{\partial^2 f}{\partial x^2}$ | $\dfrac{\partial}{\partial x}\left(\dfrac{\partial f}{\partial x}\right)$ |
| $f_{yy} = \dfrac{\partial^2 f}{\partial y^2}$ | $\dfrac{\partial}{\partial y}\left(\dfrac{\partial f}{\partial y}\right)$ |
| $f_{xy} = \dfrac{\partial^2 f}{\partial y\partial x}$ | $\dfrac{\partial}{\partial y}\left(\dfrac{\partial f}{\partial x}\right)$ |
| $f_{yx} = \dfrac{\partial^2 f}{\partial x\partial y}$ | $\dfrac{\partial}{\partial x}\left(\dfrac{\partial f}{\partial y}\right)$ |

### 混合偏导数相等的条件（克莱罗定理）

**定理**：若 $f_{xy}$ 和 $f_{yx}$ 在点 $(x_0,y_0)$ 的某邻域内连续，则：

$$
f_{xy}(x_0,y_0) = f_{yx}(x_0,y_0)
$$

即**混合偏导数与求导次序无关**。

> ⚠️ 若不连续，则混合偏导数可能不相等。例如 $f(x,y) = \begin{cases} \dfrac{xy(x^2-y^2)}{x^2+y^2}, & (x,y)\neq(0,0) \\ 0, & (x,y)=(0,0) \end{cases}$ 在 $(0,0)$ 处 $f_{xy} \neq f_{yx}$。

---

# 三、全微分（核心定义）

## 1. 全增量与全微分

### 全增量 $\Delta z$

函数 $z = f(x,y)$ 在点 $(x_0,y_0)$ 处给自变量增量 $\Delta x, \Delta y$ 后，函数值的**实际变化量**：

$$
\boxed{\Delta z = f(x_0 + \Delta x, y_0 + \Delta y) - f(x_0,y_0)}
$$

### 全微分 $dz$（线性主部）

若 $\Delta z$ 可以表示为以下形式：

$$
\boxed{\Delta z = A\Delta x + B\Delta y + o(\rho)}
$$

其中：
- $A, B$ 是与 $\Delta x, \Delta y$ **无关**的常数（只与点 $(x_0,y_0)$ 有关）
- $\rho = \sqrt{(\Delta x)^2 + (\Delta y)^2}$ 是点 $(x_0+\Delta x, y_0+\Delta y)$ 到 $(x_0,y_0)$ 的**距离**
- $o(\rho)$ 是比 $\rho$ 高阶的无穷小，即 $\displaystyle \lim_{\rho \to 0} \frac{o(\rho)}{\rho} = 0$

则称 $f$ 在 $(x_0,y_0)$ 处**可微**（可全微分），并称 $A\Delta x + B\Delta y$ 为 $f$ 在 $(x_0,y_0)$ 处的**全微分**，记作：

$$
\boxed{dz = A\,dx + B\,dy}
$$

其中 $dx = \Delta x$，$dy = \Delta y$（自变量的微分等于增量）。

### $\Delta z$ 与 $dz$ 的关系

$$
\Delta z = dz + o(\rho)
$$

- $dz$：$\Delta z$ 的**线性主部**（用切平面近似的线性部分）
- $o(\rho)$：$\Delta z$ 的**高阶无穷小误差**（当 $\rho$ 很小时可忽略）

> **一元函数类比**：
> - 一元：$\Delta y = f'(x_0)\Delta x + o(\Delta x)$，$dy = f'(x_0)dx$
> - 二元：$\Delta z = f_x\Delta x + f_y\Delta y + o(\rho)$，$dz = f_x dx + f_y dy$
> 
> 一元是线性函数近似，二元是切平面近似。

## 2. $o(\rho)$ 的详细解读

### 定义回顾

$o(\rho)$ 表示"比 $\rho$ 高阶的无穷小"，严格定义为：

$$
\lim_{\rho \to 0} \frac{o(\rho)}{\rho} = 0
$$

### 常见形式举例

| $o(\rho)$ 的形式 | 是否 $o(\rho)$ | 原因 |
|:---|:---:|:---|
| $\Delta x \cdot \Delta y$ | $\checkmark$ | $\dfrac{|\Delta x \Delta y|}{\rho} \le \dfrac{\rho^2}{\rho} = \rho \to 0$ |
| $(\Delta x)^2$ | $\checkmark$ | $\dfrac{(\Delta x)^2}{\rho} \le \dfrac{\rho^2}{\rho} = \rho \to 0$ |
| $(\Delta y)^2$ | $\checkmark$ | 同上 |
| $(\Delta x)^2 + (\Delta y)^2$ | $\checkmark$ | $\dfrac{\rho^2}{\rho} = \rho \to 0$ |
| $\dfrac{(\Delta x)^3}{(\Delta x)^2 + (\Delta y)^2}$ | $\times$ | 沿 $\Delta y = 0$ 时极限为 $0$，但沿 $\Delta x = \Delta y$ 时 $\dfrac{(\Delta x)^3}{2(\Delta x)^2} \cdot \dfrac{1}{|\Delta x|\sqrt{2}} \to \text{常数}$ |
| $\sqrt{(\Delta x)^3 + (\Delta y)^3}$ | $\checkmark$ | 分子阶数为 $3/2 > 1$ |
| $\dfrac{\Delta x \cdot \Delta y}{\sqrt{(\Delta x)^2 + (\Delta y)^2}}$ | $\times$ | 分子 $\dfrac{\rho^2}{\rho} = \rho$，比值极限不为 $0$ |

### 判断 $o(\rho)$ 的核心方法

将 $\Delta x = \rho\cos\theta$，$\Delta y = \rho\sin\theta$ 代入，看表达式中 $\rho$ 的最低幂次：

$$
o(\rho) \text{ 的条件：分子中 } \rho \text{ 的幂次 } > 1
$$

## 3. 用定义判断可微的完整步骤

判断 $f(x,y)$ 在 $(x_0,y_0)$ 处是否可微的标准流程：

```
Step 1: 先求 f_x(x_0,y_0) 和 f_y(x_0,y_0)
        ├── 若任意一个不存在 → 不可微（结束）
        └── 若都存在 → 继续 Step 2

Step 2: 构造差值
        ε = Δz - [f_x(x_0,y_0)·Δx + f_y(x_0,y_0)·Δy]

Step 3: 判断 ε 是否为 o(ρ)，即计算：
        lim_{ρ→0} |ε| / ρ = 0 ?
        ├── 极限 = 0 → 可微 ✓
        ├── 极限 ≠ 0 → 不可微 ✗
        └── 极限不存在 → 不可微 ✗
```

**经典例题**：判断 $f(x,y) = \begin{cases} \dfrac{xy}{\sqrt{x^2 + y^2}}, & (x,y) \neq (0,0) \\ 0, & (0,0) \end{cases}$ 在 $(0,0)$ 处的可微性。

> **解**：
> 1. $f_x(0,0) = \displaystyle\lim_{\Delta x\to 0} \frac{0 - 0}{\Delta x} = 0$，同理 $f_y(0,0) = 0$
> 2. $\varepsilon = \Delta z - [0\cdot\Delta x + 0\cdot\Delta y] = \dfrac{\Delta x \Delta y}{\sqrt{\Delta x^2 + \Delta y^2}}$
> 3. 令 $\Delta x = \rho\cos\theta$，$\Delta y = \rho\sin\theta$：
>    $\displaystyle \frac{|\varepsilon|}{\rho} = \frac{|\rho^2 \cos\theta \sin\theta / \rho|}{\rho} = |\cos\theta\sin\theta|$
>    取 $\theta = \frac{\pi}{4}$ 时，值为 $\frac12 \neq 0$，且不随 $\rho\to 0$ 趋近于 $0$
> 4. 故**不可微** $\times$

## 4. 可微的必要条件与充分条件

### 必要条件

若 $f$ 在 $(x_0,y_0)$ 处可微，则：
1. $f$ 在 $(x_0,y_0)$ 处**连续**
2. $f$ 在 $(x_0,y_0)$ 处**偏导数存在**，且 $A = f_x(x_0,y_0)$，$B = f_y(x_0,y_0)$

> 即：可微 $\Rightarrow$ 连续，可微 $\Rightarrow$ 偏导存在

### 充分条件

若 $f_x$ 和 $f_y$ 在 $(x_0,y_0)$ 的某邻域内**存在且连续**，则 $f$ 在 $(x_0,y_0)$ 处可微。

> 即：偏导连续 $\Rightarrow$ 可微
> 
> 注意这是**充分条件**而非必要条件，可微不要求偏导连续。

### 可微的充要条件（等价定义）

$f$ 在 $(x_0,y_0)$ 处可微 $\Longleftrightarrow$ 

$$
\lim_{\substack{\Delta x\to 0 \\ \Delta y\to 0}} \frac{|f(x_0+\Delta x, y_0+\Delta y) - f(x_0,y_0) - f_x(x_0,y_0)\Delta x - f_y(x_0,y_0)\Delta y|}{\sqrt{\Delta x^2 + \Delta y^2}} = 0
$$

## 5. 全微分与偏导数的几何意义

- $f_x(x_0,y_0)$：曲面 $z=f(x,y)$ 被平面 $y=y_0$ 所截曲线在 $x=x_0$ 处的切线斜率
- $f_y(x_0,y_0)$：曲面 $z=f(x,y)$ 被平面 $x=x_0$ 所截曲线在 $y=y_0$ 处的切线斜率
- $dz = f_x dx + f_y dy$：切平面上竖坐标的增量（线性近似部分）
- $\Delta z - dz = o(\rho)$：曲面与切平面之间的**误差**，是 $\rho$ 的高阶无穷小

### $\Delta z$、$dz$、$\rho$ 的关系图示

```
         曲面 z = f(x,y)
           /|
          / | Δz (实际增量)
         /  |
  切平面 ----/
        \__|
      dz = f_xΔx + f_yΔy (线性近似)
       
  Δz - dz = o(ρ)  → 0 (当 ρ → 0)
```

## 6. 全微分的叠加原理

若 $u = f(x,y,z)$ 可微，则：

$$
du = \frac{\partial f}{\partial x}dx + \frac{\partial f}{\partial y}dy + \frac{\partial f}{\partial z}dz
$$

## 7. 全微分的运算法则

| 运算 | 微分法则 |
|:---|:---|
| 线性组合 | $d(c_1u + c_2v) = c_1\,du + c_2\,dv$ |
| 乘积 | $d(uv) = v\,du + u\,dv$ |
| 商 | $d\left(\dfrac{u}{v}\right) = \dfrac{v\,du - u\,dv}{v^2}$ |
| 复合（链式） | $d(f(u,v)) = f_u\,du + f_v\,dv$（形式不变性） |

---

# 四、连续性、偏导存在、可微的关系

## 1. 关系总图

```
偏导数连续存在（C¹类）
      ↓  (充分条件)
       可微
      ↙         ↘
 偏导数存在     连续
      ↘         ↙
        极限存在
```

### 核心结论

| 条件 | 结论 | 逆命题 |
|:---|:---|:---:|
| 可微 | $\Rightarrow$ 连续 | $\nLeftarrow$ |
| 可微 | $\Rightarrow$ 偏导存在 | $\nLeftarrow$ |
| 偏导连续 | $\Rightarrow$ 可微 | $\nLeftarrow$ |
| 偏导存在 | $\nRightarrow$ 连续 | — |
| 连续 | $\nRightarrow$ 偏导存在 | — |
| 偏导存在 | $\nRightarrow$ 可微 | — |

## 2. 反例汇总

### (1) 连续但偏导不存在

**例**：$f(x,y) = \sqrt{x^2 + y^2}$，在 $(0,0)$ 处连续，但 $\frac{\partial f}{\partial x}(0,0)$ 不存在

> 沿 $y=0$ 时 $f(x,0)=|x|$，在 $x=0$ 处不可导

### (2) 偏导存在但不连续

**例**：$f(x,y) = \begin{cases} \dfrac{xy}{x^2 + y^2}, & (x,y)\neq(0,0) \\ 0, & (x,y)=(0,0) \end{cases}$

> $f_x(0,0) = f_y(0,0) = 0$，但沿 $y=x$ 时极限为 $\frac12 \neq 0$，故不连续

### (3) 偏导存在但不可微

**例**：$f(x,y) = \begin{cases} \dfrac{xy}{\sqrt{x^2 + y^2}}, & (x,y)\neq(0,0) \\ 0, & (x,y)=(0,0) \end{cases}$

> $f_x(0,0) = f_y(0,0) = 0$，但 $\Delta z - [f_x\Delta x + f_y\Delta y] = \frac{\Delta x \Delta y}{\sqrt{\Delta x^2 + \Delta y^2}}$ 不是 $o(\rho)$，故不可微

### (4) 可微但偏导不连续

**例**：$f(x,y) = \begin{cases} (x^2 + y^2)\sin\dfrac{1}{\sqrt{x^2 + y^2}}, & (x,y)\neq(0,0) \\ 0, & (x,y)=(0,0) \end{cases}$

> 在 $(0,0)$ 处可微（$df = 0$），但 $f_x$ 和 $f_y$ 在 $(0,0)$ 处不连续

## 3. 判断可微的步骤

```
Step 1: 检查偏导数是否连续        → 若连续，则可微（充分条件）
Step 2: 若偏导不连续，用定义判断  → 
  lim_{(Δx,Δy)→(0,0)} [f(x₀+Δx,y₀+Δy)-f(x₀,y₀)-f_xΔx-f_yΔy] / √(Δx²+Δy²) = 0 ?
Step 3: 若该极限为 0 则可微，否则不可微
```

---

# 五、复合函数求导（链式法则）

## 1. 基本链式法则

### 情形一：$z = f(u,v)$，$u = u(x,y)$，$v = v(x,y)$

$$
\frac{\partial z}{\partial x} = \frac{\partial f}{\partial u}\cdot\frac{\partial u}{\partial x} + \frac{\partial f}{\partial v}\cdot\frac{\partial v}{\partial x}
$$

$$
\frac{\partial z}{\partial y} = \frac{\partial f}{\partial u}\cdot\frac{\partial u}{\partial y} + \frac{\partial f}{\partial v}\cdot\frac{\partial v}{\partial y}
$$

### 情形二：$z = f(u)$，$u = u(x,y)$

$$
\frac{\partial z}{\partial x} = f'(u)\cdot\frac{\partial u}{\partial x}, \quad
\frac{\partial z}{\partial y} = f'(u)\cdot\frac{\partial u}{\partial y}
$$

### 情形三：$z = f(x,y,u,v)$，$u = u(x,y)$，$v = v(x,y)$

$$
\frac{\partial z}{\partial x} = \frac{\partial f}{\partial x} + \frac{\partial f}{\partial u}\cdot\frac{\partial u}{\partial x} + \frac{\partial f}{\partial v}\cdot\frac{\partial v}{\partial x}
$$

$$
\frac{\partial z}{\partial y} = \frac{\partial f}{\partial y} + \frac{\partial f}{\partial u}\cdot\frac{\partial u}{\partial y} + \frac{\partial f}{\partial v}\cdot\frac{\partial v}{\partial y}
$$

> ⚠️ $f$ 中既有直接依赖 $x,y$ 的部分（$\frac{\partial f}{\partial x}$），又有通过中间变量依赖 $x,y$ 的部分（链式项）

## 2. 全导数

若 $z = f(x,y)$，$y = y(x)$，则：

$$
\frac{dz}{dx} = \frac{\partial f}{\partial x} + \frac{\partial f}{\partial y}\cdot\frac{dy}{dx}
$$

**全导数**与**偏导数**的区别：全导数考虑所有依赖关系，偏导数只考虑直接依赖。

## 3. 一阶全微分形式不变性

若 $z = f(u,v)$ 可微，$u,v$ 是自变量或中间变量，都有：

$$
dz = \frac{\partial z}{\partial u}\,du + \frac{\partial z}{\partial v}\,dv
$$

> **应用**：在复合求导时，可以先微分再比微商，避免漏项。

## 4. 树形图法

以 $z = f(u,v)$，$u = \varphi(x,y)$，$v = \psi(x,y)$ 为例：

```
        z
       / \
      u   v
     / \ / \
    x  y x  y
```

- $\dfrac{\partial z}{\partial x}$ 看从 $z$ 到 $x$ 的所有路径：$z\to u\to x$ 和 $z\to v\to x$
- 每条路径导函数相乘，不同路径相加

---

# 六、隐函数定理与隐函数求导

## 1. 隐函数存在定理

### 一个方程的情形

**定理**：设 $F(x,y)$ 满足：
1. $F(x_0,y_0) = 0$
2. $F_y(x_0,y_0) \neq 0$
3. $F_x, F_y$ 在 $(x_0,y_0)$ 的某邻域内连续

则存在唯一可微函数 $y = y(x)$，满足 $y_0 = y(x_0)$ 且 $F(x,y(x)) \equiv 0$，且：

$$
\boxed{\frac{dy}{dx} = -\frac{F_x}{F_y}}
$$

### 两个自变量的情形

若 $F(x,y,z) = 0$ 确定隐函数 $z = z(x,y)$，则：

$$
\boxed{\frac{\partial z}{\partial x} = -\frac{F_x}{F_z}, \quad \frac{\partial z}{\partial y} = -\frac{F_y}{F_z}}
$$

> ⚠️ 分母 $F_z \neq 0$ 是隐函数存在的关键条件

### 方程组的情形

$$
\begin{cases}
F(x,y,u,v) = 0 \\
G(x,y,u,v) = 0
\end{cases}
$$

确定隐函数 $u = u(x,y)$，$v = v(x,y)$。

对方程组两边分别对 $x$ 求偏导，联立解出 $\dfrac{\partial u}{\partial x}$，$\dfrac{\partial v}{\partial x}$。

**雅可比行列式**：

$$
\frac{\partial(F,G)}{\partial(u,v)} = \begin{vmatrix}
F_u & F_v \\
G_u & G_v
\end{vmatrix} \neq 0
$$

是方程组隐函数存在的条件。

## 2. 隐函数的高阶导数

对 $\frac{dy}{dx} = -\frac{F_x}{F_y}$ 再次求导，可得 $y''$（用 $F_x,F_y,F_{xx},F_{xy},F_{yy}$ 表示）。

> **技巧**：直接在 $F(x,y)=0$ 两边对 $x$ 求导（视 $y$ 为 $x$ 的函数），反复求导即可得各阶导数。

## 3. 隐函数存在定理的直观理解

- $F_y(x_0,y_0) \neq 0$：在 $(x_0,y_0)$ 处，曲面 $z=F(x,y)$ 与 $z=0$ 平面相交的曲线在 $y$ 方向上"不水平"，能解出 $y$
- 若 $F_y = 0$ 且 $F_x \neq 0$，则可解出 $x = x(y)$

---

# 七、方向导数与梯度

## 1. 方向导数

### 定义

函数 $z = f(x,y)$ 沿方向 $\mathbf{l} = (\cos\alpha, \cos\beta)$ 的方向导数：

$$
\frac{\partial f}{\partial \mathbf{l}}(x_0,y_0) = \lim_{t\to 0^+} \frac{f(x_0 + t\cos\alpha, y_0 + t\cos\beta) - f(x_0,y_0)}{t}
$$

### 计算公式

若 $f$ 在 $(x_0,y_0)$ 处可微，则：

$$
\boxed{\frac{\partial f}{\partial \mathbf{l}} = \frac{\partial f}{\partial x}\cos\alpha + \frac{\partial f}{\partial y}\cos\beta = \nabla f \cdot \mathbf{l}^0}
$$

其中 $\mathbf{l}^0 = (\cos\alpha, \cos\beta)$ 为单位方向向量。

对于三元函数 $u = f(x,y,z)$：

$$
\frac{\partial f}{\partial \mathbf{l}} = \frac{\partial f}{\partial x}\cos\alpha + \frac{\partial f}{\partial y}\cos\beta + \frac{\partial f}{\partial z}\cos\gamma
$$

## 2. 梯度

### 定义

梯度是一个向量，指向函数值增加最快的方向：

$$
\nabla f = \left( \frac{\partial f}{\partial x}, \frac{\partial f}{\partial y} \right) \quad (\text{二元})
$$

$$
\nabla f = \left( \frac{\partial f}{\partial x}, \frac{\partial f}{\partial y}, \frac{\partial f}{\partial z} \right) \quad (\text{三元})
$$

### 梯度的性质

- 方向导数 = 梯度在该方向上的投影：$\dfrac{\partial f}{\partial \mathbf{l}} = \nabla f \cdot \mathbf{l}^0$
- 梯度方向是函数值**增加最快**的方向
- 梯度方向的反方向是函数值**减小最快**的方向
- 梯度的模 $|\nabla f|$ 等于方向导数的最大值
- 与梯度垂直的方向上，方向导数为 $0$（等高线的切线方向）

### 梯度与等值线/等值面

- 在 $f(x,y) = C$ 上，梯度 $\nabla f$ 与等值线垂直
- 在 $f(x,y,z) = C$ 上，梯度 $\nabla f$ 与等值面的切平面垂直（即等值面的**法向量**）

---

# 八、多元函数极值

## 1. 极值的定义

设 $f(x,y)$ 在 $(x_0,y_0)$ 的某邻域内有定义，若对该邻域内任意点 $(x,y)$ 有：

- $f(x,y) \le f(x_0,y_0)$，则称 $(x_0,y_0)$ 为**极大值点**
- $f(x,y) \ge f(x_0,y_0)$，则称 $(x_0,y_0)$ 为**极小值点**

## 2. 极值的必要条件

### 驻点

若 $f$ 在 $(x_0,y_0)$ 处偏导数存在且取极值，则：

$$
\boxed{f_x(x_0,y_0) = 0, \quad f_y(x_0,y_0) = 0}
$$

满足上述条件的点称为**驻点**（或**临界点**）。

> ⚠️ 驻点 $\nRightarrow$ 极值点（如鞍点）
> ⚠️ 偏导数不存在的点也可能取极值（如 $f(x,y) = \sqrt{x^2 + y^2}$ 在 $(0,0)$ 取极小值）

### 极值点可能的类型

1. **驻点**（$f_x = f_y = 0$）
2. **偏导不存在的点**（如尖点）

## 3. 极值的充分条件（二阶判别法）

**定理**：设 $(x_0,y_0)$ 是驻点，$f$ 在 $(x_0,y_0)$ 的某邻域内有二阶连续偏导数。记：

$$
A = f_{xx}(x_0,y_0), \quad B = f_{xy}(x_0,y_0), \quad C = f_{yy}(x_0,y_0)
$$

则判别式 $\Delta = AC - B^2$：

| $\Delta$ | 条件 | 结论 |
|:---|:---|:---|
| $\Delta > 0$ | $A > 0$ | 极小值点 |
| $\Delta > 0$ | $A < 0$ | 极大值点 |
| $\Delta < 0$ | — | 鞍点（非极值点） |
| $\Delta = 0$ | — | 需进一步判断（高阶判别法或直接定义） |

> **记忆口诀**：$\Delta > 0$ 是极值，$A$ 正极小 $A$ 负大；$\Delta < 0$ 是鞍点，$\Delta = 0$ 需另判

## 4. 条件极值与拉格朗日乘数法

### 一个约束条件

求 $f(x,y)$ 在约束条件 $g(x,y) = 0$ 下的极值。

**拉格朗日乘数法**：

1. 构造拉格朗日函数：
   $$
   L(x,y,\lambda) = f(x,y) + \lambda g(x,y)
   $$

2. 解方程组：
   $$
   \begin{cases}
   L_x = f_x + \lambda g_x = 0 \\
   L_y = f_y + \lambda g_y = 0 \\
   L_\lambda = g(x,y) = 0
   \end{cases}
   $$

3. 求出所有可能的极值点 $(x,y)$

### 两个约束条件

求 $f(x,y,z)$ 在 $g(x,y,z) = 0$，$h(x,y,z) = 0$ 下的极值。

**拉格朗日函数**：
$$
L(x,y,z,\lambda,\mu) = f(x,y,z) + \lambda g(x,y,z) + \mu h(x,y,z)
$$

解 $\begin{cases} L_x = L_y = L_z = 0 \\ g = 0 \\ h = 0 \end{cases}$ 得可能极值点。

### 判定条件极值的常用方法

1. **几何法**：利用约束条件消去一个变量，转化为无条件极值
2. **拉格朗日乘数法**：直接求解方程组
3. **不等式法**：利用柯西-施瓦茨、均值不等式等（适用于特定形式）
4. **二次型法**：分析 $d^2L$ 的符号

> ⚠️ 拉格朗日乘数法只给出**可能的极值点**，是否真的是极值需结合实际意义判断（在考研中通常就是极值点）

---

# 九、最值问题

## 1. 闭区域上连续函数的最值

### 步骤

```
Step 1: 求 f 在区域内部的驻点（f_x = f_y = 0）
Step 2: 求 f 在区域边界上的最值（化为条件极值或参数方程）
Step 3: 比较所有候选点的函数值，最大即为最大值，最小即为最小值
```

### 边界处理

- 边界为曲线 $g(x,y) = 0$：使用拉格朗日乘数法
- 边界为多条曲线（如矩形边界）：分段处理，每条边代入化为单变量函数

## 2. 实际问题建模

**步骤**：
1. 确定目标函数 $f$（需最大/最小的量）
2. 确定约束条件（若有）
3. 选择合适的求解方法（无条件极值 / 条件极值）
4. 根据实际意义判别极值类型

> **常见几何应用**：
> - 曲面上的最短路程
> - 最大体积 / 最小表面积
> - 点到曲面的最短距离

---

# 十、常见题型分类

## 题型一：判断极限是否存在

**例**：判断 $\displaystyle\lim_{(x,y)\to(0,0)} \frac{x^2y}{x^4 + y^2}$ 是否存在

> 沿 $y = kx^2$：$\dfrac{x^2\cdot kx^2}{x^4 + k^2x^4} = \dfrac{k}{1+k^2}$，随 $k$ 变 $\Rightarrow$ 不存在

## 题型二：偏导数的计算

**例**：设 $z = e^{xy}\sin(x+y)$，求 $\dfrac{\partial z}{\partial x}$，$\dfrac{\partial z}{\partial y}$

> $\dfrac{\partial z}{\partial x} = ye^{xy}\sin(x+y) + e^{xy}\cos(x+y)$

## 题型三：判断可微性

**例**：判断 $f(x,y) = \begin{cases} \dfrac{x^2y}{x^2 + y^2}, & (x,y)\neq(0,0) \\ 0, & (x,y)=(0,0) \end{cases}$ 在 $(0,0)$ 处的可微性

> 先求 $f_x(0,0) = 0$，$f_y(0,0) = 0$，再验证 $\displaystyle\lim_{\rho\to 0} \frac{|\Delta f|}{\rho} = 0$ 是否成立

## 题型四：复合函数求导

**例**：设 $z = f(xy, x-y)$，$f$ 具有二阶连续偏导数，求 $\dfrac{\partial^2 z}{\partial x^2}$

> 令 $u = xy$，$v = x-y$，使用链式法则逐层求导

## 题型五：隐函数求导

**例**：设 $z = z(x,y)$ 由 $F(xz, yz) = 0$ 确定，求 $\dfrac{\partial z}{\partial x}$，$\dfrac{\partial z}{\partial y}$

> 对 $F(xz, yz) = 0$ 两边分别对 $x,y$ 求偏导，注意 $z$ 是 $x,y$ 的函数

## 题型六：方向导数与梯度

**例**：求 $f(x,y) = \ln(x^2 + y^2)$ 在点 $(1,1)$ 处沿方向 $(-1,2)$ 的方向导数

> 先求梯度 $\nabla f(1,1)$，再与方向单位向量点乘

## 题型七：无条件极值

**例**：求 $f(x,y) = x^3 - 3xy + y^3$ 的极值

> 求驻点 $f_x = f_y = 0$，再用 $\Delta = AC - B^2$ 判断

## 题型八：条件极值（拉格朗日乘数法）

**例**：在 $x^2 + y^2 = 1$ 条件下，求 $f(x,y) = xy$ 的最值

> 构造 $L = xy + \lambda(x^2 + y^2 - 1)$，解方程组得 $(\pm\frac{1}{\sqrt{2}},\pm\frac{1}{\sqrt{2}})$ 和 $(\pm\frac{1}{\sqrt{2}},\mp\frac{1}{\sqrt{2}})$

## 题型九：实际应用最值

**例**：做一个容积为 $V$ 的无盖长方体水箱，问长宽高各为多少时表面积最小？

> 设长 $x$，宽 $y$，高 $z$，约束 $xyz = V$，目标 $S = xy + 2xz + 2yz$，用拉格朗日乘数法

---

# 十一、全微分方程与积分因子

（此部分已在微分方程专题中详述，此处仅作关联提示）

若 $P(x,y)dx + Q(x,y)dy = 0$ 满足 $\dfrac{\partial P}{\partial y} = \dfrac{\partial Q}{\partial x}$，则为全微分方程，存在 $u(x,y)$ 使 $du = Pdx + Qdy$。

---

# 十二、泰勒公式（多元）

## 二元函数的泰勒展开

设 $f(x,y)$ 在 $(x_0,y_0)$ 的某邻域内有 $n+1$ 阶连续偏导数，则：

$$
\begin{aligned}
f(x_0+\Delta x, y_0+\Delta y) &= f(x_0,y_0) \\
&+ \left( \Delta x\frac{\partial}{\partial x} + \Delta y\frac{\partial}{\partial y} \right) f \Big|_{(x_0,y_0)} \\
&+ \frac{1}{2!} \left( \Delta x\frac{\partial}{\partial x} + \Delta y\frac{\partial}{\partial y} \right)^2 f \Big|_{(x_0,y_0)} \\
&+ \cdots \\
&+ \frac{1}{n!} \left( \Delta x\frac{\partial}{\partial x} + \Delta y\frac{\partial}{\partial y} \right)^n f \Big|_{(x_0,y_0)} \\
&+ R_n
\end{aligned}
$$

### 二阶泰勒公式（常用）

$$
f(x_0+\Delta x, y_0+\Delta y) = f(x_0,y_0) + f_x\Delta x + f_y\Delta y + \frac12(f_{xx}\Delta x^2 + 2f_{xy}\Delta x\Delta y + f_{yy}\Delta y^2) + o(\rho^2)
$$

> **应用**：用二阶泰勒公式可以判断极值类型（$d^2f$ 的正定性对应极小/极大）

---

# 十三、知识框架图

```
多元函数微分学
│
├── 基本概念
│   ├── 多元函数
│   ├── 二重极限（路径相关！）
│   └── 累次极限 vs 二重极限
│
├── 偏导数
│   ├── 一阶偏导（定义与计算）
│   ├── 高阶偏导
│   └── 混合偏导相等条件（克莱罗定理）
│
├── 全微分
│   ├── 定义（线性主部）
│   ├── 可微的必要条件（偏导存在 + 连续）
│   ├── 可微的充分条件（偏导连续）
│   └── 关系图：连续 ↔ 偏导存在 ↔ 可微
│
├── 求导法则
│   ├── 复合函数求导（链式法则 + 树形图）
│   ├── 隐函数求导（F(x,y)=0 → dy/dx = -F_x/F_y）
│   └── 全微分形式不变性
│
├── 方向导数与梯度
│   ├── 方向导数 = ∇f·l⁰
│   ├── 梯度方向 = 增长最快方向
│   └── 梯度与等值线垂直
│
├── 极值与最值
│   ├── 无条件极值（驻点 + AC-B² 判别）
│   ├── 条件极值（拉格朗日乘数法）
│   ├── 闭区域最值（内部 + 边界）
│   └── 实际应用建模
│
└── 多元泰勒公式
    ├── 展开形式（算子表示）
    └── 二阶泰勒与极值判别
```

---

# 十四、常用记忆口诀

> **偏导计算把常数看，二元变一元**
> **可微定义增量拆，线性主部加高阶小**
> **连续偏导若存在，可微一定有保证**
> **隐函数求导公式好，F_x 除以 F_y 加负号**
> **链式法则树形图，路径相乘再相加**
> **梯度指向增最快，与等值线垂直来**
> **极值判别 AC-B²，大于 0 时看 A 正负**
> **拉格朗日乘数法，条件极值全靠它**

---

# 十五、易错点总结

1. **二重极限**：沿不同路径趋近极限可能不同，不可只验证一条路径
2. **偏导存在 ≠ 连续**：典型反例 $f(x,y) = \dfrac{xy}{x^2+y^2}$
3. **可微 ≠ 偏导连续**：如例子 $(x^2+y^2)\sin(1/\sqrt{x^2+y^2})$
4. **链式法则**：注意 $f$ 中间接依赖 $x$ 的部分和直接依赖 $x$ 的部分
5. **隐函数求导**：分母不为 0 的条件不能忽略
6. **拉格朗日乘数法**：给出的只是**可能极值点**，需结合题意判断
7. **全微分**：$\Delta z$ 中的 $o(\rho)$ 必须除以 $\rho$ 后极限为 0 才可微
8. **方向导数公式**：只适用于函数可微的情况；若不可微，需要用定义
9. **二阶判别法**：$\Delta = 0$ 时不能下结论，需用更高阶导数或定义
10. **约束条件的极值**：不要忘了边界点也可能取最值
11. **混合偏导次序**：克莱罗定理成立的前提是二阶混合偏导连续
