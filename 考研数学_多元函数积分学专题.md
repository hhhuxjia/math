# 考研数学一 · 多元函数积分学专题（完整版）

---

# 预备知识：空间曲面的识别、方程与作图

线面积分题中，最容易卡住的往往不是积分本身，而是题目给出的曲面 $\Sigma$ 不会认、不会画，也不知道该向哪个坐标面投影。本章先把这类几何对象统一成“轴向坐标 + 到轴的距离”，再进入后面的积分公式。

## 1. 三种表示方式：方程、参数、投影

同一个曲面可以有三种常用写法：

| 写法 | 适合做什么 | 典型形式 |
|:---|:---|:---|
| 隐式方程 | 判断形状、求交线、写边界 | $F(x,y,z)=0$ |
| 显函数 | 投影法计算曲面积分 | $z=z(x,y)$、$y=y(z,x)$、$x=x(y,z)$ |
| 参数方程 | 旋转曲面、复杂曲面、直接求法向量 | $\mathbf r(u,v)=(x(u,v),y(u,v),z(u,v))$ |

> **原则**：先用隐式方程认形状，再根据积分要求改写成显式或参数形式。曲面不是函数时，不要强行写成单个 $z=z(x,y)$；分片或参数化通常更稳。

## 2. 旋转曲面的统一方法

### 2.1 绕坐标轴旋转：把“半径”平方代入

设平面内的母线位于含旋转轴的平面内。旋转后，母线到轴的距离变成圆周半径，因此只需把母线中的“横向距离”替换成到轴距离：

| 旋转轴 | 母线所在平面中的关系 | 旋转曲面方程 |
|:---|:---|:---|
| $x$ 轴 | $y=f(x)$ 或 $x=\varphi(y)$ | $y^2+z^2=f(x)^2$（或 $y^2+z^2=\varphi^{-1}(x)^2$） |
| $y$ 轴 | $x=f(y)$ | $x^2+z^2=f(y)^2$ |
| $z$ 轴 | $x=f(z)$（母线在 $xOz$ 平面） | $x^2+y^2=f(z)^2$ |

更一般地，若母线在 $xOz$ 平面上满足 $G(x,z)=0$，绕 $z$ 轴旋转时，把 $x^2$ 替换为 $x^2+y^2$：

$$
G(x,z)=0\quad\Longrightarrow\quad G\bigl(\sqrt{x^2+y^2},z\bigr)=0.
$$

若 $G$ 中只出现 $x^2$，可直接作替换 $x^2\mapsto x^2+y^2$。注意母线若只取 $x\ge0$，旋转后得到的是完整曲面，但轴向范围和其他不等式限制仍要保留。

**例**：直线 $y=0, z=2x$ 绕 $z$ 轴旋转。母线中 $x=z/2$，所以
$$
x^2+y^2=\frac{z^2}{4},
$$
这是以 $z$ 轴为轴的圆锥面。若直线 $x=R, y=0$ 绕 $z$ 轴，则得到圆柱面 $x^2+y^2=R^2$。

### 2.2 绕任意直线旋转：轴向坐标与距离公式

设旋转轴为
$$
L:\ \mathbf r=\mathbf a+t\mathbf e,\qquad |\mathbf e|=1,
$$
其中 $\mathbf a$ 是轴上一点，$\mathbf e$ 是轴方向单位向量。对空间点 $\mathbf r$，定义
$$
s=(\mathbf r-\mathbf a)\cdot\mathbf e,\qquad
\rho=\operatorname{dist}(\mathbf r,L)=\left|(\mathbf r-\mathbf a)\times\mathbf e\right|.
$$

$s$ 是沿轴方向的坐标，$\rho$ 是到轴的距离。若母线在含轴的平面内写成
$$
\Phi(s,\rho)=0,
$$
则旋转曲面统一写成
$$
\boxed{\Phi\!\left((\mathbf r-\mathbf a)\cdot\mathbf e,
\left|(\mathbf r-\mathbf a)\times\mathbf e\right|\right)=0}.
$$

若关系中只出现 $\rho^2$，可用
$$
\rho^2=|(\mathbf r-\mathbf a)\times\mathbf e|^2
$$
避免根号。这个公式本质上就是“母线上的横坐标，换成点到旋转轴的距离”。

### 2.3 任意轴的参数方程：最稳的写法

取两个互相垂直且都垂直于 $\mathbf e$ 的单位向量 $\mathbf n_1,\mathbf n_2$。若母线用轴向参数 $s$ 表示，半径为 $\rho(s)$，旋转曲面可参数化为
$$
\boxed{\mathbf r(s,\theta)=\mathbf a+s\mathbf e+\rho(s)\bigl(\cos\theta\,\mathbf n_1+\sin\theta\,\mathbf n_2\bigr)},
$$
$$
\theta\in[0,2\pi].
$$
其切向量叉积 $\mathbf r_s\times\mathbf r_\theta$ 可直接给出有向面积元和法向量，适合第二类曲面积分或题目明确要求参数方程的情形。

### 2.4 直线绕直线的常见几何结论

以下结论默认母线直线与旋转轴处在同一个平面内，这是考研题最常见的情形：

| 母线与轴的关系 | 旋转结果 | 识别特征 |
|:---|:---|:---|
| 平行且不重合 | 圆柱面 | 到轴距离为常数 $\rho=R$ |
| 相交但不重合 | 圆锥面 | $\rho=k|s-s_0|$；顶点在交点 |
| 垂直相交 | 平面（圆锥面的退化情形） | 例如 $z=0$ 绕 $z$ 轴仍为 $z=0$ |
| 与轴重合 | 退化为一条直线 | 不形成通常意义下的二维曲面 |

若母线与轴是异面直线，旋转所得集合通常不能直接套用“圆柱/圆锥”口诀。此时优先使用上面的距离公式或参数式，并根据题目给出的范围保留曲面片；不要只看两条直线的方向就下结论。

## 3. 常见二次曲面：不画图也能判断

先配方、移轴，再看各变量平方项的符号。下表列出最常见的标准形：

| 标准方程 | 曲面 | 快速判断 |
|:---|:---|:---|
| $\dfrac{x^2}{a^2}+\dfrac{y^2}{b^2}+\dfrac{z^2}{c^2}=1$ | 椭球面 | 三个正平方项，封闭 |
| $x^2+y^2=R^2$ | 圆柱面 | 缺少 $z$，沿 $z$ 轴无限延伸 |
| $x^2+y^2=z^2$ | 圆锥面 | 截面半径与 $|z|$ 成正比 |
| $z=x^2+y^2$ | 椭圆抛物面 | 向 $+z$ 方向开口 |
| $\dfrac{x^2}{a^2}+\dfrac{y^2}{b^2}-\dfrac{z^2}{c^2}=1$ | 单叶双曲面 | 一个负号，横截面为椭圆 |
| $\dfrac{z^2}{c^2}-\dfrac{x^2}{a^2}-\dfrac{y^2}{b^2}=1$ | 双叶双曲面 | 两个负号，沿 $z$ 轴分成两叶 |
| $z=\dfrac{x^2}{a^2}-\dfrac{y^2}{b^2}$ | 双曲抛物面 | $x,y$ 平方项符号相反，马鞍面 |

### 截面法：不会画立体图时的替代方案

1. **坐标面截面**：分别令 $x=0,y=0,z=0$，得到平面曲线，先画这三张“切片”。
2. **平行截面**：固定 $z=c$（或 $x=c,y=c$），判断截面是圆、椭圆、双曲线还是空集；截面随参数如何变化，就决定曲面的开口方向。
3. **对称性**：只含 $x^2$、$y^2$、$z^2$ 时分别关于对应坐标面和原点对称；含 $x^2+y^2$ 时绕 $z$ 轴旋转对称。
4. **范围与边界**：从方程或不等式先求 $x,y,z$ 的取值范围，再判断题目取的是整曲面、上半片、外侧还是两曲面之间的部分。
5. **交线优先**：两个曲面相交时，联立方程并优先消去最容易的变量；交线常是圆、椭圆或参数曲线，它比凭感觉画图可靠。

## 4. 曲面积分题的“识图—投影—计算”流程

1. **确认曲面是什么**：旋转面、柱面、平面、球面或二次曲面；写出等式和所有不等式范围。
2. **找最简单投影**：若能写成 $z=z(x,y)$，优先投影到 $xOy$；若含 $x^2+y^2$，优先考虑柱坐标；球面或锥面常考虑球坐标。
3. **先求投影区域**：把曲面的边界与投影面联立，得到 $D_{xy}$（或 $D_{yz},D_{zx}$），不要直接猜积分上下限。
4. **确定侧与符号**：第一类曲面积分与侧无关；第二类曲面积分先确定上/下、前/后、右/左，或用参数叉积确定法向量。
5. **能用公式就换公式**：封闭曲面优先检查高斯公式；空间闭曲线优先检查斯托克斯公式；非闭曲面再直接投影或参数化。

> **作图最低要求**：不必画出漂亮的三维图，只要能回答“轴向、截面、投影区域、边界范围、法向方向”五件事，就足以完成大多数线面积分题。

# 一、二重积分

## 1. 基本概念与性质

### 定义
设 $f(x,y)$ 在有界闭区域 $D$ 上有定义，将 $D$ 分割成 $n$ 个小区域 $\Delta\sigma_i$，任取 $(\xi_i,\eta_i)\in\Delta\sigma_i$，若极限
$$
\iint_D f(x,y)\,d\sigma = \lim_{\lambda\to0}\sum_{i=1}^n f(\xi_i,\eta_i)\Delta\sigma_i
$$
存在（$\lambda$ 为小区域直径最大值），则称 $f(x,y)$ 在 $D$ 上可积，该极限值为二重积分。

### 性质
| 性质 | 内容 |
|:----|:-----|
| **线性性** | $\iint_D (\alpha f+\beta g)\,d\sigma = \alpha\iint_D f\,d\sigma + \beta\iint_D g\,d\sigma$ |
| **可加性** | 若 $D = D_1\cup D_2$，$D_1\cap D_2$ 无内点，则 $\iint_D f=\iint_{D_1} f+\iint_{D_2} f$ |
| **保序性** | $f\ge g$ 则 $\iint_D f\ge\iint_D g$ |
| **估值不等式** | $m\cdot S_D \le \iint_D f(x,y)\,d\sigma \le M\cdot S_D$（$m,M$ 为 $f$ 在 $D$ 上的最小、最大值，$S_D$ 为 $D$ 的面积） |
| **中值定理** | 见 §1.4 |

### 被积函数与积分区域怎样共同影响积分大小

二重积分可以直观理解为“带符号的高度”乘以面积的累加。因此比较大小时，必须同时看被积函数的正负、大小以及积分区域，不能只看其中一个。

**固定区域，比较被积函数。** 在同一区域 $D$ 上，若
$$
f(x,y)\ge g(x,y),
$$
则
$$
\iint_D f(x,y)\,d\sigma\ge\iint_D g(x,y)\,d\sigma.
$$
特别地，若 $f\ge0$，则积分非负；若 $f\le0$，则积分非正。

**固定被积函数，比较区域。** 若 $D_1\subseteq D_2$，则
$$
\iint_{D_2}f\,d\sigma
=\iint_{D_1}f\,d\sigma+\iint_{D_2\setminus D_1}f\,d\sigma.
$$
所以只有当 $f\ge0$（至少在新增部分 $D_2\setminus D_1$ 上非负）时，才有
$$
\iint_{D_1}f\,d\sigma\le\iint_{D_2}f\,d\sigma.
$$
若 $f\le0$，不等号反向；若 $f$ 变号，区域变大后积分可能变大、变小，也可能不变，不能直接判断。

**函数和区域都变化。** 最常用的充分条件是：若
$$
D_1\subseteq D_2,\qquad 0\le g(x,y)\le f(x,y)\quad\bigl((x,y)\in D_1\bigr),
$$
并且 $f\ge0$ 在 $D_2\setminus D_1$ 上成立，则
$$
\boxed{\iint_{D_1}g\,d\sigma\le\iint_{D_2}f\,d\sigma.}
$$
理解为：先在原区域内把“高度”从 $g$ 增到 $f$，再增加一块非负的区域，两步都不会使积分减小。

> **判断口诀：** 同区域比函数，函数非负时可比区域；函数、区域同时变，先拆成“改高度”和“扩区域”两步。被积函数变号时，面积变大不代表积分变大。

## 2. 二重积分的计算

### 2.1 直角坐标

- **X-型区域**：$D=\{(x,y)\mid a\le x\le b,\;\varphi_1(x)\le y\le\varphi_2(x)\}$
  $$
  \iint_D f(x,y)\,d\sigma = \int_a^b dx\int_{\varphi_1(x)}^{\varphi_2(x)} f(x,y)\,dy
  $$
- **Y-型区域**：$D=\{(x,y)\mid c\le y\le d,\;\psi_1(y)\le x\le\psi_2(y)\}$
  $$
  \iint_D f(x,y)\,d\sigma = \int_c^d dy\int_{\psi_1(y)}^{\psi_2(y)} f(x,y)\,dx
  $$

> **技巧**：选择合适的积分次序可以简化计算。当被积函数或积分区域具有特点时，可交换积分次序。

### 2.2 极坐标

极坐标变换：
$$
\begin{cases}
x = r\cos\theta,\\
y = r\sin\theta,
\end{cases}\quad
d\sigma = r\,dr\,d\theta
$$

$$
\iint_D f(x,y)\,d\sigma = \iint_{D_{r\theta}} f(r\cos\theta,r\sin\theta)\,r\,dr\,d\theta
$$

**适用情形**：被积函数含 $x^2+y^2$、$\frac{y}{x}$ 等；积分区域为圆域、环域、扇形区域等。

**常用积分区域描述**：
- 极点在区域内部：$\theta\in[0,2\pi],\;0\le r\le r(\theta)$
- 极点在边界上：$\theta\in[\alpha,\beta],\;0\le r\le r(\theta)$
- 极点在区域外部：$\theta\in[\alpha,\beta],\;r_1(\theta)\le r\le r_2(\theta)$

## 3. 二重积分的对称性（重点）

### 3.1 奇偶对称性（普通对称性）

| 区域对称性 | 条件 | 结论 |
|:-----------|:-----|:-----|
| $D$ 关于 $x$ 轴对称 | $f(x,-y)=-f(x,y)$（关于 $y$ 奇） | $\iint_D f = 0$ |
| $D$ 关于 $x$ 轴对称 | $f(x,-y)=f(x,y)$（关于 $y$ 偶） | $\iint_D f = 2\iint_{D_{y\ge0}} f$ |
| $D$ 关于 $y$ 轴对称 | $f(-x,y)=-f(x,y)$（关于 $x$ 奇） | $\iint_D f = 0$ |
| $D$ 关于 $y$ 轴对称 | $f(-x,y)=f(x,y)$（关于 $x$ 偶） | $\iint_D f = 2\iint_{D_{x\ge0}} f$ |
| $D$ 关于原点对称 | $f(-x,-y)=f(x,y)$ | $\iint_D f = 2\iint_{D_{x\ge0}} f$（半区域） |
| $D$ 关于原点对称 | $f(-x,-y)=-f(x,y)$ | $\iint_D f = 0$ |
| $D$ 关于直线 $y=x$ 对称 | $f(y,x)=f(x,y)$ | $\iint_D f = 2\iint_{D_{x\le y}} f$ |
| $D$ 关于直线 $y=x$ 对称 | $f(y,x)=-f(x,y)$ | $\iint_D f = 0$ |

### 3.2 轮换对称性

若积分区域 $D$ 关于直线 $y=x$ 对称（即交换 $x$ 与 $y$ 后区域不变），则
$$
\iint_D f(x,y)\,d\sigma = \iint_D f(y,x)\,d\sigma
$$

**常用技巧**：当 $f(x,y)+f(y,x)$ 容易积分时，可用
$$
\iint_D f(x,y)\,d\sigma = \frac12\iint_D \bigl[f(x,y)+f(y,x)\bigr]\,d\sigma
$$

## 4. 二重积分的中值定理

### 定理内容
设 $f(x,y)$ 在有界闭区域 $D$ 上连续，$D$ 的面积为 $S_D$，则存在 $(\xi,\eta)\in D$ 使得
$$
\iint_D f(x,y)\,d\sigma = f(\xi,\eta)\cdot S_D
$$

### 推广形式
若 $f$ 连续，$g$ 可积且在 $D$ 上不变号，则存在 $(\xi,\eta)\in D$ 使
$$
\iint_D f(x,y)g(x,y)\,d\sigma = f(\xi,\eta)\iint_D g(x,y)\,d\sigma
$$

## 5. 二重积分的变量变换

### 一般变换
设变换
$$
\begin{cases}
x = x(u,v),\\
y = y(u,v),
\end{cases}
$$
将 $uv$ 平面上的区域 $D'$ 一对一定映到 $xy$ 平面上的区域 $D$，且变换的雅可比行列式 $J(u,v)\neq0$，则
$$
\iint_D f(x,y)\,dxdy = \iint_{D'} f\bigl(x(u,v),y(u,v)\bigr)\,|J(u,v)|\,dudv
$$

### 常用变换的雅可比行列式

| 变换 | 公式 | 雅可比行列式 |
|:----|:-----|:------------:|
| 极坐标 | $x=r\cos\theta,\;y=r\sin\theta$ | $J=r$ |
| 广义极坐标 | $x=ar\cos\theta,\;y=br\sin\theta$ | $J=abr$ |
| 平移 | $x=u+a,\;y=v+b$ | $J=1$ |
| 旋转 | $x=u\cos\alpha-v\sin\alpha,\;y=u\sin\alpha+v\cos\alpha$ | $J=1$ |

### 典型应用

> **例**：求椭圆域 $\dfrac{x^2}{a^2}+\dfrac{y^2}{b^2}\le1$ 的面积。

令 $x=ar\cos\theta,\;y=br\sin\theta$，则 $|J|=abr$，区域变为 $0\le r\le1,\;0\le\theta\le2\pi$，面积
$$
S = \iint_D 1\,dxdy = \int_0^{2\pi}d\theta\int_0^1 abr\,dr = \pi ab
$$

---

# 二、三重积分

## 1. 定义与性质

三重积分 $\iiint_{\Omega} f(x,y,z)\,dV$ 定义类似二重积分，性质完全类比。

## 2. 三重积分的计算

### 2.1 直角坐标

**投影法（先一后二）**：
$$
\iiint_{\Omega} f(x,y,z)\,dV = \iint_{D_{xy}} dxdy\int_{z_1(x,y)}^{z_2(x,y)} f(x,y,z)\,dz
$$
其中 $D_{xy}$ 是 $\Omega$ 在 $xOy$ 平面上的投影。

**截面法（先二后一）**：
若 $\Omega$ 夹在 $z=c_1$ 与 $z=c_2$ 之间，用垂直于 $z$ 轴的平面截得截面 $D_z$，则
$$
\iiint_{\Omega} f(x,y,z)\,dV = \int_{c_1}^{c_2} dz\iint_{D_z} f(x,y,z)\,dxdy
$$

### 2.2 柱坐标

变换：
$$
\begin{cases}
x = r\cos\theta,\\
y = r\sin\theta,\\
z = z,
\end{cases}\quad
dV = r\,dr\,d\theta\,dz
$$

**适用情形**：积分区域在 $xOy$ 平面上的投影为圆域、环域；被积函数含 $x^2+y^2$ 等。

### 2.3 球坐标

变换：
$$
\begin{cases}
x = r\sin\varphi\cos\theta,\\
y = r\sin\varphi\sin\theta,\\
z = r\cos\varphi,
\end{cases}\quad
dV = r^2\sin\varphi\,dr\,d\varphi\,d\theta
$$

其中 $r\in[0,+\infty)$，$\varphi\in[0,\pi]$（$z$ 轴正方向夹角），$\theta\in[0,2\pi]$。

**适用情形**：积分区域为球体、半球体、锥体等；被积函数含 $x^2+y^2+z^2$。

## 3. 三重积分的对称性

完全类比二重积分，可以推广到三维：

- 关于 $xOy$ 平面对称：$f(x,y,-z)=\pm f(x,y,z)$ → 奇零偶倍
- 关于 $yOz$ 平面对称：$f(-x,y,z)=\pm f(x,y,z)$
- 关于 $xOz$ 平面对称：$f(x,-y,z)=\pm f(x,y,z)$
- 轮换对称性：若 $\Omega$ 关于 $x=y$、$y=z$ 等对称，则积分 $\iiint_{\Omega} f(x,y,z)\,dV = \iiint_{\Omega} f(y,x,z)\,dV$ 等。

## 4. 三重积分的变量变换与雅可比

一般变换 $\begin{cases}x=x(u,v,w)\\ y=y(u,v,w)\\ z=z(u,v,w)\end{cases}$，雅可比行列式
$$
J = \frac{\partial(x,y,z)}{\partial(u,v,w)} = \begin{vmatrix}
x_u & x_v & x_w\\
y_u & y_v & y_w\\
z_u & z_v & z_w
\end{vmatrix}
$$
则
$$
\iiint_{\Omega} f(x,y,z)\,dV = \iiint_{\Omega'} f\bigl(x(u,v,w),y(u,v,w),z(u,v,w)\bigr)\,|J|\,dudvdw
$$

---

# 三、第一类曲线积分（对弧长的曲线积分）

## 1. 定义与性质

$$
\int_L f(x,y,z)\,ds = \lim_{\lambda\to0}\sum_{i=1}^n f(\xi_i,\eta_i,\zeta_i)\Delta s_i
$$

**性质**：线性性、可加性、与路径方向**无关**。

## 2. 计算公式

### 平面曲线
$L: y=y(x)$（$a\le x\le b$）：
$$
\int_L f(x,y)\,ds = \int_a^b f\bigl(x,y(x)\bigr)\,\sqrt{1+[y'(x)]^2}\,dx
$$

$L$ 参数方程：$\begin{cases}x=x(t)\\ y=y(t)\end{cases}$（$\alpha\le t\le\beta$）：
$$
\int_L f(x,y)\,ds = \int_\alpha^\beta f\bigl(x(t),y(t)\bigr)\,\sqrt{[x'(t)]^2+[y'(t)]^2}\,dt
$$

### 空间曲线
$L$ 参数方程：$\begin{cases}x=x(t)\\ y=y(t)\\ z=z(t)\end{cases}$（$\alpha\le t\le\beta$）：
$$
\int_L f(x,y,z)\,ds = \int_\alpha^\beta f\bigl(x(t),y(t),z(t)\bigr)\,\sqrt{[x'(t)]^2+[y'(t)]^2+[z'(t)]^2}\,dt
$$

## 3. 对称性
若曲线 $L$ 关于 $x$ 轴对称，则 $f$ 关于 $y$ 奇→0，偶→2 倍上半部分。

---

# 四、第二类曲线积分（对坐标的曲线积分）

## 1. 定义

$$
\int_L P\,dx + Q\,dy + R\,dz = \lim_{\lambda\to0}\sum_{i=1}^n \bigl[P(\xi_i,\eta_i,\zeta_i)\Delta x_i + Q\Delta y_i + R\Delta z_i\bigr]
$$

**性质**：与路径方向**有关**，反向变号。

## 2. 计算公式

参数方程：$\begin{cases}x=x(t)\\ y=y(t)\\ z=z(t)\end{cases}$，$t$ 从 $\alpha$ 到 $\beta$（起点对应 $\alpha$，终点对应 $\beta$）：
$$
\int_L P\,dx+Q\,dy+R\,dz = \int_\alpha^\beta \bigl[P\,x'(t)+Q\,y'(t)+R\,z'(t)\bigr]\,dt
$$

## 3. 两类曲线积分的联系
$$
\int_L P\,dx+Q\,dy+R\,dz = \int_L (P\cos\alpha + Q\cos\beta + R\cos\gamma)\,ds
$$
其中 $(\cos\alpha,\cos\beta,\cos\gamma)$ 为曲线 $L$ 在点 $(x,y,z)$ 处的**有向切线方向余弦**（方向与 $L$ 方向一致）。

---

# 五、格林公式（平面上的桥梁）

## 定理
设 $D$ 为平面有界闭区域，其边界 $L$ 为分段光滑曲线，方向取**正向**（逆时针），$P(x,y),Q(x,y)$ 在 $D$ 上具有一阶连续偏导数，则
$$
\oint_L P\,dx+Q\,dy = \iint_D \left(\frac{\partial Q}{\partial x} - \frac{\partial P}{\partial y}\right)\,dxdy
$$

## 重要推论

### 1. 平面面积公式
$$
A(D) = \iint_D 1\,dxdy = \frac12\oint_L x\,dy - y\,dx
$$

### 2. 积分与路径无关的条件
在单连通区域 $D$ 内，曲线积分 $\int_L P\,dx+Q\,dy$ 与路径无关 $\Longleftrightarrow$ $\displaystyle\frac{\partial P}{\partial y} = \frac{\partial Q}{\partial x}$ 在 $D$ 内恒成立。

**等价于**：存在可微函数 $u(x,y)$ 使得 $du = P\,dx+Q\,dy$（$u$ 为原函数），此时积分值 $= u(B)-u(A)$。

## 应用技巧
- 当被积函数或边界曲线复杂时，可补线用格林公式转化为二重积分。
- 注意方向：正向（逆时针）对应 $+D$，负向（顺时针）对应 $-D$。

## 奇点（瑕点）的处理（重点）

> 瑕点必须先检查，不能直接套用格林公式。

### 什么是奇点
若在区域 $D$ 内存在点 $M_0$，使得 $P,Q$ 在 $M_0$ 处不满足一阶偏导连续（如分母为零的点），则 $M_0$ 称为**奇点（瑕点）**。此时不能直接在含奇点的闭曲线 $L$ 上用格林公式。

### 处理方法：挖洞法
将奇点用一个小闭曲线 $l$（常取小圆或小椭圆）**挖掉**，记 $D'$ 为 $L$ 与 $l$ 之间的区域（不含奇点），则在 $D'$ 上格林公式可用：

$$
\oint_L P\,dx+Q\,dy + \oint_{l^-} P\,dx+Q\,dy = \iint_{D'} \left(\frac{\partial Q}{\partial x} - \frac{\partial P}{\partial y}\right)\,dxdy
$$

其中 $l^-$ 表示 $l$ 取**反向**（使 $L$ 与 $l^-$ 共同构成 $D'$ 的正向边界：区域 $D'$ 始终在左侧）。

于是：
$$
\oint_L P\,dx+Q\,dy = \iint_{D'} \left(\frac{\partial Q}{\partial x} - \frac{\partial P}{\partial y}\right)\,dxdy + \oint_{l^+} P\,dx+Q\,dy
$$

实际做题时通常直接在小圆 $l$（半径 $\varepsilon$）上计算 $\oint_l$，再取 $\varepsilon\to0$ 的极限。

### 多个瑕点
若区域内有多个瑕点，分别围住 $M_1,\ldots,M_n$ 挖小洞即可。外边界取逆时针、各内边界取顺时针，若 $Q_x-P_y=0$，则

$$
\oint_LP\,dx+Q\,dy=\sum_{j=1}^{n}\oint_{l_j^+}P\,dx+Q\,dy.
$$

这里 $l_j^+$ 是围住第 $j$ 个瑕点的逆时针小曲线；若某个瑕点不在 $L$ 的内部，就不贡献对应的小圆积分。

### 典型例题
> 计算 $I = \oint_L \frac{-y}{x^2+y^2}\,dx + \frac{x}{x^2+y^2}\,dy$，其中 $L$ 为绕原点的正向光滑闭曲线。

**解**：$P=\dfrac{-y}{x^2+y^2}$，$Q=\dfrac{x}{x^2+y^2}$，当 $(x,y)\neq(0,0)$ 时
$$\frac{\partial Q}{\partial x} = \frac{y^2-x^2}{(x^2+y^2)^2} = \frac{\partial P}{\partial y}$$
故原点是唯一奇点。取小圆 $x^2+y^2=\varepsilon^2$（参数化 $x=\varepsilon\cos\theta,\ y=\varepsilon\sin\theta$，$\theta:0\to2\pi$）：
$$\oint_l \frac{-y}{x^2+y^2}\,dx + \frac{x}{x^2+y^2}\,dy
= \int_0^{2\pi} \frac{-\varepsilon\sin\theta\cdot(-\varepsilon\sin\theta) + \varepsilon\cos\theta\cdot\varepsilon\cos\theta}{\varepsilon^2}\,d\theta
= \int_0^{2\pi} 1\,d\theta = 2\pi$$

由挖洞法：$\displaystyle\oint_L = \oint_l = 2\pi$（与 $L$ 的具体形状无关，只取决于是否绕过原点以及绕向）。

> **⚠️ 结论记住**
>
> $$
> \oint_L \frac{-y}{x^2+y^2}\,dx
> +\frac{x}{x^2+y^2}\,dy
> =
> \begin{cases}
> 2\pi, & L\text{ 逆时针绕原点一圈},\\
> 0, & L\text{ 不绕原点},\\
> -2\pi, & L\text{ 顺时针绕原点一圈}.
> \end{cases}
> $$

---

# 六、第一类曲面积分（对面积的曲面积分）

## 1. 定义与性质
$$
\iint_{\Sigma} f(x,y,z)\,dS = \lim_{\lambda\to0}\sum f(\xi_i,\eta_i,\zeta_i)\Delta S_i
$$
**性质**：与曲面**侧无关**，只与面积微元有关。

## 2. 计算公式

### 曲面 $z=z(x,y)$（向 $xOy$ 投影）
$$
\iint_{\Sigma} f(x,y,z)\,dS = \iint_{D_{xy}} f\bigl(x,y,z(x,y)\bigr)\,\sqrt{1+z_x^2+z_y^2}\,dxdy
$$

### 曲面 $y=y(z,x)$ 或 $x=x(y,z)$ 类似

### 参数形式
若 $\Sigma: \begin{cases}x=x(u,v)\\ y=y(u,v)\\ z=z(u,v)\end{cases}$，$(u,v)\in D$，则
$$
\iint_{\Sigma} f\,dS = \iint_D f\bigl(x(u,v),y(u,v),z(u,v)\bigr)\,\|\mathbf{r}_u\times\mathbf{r}_v\|\,dudv
$$
其中 $\mathbf{r}=(x,y,z)$，$\|\mathbf{r}_u\times\mathbf{r}_v\| = \sqrt{\left|\frac{\partial(y,z)}{\partial(u,v)}\right|^2+\left|\frac{\partial(z,x)}{\partial(u,v)}\right|^2+\left|\frac{\partial(x,y)}{\partial(u,v)}\right|^2}$

### 常见曲面面积微元

| 曲面形式 | 面积微元 $dS$ |
|:---------|:--------------|
| $z=z(x,y)$ | $\sqrt{1+z_x^2+z_y^2}\,dxdy$ |
| $y=y(z,x)$ | $\sqrt{1+y_z^2+y_x^2}\,dzdx$ |
| $x=x(y,z)$ | $\sqrt{1+x_y^2+x_z^2}\,dydz$ |
| 球面 $x^2+y^2+z^2=R^2$ | $R^2\sin\varphi\,d\varphi\,d\theta$（球坐标） |

---

# 七、第二类曲面积分（对坐标的曲面积分）

## 1. 定义
$$
\iint_{\Sigma} P\,dydz + Q\,dzdx + R\,dxdy
$$
其中 $\Sigma$ 取**定侧**（有向曲面）。

## 2. 计算公式

### 投影法
- $\iint_{\Sigma} R(x,y,z)\,dxdy = \pm\iint_{D_{xy}} R\bigl(x,y,z(x,y)\bigr)\,dxdy$，上侧取 $+$，下侧取 $-$。
- $\iint_{\Sigma} P\,dydz = \pm\iint_{D_{yz}} P\bigl(x(y,z),y,z\bigr)\,dydz$，前侧取 $+$，后侧取 $-$。
- $\iint_{\Sigma} Q\,dzdx = \pm\iint_{D_{zx}} Q\bigl(x,y(z,x),z\bigr)\,dzdx$，右侧取 $+$，左侧取 $-$。

### 统一成对面积的曲面积分（联系两类曲面积分）
$$
\iint_{\Sigma} P\,dydz+Q\,dzdx+R\,dxdy = \iint_{\Sigma} (P\cos\alpha+Q\cos\beta+R\cos\gamma)\,dS
$$
其中 $(\cos\alpha,\cos\beta,\cos\gamma)$ 为曲面 $\Sigma$ 上的**有向法向量的方向余弦**（指向所选侧的方向）。

## 3. 两类曲面积分的联系
$$
\iint_{\Sigma} P\,dydz+Q\,dzdx+R\,dxdy = \iint_{\Sigma} \mathbf{F}\cdot\mathbf{n}\,dS
$$
其中 $\mathbf{F}=(P,Q,R)$，$\mathbf{n}$ 为 $\Sigma$ 上指向所选侧的单位法向量。这就是第二类曲面积分的**向量形式**。

## 4. 三个投影微元、方向余弦与 $dS$

设所选侧的单位法向量为
$$
\mathbf n=(\cos\alpha,\cos\beta,\cos\gamma).
$$
三个有向投影面积元满足
$$
dydz=\cos\alpha\,dS,\qquad
dzdx=\cos\beta\,dS,\qquad
dxdy=\cos\gamma\,dS.
$$
因此
$$
P\,dydz+Q\,dzdx+R\,dxdy
=(P\cos\alpha+Q\cos\beta+R\cos\gamma)\,dS.
$$

| 微元 | 投影平面 | 对应方向余弦 | 正向 |
|:-----|:---------|:-------------|:-----|
| $dydz$ | $yOz$ 平面 | $\cos\alpha$ | 前侧 |
| $dzdx$ | $zOx$ 平面 | $\cos\beta$ | 右侧 |
| $dxdy$ | $xOy$ 平面 | $\cos\gamma$ | 上侧 |

$$
\cos^2\alpha+\cos^2\beta+\cos^2\gamma=1.
$$

标准顺序固定为 $dydz,dzdx,dxdy$。有向微分交换次序变号：
$$
\boxed{dxdz=-dzdx}.
$$
因此 $Q\,dxdz=-Q\,dzdx$，不能把 $dxdz$ 与 $dzdx$ 当成同一个量。只求面积时使用
$$
|dydz|=|\cos\alpha|dS,\quad
|dzdx|=|\cos\beta|dS,\quad
|dxdy|=|\cos\gamma|dS.
$$

## 5. 三种显函数曲面的偏导公式

### 5.1 $z=z(x,y)$，取上侧

$$
dS=\sqrt{1+z_x^2+z_y^2}\,dxdy.
$$

**第一步：先由微分关系确定符号，与曲面定向无关。** 因为
$$
dz=z_x\,dx+z_y\,dy,\qquad dy\,dx=-dx\,dy,
$$
所以
$$
dydz=-z_x\,dxdy,\qquad dzdx=-z_y\,dxdy,\qquad dxdy=dxdy.
$$
例如
$$
dydz=dy\,(z_x\,dx+z_y\,dy)
=z_x\,dy\,dx=-z_x\,dxdy.
$$

**第二步：再由曲面定向决定整体符号。** 上侧法向量取
$$
\mathbf n_+=\frac{(-z_x,-z_y,1)}
{\sqrt{1+z_x^2+z_y^2}},
$$
故
$$
\boxed{
\iint_{\Sigma_+}P\,dydz+Q\,dzdx+R\,dxdy
=\iint_{D_{xy}}(-Pz_x-Qz_y+R)\,dxdy
}.
$$
下侧法向量为 $-\mathbf n_+$，三个有向投影微元整体变号，积分式整体乘以 $-1$。

### 5.2 $y=y(z,x)$，取 $y$ 轴正方向一侧

$$
dS=\sqrt{1+y_x^2+y_z^2}\,dzdx.
$$

**第一步：先由微分关系确定符号，与曲面定向无关。** 因为
$$
dy=y_z\,dz+y_x\,dx,\qquad dx\,dz=-dz\,dx,
$$
所以
$$
dydz=-y_x\,dzdx,\qquad dzdx=dzdx,\qquad dxdy=-y_z\,dzdx.
$$
例如
$$
dydz=(y_z\,dz+y_x\,dx)\,dz
=y_x\,dx\,dz=-y_x\,dzdx.
$$

**第二步：再由曲面定向决定整体符号。** 取 $y$ 轴正方向一侧，法向量为
$$
\mathbf n_+=\frac{(-y_x,1,-y_z)}
{\sqrt{1+y_x^2+y_z^2}},
$$
故
$$
\boxed{
\iint_{\Sigma_+}P\,dydz+Q\,dzdx+R\,dxdy
=\iint_{D_{zx}}(-Py_x+Q-Ry_z)\,dzdx
}.
$$
取 $y$ 轴负方向一侧时，三个有向投影微元整体变号，积分式整体乘以 $-1$。

### 5.3 $x=x(y,z)$，取 $x$ 轴正方向一侧

$$
dS=\sqrt{1+x_y^2+x_z^2}\,dydz.
$$

**第一步：先由微分关系确定符号，与曲面定向无关。** 因为
$$
dx=x_y\,dy+x_z\,dz,\qquad dz\,dy=-dy\,dz,
$$
所以
$$
dydz=dydz,\qquad dzdx=-x_y\,dydz,\qquad dxdy=-x_z\,dydz.
$$
例如
$$
dzdx=dz\,(x_y\,dy+x_z\,dz)
=x_y\,dz\,dy=-x_y\,dydz.
$$

**第二步：再由曲面定向决定整体符号。** 取 $x$ 轴正方向一侧，法向量为
$$
\mathbf n_+=\frac{(1,-x_y,-x_z)}
{\sqrt{1+x_y^2+x_z^2}},
$$
故
$$
\boxed{
\iint_{\Sigma_+}P\,dydz+Q\,dzdx+R\,dxdy
=\iint_{D_{yz}}(P-Qx_y-Rx_z)\,dydz
}.
$$
取 $x$ 轴负方向一侧时，三个有向投影微元整体变号，积分式整体乘以 $-1$。

> **统一记忆：** 第一步只处理微分交换，例如 $dy\,dx=-dx\,dy$，与上下侧、前后侧、左右侧无关；第二步才根据法向方向决定是否把三个有向投影微元整体变号。不要把这两个负号混为一谈。

| 曲面 | 正向侧 | $dS$ | $(dydz,dzdx,dxdy)$ |
|:-----|:-------|:-----|:-------------------|
| $z=z(x,y)$ | $z$ 轴正方向 | $\sqrt{1+z_x^2+z_y^2}\,dxdy$ | $(-z_x,-z_y,1)\,dxdy$ |
| $y=y(z,x)$ | $y$ 轴正方向 | $\sqrt{1+y_x^2+y_z^2}\,dzdx$ | $(-y_x,1,-y_z)\,dzdx$ |
| $x=x(y,z)$ | $x$ 轴正方向 | $\sqrt{1+x_y^2+x_z^2}\,dydz$ | $(1,-x_y,-x_z)\,dydz$ |

## 6. 参数曲面公式

若 $\mathbf r(u,v)=(x(u,v),y(u,v),z(u,v))$，则
$$
\mathbf r_u\times\mathbf r_v
=\left(
\frac{\partial(y,z)}{\partial(u,v)},
\frac{\partial(z,x)}{\partial(u,v)},
\frac{\partial(x,y)}{\partial(u,v)}
\right).
$$
当叉乘方向与所选侧一致时
$$
dydz=\frac{\partial(y,z)}{\partial(u,v)}\,dudv,\quad
dzdx=\frac{\partial(z,x)}{\partial(u,v)}\,dudv,\quad
dxdy=\frac{\partial(x,y)}{\partial(u,v)}\,dudv,
$$
$$
dS=\|\mathbf r_u\times\mathbf r_v\|\,dudv.
$$
方向相反时，三个有向投影面积元同时变号。

## 7. 做题顺序

1. 先确定曲面侧与法向量方向。
2. 固定写成 $P\,dydz+Q\,dzdx+R\,dxdy$，看到 $dxdz$ 先改为 $-dzdx$。
3. 选择最方便的投影平面，写出对应偏导。
4. 代入显函数公式；换侧时结果整体变号。

---

# 八、高斯公式（曲面积分 → 三重积分）

## 定理
设 $\Omega$ 为空间有界闭区域，其边界曲面 $\Sigma$ 取**外侧**，$P,Q,R$ 在 $\Omega$ 上具有一阶连续偏导数，则
$$
\oiint_{\Sigma} P\,dydz+Q\,dzdx+R\,dxdy = \iiint_{\Omega} \left(\frac{\partial P}{\partial x}+\frac{\partial Q}{\partial y}+\frac{\partial R}{\partial z}\right)\,dV
$$

### 向量形式
$$
\oiint_{\Sigma} \mathbf{F}\cdot d\mathbf{S} = \iiint_{\Omega} \nabla\cdot\mathbf{F}\,dV
$$
其中 $d\mathbf{S} = \mathbf{n}\,dS$（有向面积元），$\nabla\cdot\mathbf{F}=\frac{\partial P}{\partial x}+\frac{\partial Q}{\partial y}+\frac{\partial R}{\partial z}$ 称为散度。

## 应用
- 将封闭曲面积分转化为三重积分（有时更简单）。
- 计算散度通量（如电场高斯定理）。

---

# 九、斯托克斯公式（空间曲线积分 → 曲面积分，核心版）

## 1. 定理与主要写法
设 $\Sigma$ 为有向光滑曲面，边界 $L$ 的方向与所选法向量满足**右手法则**，且 $P,Q,R$ 具有一阶连续偏导数，则

$$
\oint_L P\,dx+Q\,dy+R\,dz
=\iint_{\Sigma}
\begin{vmatrix}
dydz & dzdx & dxdy\\
\dfrac{\partial}{\partial x} & \dfrac{\partial}{\partial y} & \dfrac{\partial}{\partial z}\\
P & Q & R
\end{vmatrix}.
$$

$$
\oint_L P\,dx+Q\,dy+R\,dz
=\iint_{\Sigma}(\nabla\times\mathbf F)\cdot d\mathbf S,
\qquad \mathbf F=(P,Q,R).
$$

令
$$
\nabla\times\mathbf F=(A,B,C),\qquad
A=R_y-Q_z,\quad B=P_z-R_x,\quad C=Q_x-P_y.
$$

考研计算时主要使用
$$
\oint_L P\,dx+Q\,dy+R\,dz
=\iint_{\Sigma}(A\,dydz+B\,dzdx+C\,dxdy)
=\iint_{\Sigma}(A\cos\alpha+B\cos\beta+C\cos\gamma)\,dS.
$$

若第一行采用 $dxdy,\ dydz,\ dzdx$ 的顺序，也可以整体写成
$$
\oint_L P\,dx+Q\,dy+R\,dz
=\iint_{\Sigma}
\begin{vmatrix}
dxdy & dydz & dzdx\\
\dfrac{\partial}{\partial z} & \dfrac{\partial}{\partial x} & \dfrac{\partial}{\partial y}\\
R & P & Q
\end{vmatrix}.
$$

> **记忆原则**：三阶行列式整体保留，不要只调换第一行；若调换投影面积元的顺序，第二、三行必须同步调换。$dydz,dzdx,dxdy$ 分别对应 $x,y,z$ 分量。

其中 $(\cos\alpha,\cos\beta,\cos\gamma)$ 是所选单位法向量的方向余弦，且
$$
dydz=\cos\alpha\,dS,\qquad dzdx=\cos\beta\,dS,\qquad dxdy=\cos\gamma\,dS.
$$

> **易错点**：$dydz,dzdx,dxdy$ 分别对应 $x,y,z$ 分量；固定写成 $dzdx$，不要改成 $dxdz$。

## 2. 与第二类曲面积分的衔接
斯托克斯右端就是第二类曲面积分，只是被积向量场换成旋度：

$$
\iint_{\Sigma}(A\,dydz+B\,dzdx+C\,dxdy)
=\iint_{\Sigma}(\nabla\times\mathbf F)\cdot\mathbf n\,dS.
$$

因此前面第七章的投影法、方向余弦和参数曲面法都可以直接使用，不再重复展开行列式。

## 3. 做题流程与方向
1. 将曲线积分写成 $P\,dx+Q\,dy+R\,dz$，确定 $\mathbf F=(P,Q,R)$。
2. 计算旋度 $(A,B,C)=(R_y-Q_z,\ P_z-R_x,\ Q_x-P_y)$。
3. 选取以 $L$ 为边界且最易计算的曲面，通常优先选平面片。
4. 用右手法则确定侧：从法向量正端看，边界应为逆时针；方向相反时整体加负号。
5. 用投影法或 $d\mathbf S=\mathbf n\,dS$ 化为二重积分。

若 $\Sigma:z=z(x,y)$ 取上侧，可直接用
$$
d\mathbf S=(-z_x,-z_y,1)\,dxdy;
$$
若用参数方程 $\mathbf r(u,v)$，则
$$
d\mathbf S=\pm(\mathbf r_u\times\mathbf r_v)\,dudv.
$$

## 4. 公式中最容易错的地方
只需固定记忆
$$
\oint_L P\,dx+Q\,dy+R\,dz
=\iint_{\Sigma}(R_y-Q_z)\,dydz+(P_z-R_x)\,dzdx+(Q_x-P_y)\,dxdy.
$$

不要随意调换 $dydz,dzdx,dxdy$ 的顺序；若使用行列式写法，三列必须和 $\partial_x,\partial_y,\partial_z$ 以及 $P,Q,R$ 同步交换，否则会产生符号错误。

## 5. 方向与正负号（保留核心）
右手四指沿 $L$ 的正向弯曲时，大拇指指向所选法向量；等价地，从法向量正端看，$L$ 为逆时针。换曲面侧或换曲线方向，积分都变号：

$$
\mathbf n\to-\mathbf n\quad\Longrightarrow\quad
\iint_{\Sigma}(\nabla\times\mathbf F)\cdot\mathbf n\,dS
\to-\iint_{\Sigma}(\nabla\times\mathbf F)\cdot\mathbf n\,dS.
$$

> **口诀**：边反则面反，面反则积分反号。

### 投影法与参数法
投影法直接沿用第二类曲面积分的符号规则：$dydz$ 看前后，$dzdx$ 看左右，$dxdy$ 看上下；侧与投影同向取正，反向取负。

参数曲面 $\mathbf r(u,v)$ 使用
$$
d\mathbf S=\pm(\mathbf r_u\times\mathbf r_v)\,dudv,
$$
其中符号由 $\mathbf r_u\times\mathbf r_v$ 与题目要求的法向量方向决定。

## 6. 选取曲面的原则
斯托克斯公式右端只与边界 $L$ 及其方向有关，因此在不穿过奇点的前提下，可以把曲面换成同一边界下更简单的曲面。平面曲线取平面片；球面、柱面与平面的交线通常也优先取平面片。

若 $P,Q,R$ 在曲面上有奇点，不能直接套用斯托克斯公式，应先挖洞，再按边界方向处理。


# 十、两类积分与三大公式的联系

## 1. 两类积分先统一

| 类型 | 形式 | 是否与方向有关 | 本质 |
|:-----|:-----|:-----:|:-----|
| 第一类曲线积分 | $\displaystyle\int_L f\,ds$ | 否 | 对弧长加权 |
| 第二类曲线积分 | $\displaystyle\int_LP\,dx+Q\,dy+R\,dz=\int_L\mathbf F\cdot d\mathbf r$ | 是 | 向量场沿切向量的积分 |
| 第一类曲面积分 | $\displaystyle\iint_{\Sigma}f\,dS$ | 否 | 对面积加权 |
| 第二类曲面积分 | $\displaystyle\iint_{\Sigma}P\,dydz+Q\,dzdx+R\,dxdy=\iint_{\Sigma}\mathbf F\cdot\mathbf n\,dS$ | 是 | 向量场通过曲面的通量 |

两类积分的转换公式为

$$
P\,dx+Q\,dy+R\,dz
=(P\cos\alpha+Q\cos\beta+R\cos\gamma)\,ds,
$$

$$
P\,dydz+Q\,dzdx+R\,dxdy
=(P\cos\alpha+Q\cos\beta+R\cos\gamma)\,dS.
$$

## 2. 三大公式各自连接什么

| 名称 | 形式 | 转化关系 | 使用条件 |
|:-----|:-----|:---------|:---------|
| **格林公式** | $\oint_L Pdx+Qdy = \iint_D (Q_x-P_y)dxdy$ | 平面第二类曲线积分→二重积分 | $L$ 正向，注意瑕点 |
| **高斯公式** | $\oiint_{\Sigma}Pdydz+Qdzdx+Rdxdy = \iiint_{\Omega}(P_x+Q_y+R_z)dV$ | 封闭第二类曲面积分→三重积分 | 取外侧，注意内外侧符号 |
| **斯托克斯公式** | $\oint_LPdx+Qdy+Rdz = \iint_{\Sigma}(\nabla\times\mathbf F)\cdot d\mathbf S$ | 空间第二类曲线积分→第二类曲面积分 | $L$ 与 $\Sigma$ 满足右手法则 |

### 格林公式是斯托克斯公式的平面特例
取 $\Sigma=D\subset xOy$，法向量为 $(0,0,1)$，并令 $\mathbf F=(P,Q,0)$，则

$$
(\nabla\times\mathbf F)\cdot\mathbf n=Q_x-P_y,
$$

斯托克斯公式立即化为格林公式。因此，平面封闭曲线优先想到格林公式，空间封闭曲线优先想到斯托克斯公式。

### 高斯公式与斯托克斯公式的区别
高斯公式处理**封闭曲面上的通量**，核心是散度；斯托克斯公式处理**封闭曲线上的环量**，核心是旋度。曲面不封闭不能直接套高斯公式，曲线不闭合不能直接套斯托克斯公式。

## 3. 一页式选公式策略

| 问题类型 | 常用方法 |
|:---------|:---------|
| 平面封闭的第二类曲线积分 | 格林公式 |
| 空间封闭的第二类曲线积分 | 斯托克斯公式，优先选简单曲面 |
| 封闭的第二类曲面积分 | 高斯公式 |
| 非封闭曲线或曲面 | 补线、补面，或直接参数化 |
| 只含 $ds$ 或 $dS$ | 第一类积分，重点处理长度、面积和对称性 |
| 怀疑与路径无关 | 检查偏导相等，并确认区域单连通 |

## 4. 对称性在不同类型积分中的应用

| 积分类型 | 对称性应用要点 |
|:---------|:--------------|
| **二重积分** | 奇偶对称性、轮换对称性（见 §1.3） |
| **三重积分** | 奇偶对称性、轮换对称性（见 §2.3） |
| **第一类曲线积分** | 关于 $x$ 轴对称：$f$ 为 $y$ 奇函数则 $0$，偶函数则 $\times2$ |
| **第二类曲线积分** | 若曲线关于 $x$ 轴对称，$P$ 为 $y$ 偶则 $0$（需注意方向） |
| **第一类曲面积分** | 关于坐标面对称：$f$ 为奇函数则 $0$，偶函数则 $\times2$ |
| **第二类曲面积分** | 利用对称性时需同时考虑侧的方向，较复杂，一般用奇零偶倍处理第二类时需结合被积函数和侧 |

**常用口诀**：
> **一类用奇偶，二类看方向；
> 平格林，空高斯，曲线围面斯托克斯。**

---

# 十一、典型例题思路

#### 例1 用格林公式计算曲线积分
计算 $I=\oint_L (x^2y+3xe^x)\,dx + (x^3-y^2\sin y)\,dy$，其中 $L$ 为逆时针方向的圆周 $x^2+y^2=a^2$。

**解**：$P=x^2y+3xe^x$，$Q=x^3-y^2\sin y$，则
$\frac{\partial Q}{\partial x}-\frac{\partial P}{\partial y}=3x^2 - x^2 = 2x^2$。

由格林公式：
$$
I = \iint_{x^2+y^2\le a^2} 2x^2\,dxdy
$$
利用极坐标或对称性：$\iint x^2\,dxdy = \frac12\iint (x^2+y^2)\,dxdy = \frac12\int_0^{2\pi}d\theta\int_0^a r^2\cdot r\,dr = \frac12\cdot2\pi\cdot\frac{a^4}{4} = \frac{\pi a^4}{4}$。
所以 $I = 2\cdot\frac{\pi a^4}{4} = \frac{\pi a^4}{2}$。

#### 例2 用高斯公式计算曲面积分
计算 $I=\oiint_{\Sigma} x^3\,dydz + y^3\,dzdx + z^3\,dxdy$，其中 $\Sigma$ 是球面 $x^2+y^2+z^2=a^2$ 的外侧。

**解**：$P=x^3,\;Q=y^3,\;R=z^3$，散度 $\frac{\partial P}{\partial x}+\frac{\partial Q}{\partial y}+\frac{\partial R}{\partial z}=3(x^2+y^2+z^2)=3r^2$。

由高斯公式：
$$
I = \iiint_{x^2+y^2+z^2\le a^2} 3(x^2+y^2+z^2)\,dV
$$
球坐标：$dV = r^2\sin\varphi\,drd\varphi d\theta$，$x^2+y^2+z^2=r^2$，
$$
I = 3\int_0^{2\pi}d\theta\int_0^\pi d\varphi\int_0^a r^2\cdot r^2\sin\varphi\,dr = 3\cdot2\pi\cdot2\cdot\frac{a^5}{5} = \frac{12\pi a^5}{5}
$$

#### 例3 用斯托克斯公式计算曲线积分
计算 $I=\oint_L y\,dx+z\,dy+x\,dz$，其中 $L$ 是平面 $x+y+z=1$ 与坐标面所围三角形区域的边界，方向从 $z$ 轴正向看为逆时针。

**解**：$P=y,\;Q=z,\;R=x$，旋度：
$$
\nabla\times\mathbf{F} = \begin{vmatrix}
\mathbf{i} & \mathbf{j} & \mathbf{k}\\
\frac{\partial}{\partial x} & \frac{\partial}{\partial y} & \frac{\partial}{\partial z}\\
y & z & x
\end{vmatrix} = (-1,-1,-1)
$$

**右手定则判断侧**：题目说"方向从 $z$ 轴正向看为逆时针"，即从 $z$ 轴正方向往下看，曲线绕三角形逆时针。用右手定则：右手四指沿 $L$ 逆时针方向弯曲时，大拇指指向 $z$ 轴正方向一侧，而平面 $x+y+z=1$ 的上侧法向量方向为 $(1,1,1)$（指向 $z$ 轴正方向一侧），两者一致，故可取

取 $\Sigma$ 为平面 $x+y+z=1$ 上被 $L$ 所围的三角形区域（上侧，法向量与 $(1,1,1)$ 同向），则
$$
I = \iint_{\Sigma} (-1,-1,-1)\cdot d\mathbf{S} = -\iint_{\Sigma} (1,1,1)\cdot \mathbf{n}\,dS
$$

平面 $x+y+z=1$ 的单位法向量 $\mathbf{n}=(\frac{1}{\sqrt3},\frac{1}{\sqrt3},\frac{1}{\sqrt3})$（上侧），则
$$
(1,1,1)\cdot\mathbf{n} = \frac{3}{\sqrt3} = \sqrt3
$$
所以 $I = -\sqrt3\cdot\iint_{\Sigma} dS$。$\Sigma$ 面积为边长为 $\sqrt2$ 的等边三角形面积 $=\frac{\sqrt3}{2}\cdot(\sqrt2)^2 = \sqrt3$？实际上三角形三顶点 $(1,0,0),(0,1,0),(0,0,1)$，边长 $\sqrt2$，面积 $=\frac{\sqrt3}{2}\cdot(\sqrt2)^2 = \sqrt3$。
所以 $I = -\sqrt3\cdot\sqrt3 = -3$。

---

# 十二、易错点与高频考点

## ⚠️ 易错点清单

| 易错点 | 正解 |
|:-------|:-----|
| 对弧长曲线积分误以为与方向有关 | ❌ 第一类曲线积分与方向无关 |
| 对坐标曲线积分用参数方程时代入顺序反了 | ❌ 参数应从起点到终点 |
| 格林公式忘记检查 $P,Q$ 偏导连续（奇点） | ❌ 若有奇点需挖洞处理 |
| 格林公式方向混淆（正向定义） | ✅ 正向：区域始终在左侧（逆时针） |
| 曲面积分投影时忘记正负号（侧的选择） | ❌ 上侧 $+$ 下侧 $-$，前侧 $+$ 后侧 $-$，右侧 $+$ 左侧 $-$ |
| 两类曲面积分转换时单位法向量方向弄反 | ❌ 法向量方向必须与所选侧一致 |
| 高斯公式中曲面取内侧还是外侧 | ❌ 高斯公式要求**外侧**，内侧需加负号 |
| 斯托克斯公式中曲线方向与曲面侧不满足右手法则 | ❌ 必须右手螺旋 |
| 三重积分柱坐标与球坐标中的体积元弄错 | ✅ 柱：$dV = r\,drd\theta dz$；球：$dV = r^2\sin\varphi\,drd\varphi d\theta$ |
| 积分与路径无关的条件忽略单连通区域 | ❌ 若区域非单连通（有洞），需额外处理 |
| 对弧长曲线积分计算时 $ds$ 表达式漏根号 | ✅ $\sqrt{1+(y')^2}\,dx$ |
| 对面积曲面积分计算时 $dS$ 表达式漏根号 | ✅ $\sqrt{1+z_x^2+z_y^2}\,dxdy$ |
| 对称性应用时未检查被积函数的奇偶性是否匹配 | ❌ 只有奇函数在对称区域上积分才为零 |
| 轮换对称性的使用条件：区域对称 | ❌ 必须有区域关于交换变量的对称性 |

## 📌 高频考点

1. **二重积分计算**（直角坐标换序、极坐标）
2. **三重积分计算**（柱坐标、球坐标）
3. **二重积分对称性**（含轮换对称性）
4. **二重积分变量变换与雅可比行列式**（极坐标、广义极坐标等）
5. **格林公式**（判断路径无关、求原函数、求平面图形面积）
6. **高斯公式**（封闭曲面积分计算）
7. **斯托克斯公式**（空间曲线积分转化为曲面积分）
8. **两类曲线积分、两类曲面积分的互转**
9. **曲线积分与路径无关的应用**
10. **综合运用三大公式求解**

## 💡 常用结论速记

$$
\iint_{x^2+y^2\le R^2} x^2\,dxdy = \iint_{x^2+y^2\le R^2} y^2\,dxdy = \frac{\pi R^4}{4}
$$

$$
\iiint_{x^2+y^2+z^2\le R^2} x^2\,dV = \iiint y^2\,dV = \iiint z^2\,dV = \frac{4\pi R^5}{15}
$$

$$
\iint_{x^2+y^2\le R^2} (x^2+y^2)^k\,dxdy = \frac{2\pi R^{2(k+1)}}{2(k+1)}
$$

$$
\iiint_{x^2+y^2+z^2\le R^2} (x^2+y^2+z^2)^k\,dV = 4\pi\int_0^R r^{2k+2}\,dr = \frac{4\pi R^{2k+3}}{2k+3}
$$

---

*本专题涵盖了考研数学一中多元函数积分学的全部核心内容：二重积分、三重积分、雅可比行列式、对称性、中值定理、两类曲线积分、两类曲面积分、格林公式、高斯公式、斯托克斯公式及其内在联系。建议配合《级数专题》及《公式整理》一起使用。*
